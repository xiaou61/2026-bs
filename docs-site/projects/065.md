---
title: 065 - 名城小区物业管理系统
description: 基于SpringBoot+Vue3的名城小区物业管理系统（Spring Boot 2.7.18 + MyBatis + PageHelper + Redis + Vue3 + Element Plus + ECharts + 三角色：管理员/物业人员/业主 + 楼栋房屋管理+物业缴费+报修工单+投诉建议+访客登记+车位管理+运营看板）
---

# 名城小区物业管理系统

> 基于SpringBoot+Vue3的名城小区物业管理系统（Spring Boot 2.7.18 + MyBatis + PageHelper + Redis + Vue3 + Element Plus + ECharts + 三角色：管理员/物业人员/业主 + 楼栋房屋管理+物业缴费+报修工单+投诉建议+访客登记+车位管理+运营看板）

## 项目概览

| 属性 | 内容 |
|------|------|
| 项目编号 | 065 |
| 项目名称 | 名城小区物业管理系统 |
| 项目全称 | 基于SpringBoot+Vue3的名城小区物业管理系统（Spring Boot 2.7.18 + MyBatis + PageHelper + Redis + Vue3 + Element Plus + ECharts + 三角色：管理员/物业人员/业主 + 楼栋房屋管理+物业缴费+报修工单+投诉建议+访客登记+车位管理+运营看板） |
| 项目类型 | Web应用 |
| 后端目录 | `065-backend` |
| 前端目录 | `065-frontend` |
| Spring Boot 版本 | 2.7.18 |
| 后端默认端口 | `8065` |
| 数据库名 | `Shanghai` |
| 后端 Controller | 11 个 |
| 后端 API 接口 | 51 个 |
| 数据库表 | 9 张 |
| 实体类 | 9 个 |
| 前端页面 | 13 个 |

## 项目简介

本项目是名城小区物业管理系统后端服务，配套 `065-frontend` 使用。系统覆盖登录注册、用户管理、楼栋房屋、物业缴费、报修工单、投诉建议、访客登记、车位管理、公告和物业看板等流程。

## 项目截图

已收录 **13** 张运行截图。

### 管理员

#### admin-01-dashboard

![admin-01-dashboard](/previews/assets/065/admin-01-dashboard.png)

#### admin-02-user

![admin-02-user](/previews/assets/065/admin-02-user.png)

#### admin-03-building

![admin-03-building](/previews/assets/065/admin-03-building.png)

#### admin-04-house

![admin-04-house](/previews/assets/065/admin-04-house.png)

#### admin-05-fee

![admin-05-fee](/previews/assets/065/admin-05-fee.png)

#### admin-06-repair

![admin-06-repair](/previews/assets/065/admin-06-repair.png)

#### admin-07-complaint

![admin-07-complaint](/previews/assets/065/admin-07-complaint.png)

#### admin-08-visitor

![admin-08-visitor](/previews/assets/065/admin-08-visitor.png)

#### admin-09-parking

![admin-09-parking](/previews/assets/065/admin-09-parking.png)

#### admin-10-announcement

![admin-10-announcement](/previews/assets/065/admin-10-announcement.png)

#### admin-11-profile

![admin-11-profile](/previews/assets/065/admin-11-profile.png)

### 访客

#### guest-01-login

![guest-01-login](/previews/assets/065/guest-01-login.png)

#### guest-02-register

![guest-02-register](/previews/assets/065/guest-02-register.png)

## 技术栈

### 后端

- Spring Boot 2.7.18
- MyBatis 3.5 + PageHelper 1.4.7
- MySQL 8.0
- Redis
- JWT认证
- Hutool

### 前端

- Vue 3.4.0
- Element Plus 2.4.4
- Vite
- Pinia
- Axios
- ECharts 5.4.3

## 角色权限

系统支持 **3** 种角色，各角色权限如下：

### - ADMIN

- 系统管理员
- 负责账号与基础数据全局管理

### - STAFF

- 物业人员
- 负责收费
- 报修
- 投诉
- 访客等业务处理

### - OWNER

- 业主
- 负责查看房屋
- 缴费
- 报修
- 投诉与访客申请

## 功能模块

| 模块 | 说明 |
|------|------|
| **用户与认证** |  |
| **用户管理** |  |
| **楼栋管理** |  |
| **房屋管理** |  |
| **物业缴费** |  |
| **报修工单** |  |
| **投诉建议** |  |
| **访客登记** |  |
| **车位管理** |  |
| **公告中心** |  |
| **运营看板** |  |

### 模块架构

```
名城小区物业管理系统
├── 用户与认证
├── 用户管理
├── 楼栋管理
├── 房屋管理
├── 物业缴费
├── 报修工单
├── 投诉建议
├── 访客登记
├── 车位管理
├── 公告中心
├── 运营看板
```

## 数据库设计

本项目共包含 **9** 张数据库表。

### sys_user

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | AUTO_INCREMENT PRIMARY KEY, |
| `username` | VARCHAR(50) | NOT NULL UNIQUE, |
| `password` | VARCHAR(100) | NOT NULL, |
| `nickname` | VARCHAR(50) | , |
| `phone` | VARCHAR(20) | , |
| `email` | VARCHAR(100) | , |
| `role` | VARCHAR(20) | DEFAULT 'OWNER', |
| `status` | INT | DEFAULT 1, |
| ... | ... | 更多字段请查看源码 |

### building

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | AUTO_INCREMENT PRIMARY KEY, |
| `name` | VARCHAR(100) | NOT NULL, |
| `floors` | INT | DEFAULT 0, |
| `status` | INT | DEFAULT 1, |
| `create_time` | DATETIME | DEFAULT CURRENT_TIMESTAMP, |
| `update_time` | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMEST |

### house

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | AUTO_INCREMENT PRIMARY KEY, |
| `building_id` | BIGINT | NOT NULL, |
| `unit_no` | VARCHAR(20) | NOT NULL, |
| `room_no` | VARCHAR(20) | NOT NULL, |
| `area` | DECIMAL(10,2) | DEFAULT 0, |
| `owner_id` | BIGINT | , |
| `status` | INT | DEFAULT 1, |
| `create_time` | DATETIME | DEFAULT CURRENT_TIMESTAMP, |
| ... | ... | 更多字段请查看源码 |

### fee_order

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | AUTO_INCREMENT PRIMARY KEY, |
| `order_no` | VARCHAR(50) | NOT NULL UNIQUE, |
| `house_id` | BIGINT | NOT NULL, |
| `owner_id` | BIGINT | NOT NULL, |
| `amount` | DECIMAL(10,2) | NOT NULL, |
| `fee_month` | VARCHAR(20) | NOT NULL, |
| `status` | INT | DEFAULT 0, |
| `pay_time` | DATETIME | , |
| ... | ... | 更多字段请查看源码 |

### repair_order

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | AUTO_INCREMENT PRIMARY KEY, |
| `order_no` | VARCHAR(50) | NOT NULL UNIQUE, |
| `house_id` | BIGINT | NOT NULL, |
| `owner_id` | BIGINT | NOT NULL, |
| `title` | VARCHAR(120) | NOT NULL, |
| `content` | VARCHAR(500) | NOT NULL, |
| `status` | INT | DEFAULT 0, |
| `assignee_id` | BIGINT | , |
| ... | ... | 更多字段请查看源码 |

### complaint

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | AUTO_INCREMENT PRIMARY KEY, |
| `order_no` | VARCHAR(50) | NOT NULL UNIQUE, |
| `owner_id` | BIGINT | NOT NULL, |
| `title` | VARCHAR(120) | NOT NULL, |
| `content` | VARCHAR(500) | NOT NULL, |
| `status` | INT | DEFAULT 0, |
| `reply` | VARCHAR(255) | , |
| `create_time` | DATETIME | DEFAULT CURRENT_TIMESTAMP, |
| ... | ... | 更多字段请查看源码 |

### visitor_record

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | AUTO_INCREMENT PRIMARY KEY, |
| `order_no` | VARCHAR(50) | NOT NULL UNIQUE, |
| `owner_id` | BIGINT | NOT NULL, |
| `visitor_name` | VARCHAR(50) | NOT NULL, |
| `visitor_phone` | VARCHAR(20) | NOT NULL, |
| `visit_time` | DATETIME | NOT NULL, |
| `status` | INT | DEFAULT 0, |
| `remark` | VARCHAR(255) | , |
| ... | ... | 更多字段请查看源码 |

### parking_slot

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | AUTO_INCREMENT PRIMARY KEY, |
| `slot_no` | VARCHAR(50) | NOT NULL UNIQUE, |
| `location` | VARCHAR(100) | , |
| `owner_id` | BIGINT | , |
| `status` | INT | DEFAULT 0, |
| `create_time` | DATETIME | DEFAULT CURRENT_TIMESTAMP, |
| `update_time` | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMEST |

### announcement

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | AUTO_INCREMENT PRIMARY KEY, |
| `title` | VARCHAR(100) | NOT NULL, |
| `content` | TEXT | NOT NULL, |
| `admin_id` | BIGINT | , |
| `status` | INT | DEFAULT 1, |
| `create_time` | DATETIME | DEFAULT CURRENT_TIMESTAMP, |
| `update_time` | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMEST |

## API 接口

本项目共包含 **11** 个 Controller，**51** 个 API 接口。

### Announcement

| 方法 | 路径 |
|------|------|
| GET | `/api/announcement/list` |
| GET | `/api/announcement/page` |
| DELETE | `/api/announcement/{id}` |

### Auth

| 方法 | 路径 |
|------|------|
| POST | `/api/auth/login` |
| POST | `/api/auth/register` |
| GET | `/api/auth/info` |
| PUT | `/api/auth/password` |
| POST | `/api/auth/logout` |

### Building

| 方法 | 路径 |
|------|------|
| GET | `/api/building/list` |
| GET | `/api/building/page` |
| DELETE | `/api/building/{id}` |

### Complaint

| 方法 | 路径 |
|------|------|
| GET | `/api/complaint/my-page` |
| GET | `/api/complaint/page` |
| PUT | `/api/complaint/reply` |
| DELETE | `/api/complaint/{id}` |

### Dashboard

| 方法 | 路径 |
|------|------|
| GET | `/api/dashboard/stats` |
| GET | `/api/dashboard/trend` |

### FeeOrder

| 方法 | 路径 |
|------|------|
| GET | `/api/fee/my-page` |
| GET | `/api/fee/page` |
| PUT | `/api/fee/pay/{id}` |
| DELETE | `/api/fee/{id}` |

### House

| 方法 | 路径 |
|------|------|
| GET | `/api/house/list` |
| GET | `/api/house/page` |
| DELETE | `/api/house/{id}` |

### ParkingSlot

| 方法 | 路径 |
|------|------|
| GET | `/api/parking/list` |
| GET | `/api/parking/page` |
| DELETE | `/api/parking/{id}` |

> 还有 3 个 Controller 未展示。

## 前端页面

本项目前端共包含 **13** 个页面组件。

| 目录 | 页面 | 路径 |
|------|------|------|
| announcement | index | `announcement/index` |
| building | index | `building/index` |
| complaint | index | `complaint/index` |
| 根目录 | Dashboard | `Dashboard` |
| 根目录 | Layout | `Layout` |
| 根目录 | Login | `Login` |
| fee | index | `fee/index` |
| house | index | `house/index` |
| parking | index | `parking/index` |
| profile | index | `profile/index` |
| repair | index | `repair/index` |
| user | index | `user/index` |
| visitor | index | `visitor/index` |

## 项目结构

```
065-backend/
├── src/main/java/com/xiaou/
│   ├── controller/     # 控制器 (11 个)
│   ├── entity/         # 实体类 (9 个)
│   ├── mapper/         # MyBatis映射
│   ├── service/        # 业务逻辑
│   ├── dto/            # 数据传输对象
│   ├── config/         # 配置类
│   └── common/         # 通用类
├── src/main/resources/
│   ├── application.yml # 配置文件
│   └── sql/            # 数据库脚本
└── pom.xml             # Maven配置

065-frontend/
├── src/views/         # 页面组件 (13 个)
├── src/api/           # API封装
├── src/router/        # 路由配置
├── src/utils/         # 工具类
├── package.json       # 依赖配置
└── vite.config.js     # Vite配置
```

## 快速启动

### 环境要求

| 工具 | 版本 |
|------|------|
| JDK | 17+ |
| Maven | 3.8+ |
| 数据库 | H2 内存模式（默认） |
| Node.js | 以 package.json 为准 |

### 后端启动

默认配置使用 H2 内存数据库，通常无需预先创建外部数据库。 如需切换外部数据库，请先查看 `065-backend/sql/init.sql`。

```bash
cd 065-backend
mvn spring-boot:run
```

### 前端启动

```bash
cd 065-frontend
npm install
npm run dev
```

### 默认账号

| 角色 | 用户名 | 密码 | 说明 |
|------|--------|------|------|
| 系统管理员 | ``admin`` | ``123456`` |  |
| 物业人员 | ``staff`` | ``123456`` |  |
| 业主 | ``owner`` | ``123456`` |  |

### 开发注意事项

1. 默认使用 H2 内存数据库，无需预先创建外部数据库
2. 如需切换外部数据库，请查看 `065-backend/sql/init.sql`
3. 后端默认端口为 `8065`，前端代理需配置对应端口
4. 默认账号密码请查看上方表格，首次登录后建议修改密码

## 检查报告

**检查日期：** 2026-04-29

**综合结论：** 本轮已修复默认环境强依赖 MySQL/Redis/8080、H2 自举缺失、MySQL 驱动旧坐标、PageHelper 固定 MySQL 方言、JWT 不兼容 Bearer token、Redis 不可用会阻断默认演示登录、未登录/越权仍返回 HTTP 200、用户密码响应泄露、H2 下收费趋势统计 SQL 别名冲突、前端代理仍指向 8080 等问题。当前后端可在默认 H2 环境下启动并通过自动化冒烟测试，前端可完成生产构建，真实 HTTP 核心链路抽测通过。MySQL profile 已按本机账号 

> 完整检查报告请查看 `docs/checks/065-property-management.md`

