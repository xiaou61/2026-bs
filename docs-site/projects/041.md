---
title: 041 - 在线心理评测与咨询系统 🔥最新
description: 基于SpringBoot的在线心理评测与咨询系统的设计与实现（MyBatis-Plus 3.5.5 + Redis + WebSocket + MySQL + Vue3 + Element Plus）
---

# 在线心理评测与咨询系统 🔥最新

> 基于SpringBoot的在线心理评测与咨询系统的设计与实现（MyBatis-Plus 3.5.5 + Redis + WebSocket + MySQL + Vue3 + Element Plus）

## 项目概览

| 属性 | 内容 |
|------|------|
| 项目编号 | 041 |
| 项目名称 | 在线心理评测与咨询系统 🔥最新 |
| 项目全称 | 基于SpringBoot的在线心理评测与咨询系统的设计与实现（MyBatis-Plus 3.5.5 + Redis + WebSocket + MySQL + Vue3 + Element Plus） |
| 项目类型 | Web应用 |
| Spring Boot 版本 | 3.2.0 |
| 后端默认端口 | `6379` |
| 数据库名 | `Shanghai` |
| 数据库表 | 52 张 |
| 前端页面 | 14 个 |

## 项目简介

本项目是基于 Spring Boot 3.2、MyBatis-Plus、JWT 的在线心理评测与咨询系统后端，提供用户登录注册、心理量表、题目查询、咨询师列表、心理文章、测评提交和咨询预约等核心接口。

## 项目截图

已收录 **3** 张运行截图。

### 访客

#### guest-01-home

![guest-01-home](/previews/assets/041/guest-01-home.png)

#### guest-01-login

![guest-01-login](/previews/assets/041/guest-01-login.png)

#### guest-02-register

![guest-02-register](/previews/assets/041/guest-02-register.png)

## 技术栈

### 后端

- Spring Boot 3.x
- MyBatis-Plus
- MySQL 8.0

### 前端

- Vue 3
- Element Plus

## 角色权限

系统支持 **2** 种角色，各角色权限如下：

### 管理员

- 基本操作权限

### 用户

- 基本操作权限

## 数据库设计

本项目共包含 **13** 张数据库表。

### user

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `username` | VARCHAR(50) | NOT NULL UNIQUE, |
| `password` | VARCHAR(255) | NOT NULL, |
| `nickname` | VARCHAR(50) | , |
| `avatar` | VARCHAR(255) | , |
| `gender` | VARCHAR(10) | , |
| `age` | INT | , |
| `phone` | VARCHAR(20) | , |
| ... | ... | 更多字段请查看源码 |

### counselor

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `user_id` | BIGINT | NOT NULL, |
| `real_name` | VARCHAR(50) | NOT NULL, |
| `certificate` | VARCHAR(255) | , |
| `certificate_no` | VARCHAR(100) | , |
| `specialties` | VARCHAR(500) | , |
| `introduction` | TEXT | , |
| `style` | VARCHAR(500) | , |
| ... | ... | 更多字段请查看源码 |

### scale

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `name` | VARCHAR(100) | NOT NULL, |
| `category` | VARCHAR(50) | , |
| `description` | TEXT | , |
| `question_count` | INT | DEFAULT 0, |
| `duration` | INT | DEFAULT 15, |
| `status` | VARCHAR(20) | DEFAULT 'ACTIVE', |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP, |
| ... | ... | 更多字段请查看源码 |

### question

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `scale_id` | BIGINT | NOT NULL, |
| `content` | TEXT | NOT NULL, |
| `type` | VARCHAR(20) | DEFAULT 'SINGLE', |
| `options` | TEXT | , |
| `score_rule` | TEXT | , |
| `order_num` | INT | , |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP, |
| ... | ... | 更多字段请查看源码 |

### assessment_record

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `user_id` | BIGINT | NOT NULL, |
| `scale_id` | BIGINT | NOT NULL, |
| `answers` | TEXT | , |
| `total_score` | INT | , |
| `result_level` | VARCHAR(50) | , |
| `report` | TEXT | , |
| `status` | VARCHAR(20) | DEFAULT 'COMPLETED', |
| ... | ... | 更多字段请查看源码 |

### time_slot

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `counselor_id` | BIGINT | NOT NULL, |
| `slot_date` | DATE | NOT NULL, |
| `slot_time` | TIME | NOT NULL, |
| `duration` | INT | DEFAULT 60, |
| `status` | VARCHAR(20) | DEFAULT 'AVAILABLE', |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP, |
| `updated_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMEST |

### appointment

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `user_id` | BIGINT | NOT NULL, |
| `counselor_id` | BIGINT | NOT NULL, |
| `time_slot_id` | BIGINT | , |
| `requirement` | TEXT | , |
| `price` | DECIMAL(10,2) | , |
| `status` | VARCHAR(20) | DEFAULT 'PENDING', |
| `payment_status` | VARCHAR(20) | DEFAULT 'UNPAID', |
| ... | ... | 更多字段请查看源码 |

### consult_record

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `appointment_id` | BIGINT | NOT NULL, |
| `counselor_id` | BIGINT | NOT NULL, |
| `user_id` | BIGINT | NOT NULL, |
| `record_content` | TEXT | , |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP, |
| `updated_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMEST |

### review

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `appointment_id` | BIGINT | NOT NULL, |
| `user_id` | BIGINT | NOT NULL, |
| `counselor_id` | BIGINT | NOT NULL, |
| `rating` | INT | , |
| `comment` | TEXT | , |
| `is_anonymous` | BOOLEAN | DEFAULT FALSE, |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP, |
| ... | ... | 更多字段请查看源码 |

### article

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `title` | VARCHAR(200) | NOT NULL, |
| `cover` | VARCHAR(255) | , |
| `category` | VARCHAR(50) | , |
| `content` | TEXT | , |
| `tags` | VARCHAR(255) | , |
| `author_id` | BIGINT | , |
| `view_count` | INT | DEFAULT 0, |
| ... | ... | 更多字段请查看源码 |

> 还有 3 张表未展示，完整表结构请查看 `041-backend/src/main/resources/sql/init.sql`

## 前端页面

本项目前端共包含 **14** 个页面组件。

| 目录 | 页面 | 路径 |
|------|------|------|
| admin | AdminDashboard | `admin/AdminDashboard` |
| admin | AdminLogin | `admin/AdminLogin` |
| counselor | CounselorDashboard | `counselor/CounselorDashboard` |
| counselor | CounselorLogin | `counselor/CounselorLogin` |
| user | ArticleDetail | `user/ArticleDetail` |
| user | ArticleList | `user/ArticleList` |
| user | Assessment | `user/Assessment` |
| user | CounselorDetail | `user/CounselorDetail` |
| user | CounselorList | `user/CounselorList` |
| user | ... | 还有 5 个页面 |

## 项目结构

```
041-backend/
├── src/main/java/com/xiaou/
│   ├── controller/     # 控制器
│   ├── entity/         # 实体类
│   ├── mapper/         # MyBatis映射
│   ├── service/        # 业务逻辑
│   ├── dto/            # 数据传输对象
│   ├── config/         # 配置类
│   └── common/         # 通用类
├── src/main/resources/
│   ├── application.yml # 配置文件
│   └── sql/            # 数据库脚本
└── pom.xml             # Maven配置

041-frontend/
├── src/views/         # 页面组件 (14 个)
├── src/api/           # API封装
├── src/router/        # 路由配置
├── src/utils/         # 工具类
├── package.json       # 依赖配置
└── vite.config.js     # Vite配置
```

## 快速启动

### 环境要求

| 工具 | 版本 |
|------|------|
| JDK | 17+ |
| Maven | 3.8+ |
| MySQL | 8.0+ |
| Node.js | 18+ |

### 后端启动

```bash
cd 041-backend
# 1. 创建数据库并执行 init.sql
# 2. 修改 application.yml 数据库配置
# spring.datasource.url=jdbc:mysql://localhost:3306/Shanghai
# spring.datasource.username=root
# spring.datasource.password=你的密码
mvn spring-boot:run
```

### 前端启动

```bash
cd 041-frontend
npm install
# 修改后端接口地址（通常在 .env.development 或 src/utils/request.js 中）
npm run dev
```

### 默认账号

| 角色 | 用户名 | 密码 | 说明 |
|------|--------|------|------|
| 管理员 | `admin` | `123456` |  |
| 用户 | `user1` | `123456` |  |

### 开发注意事项

1. 数据库配置：修改 `application.yml` 中的数据库连接信息（数据库名：`Shanghai`）
2. 后端启动前需先创建数据库并执行 SQL 初始化脚本
3. 后端默认端口为 `6379`，前端代理需配置对应端口
4. 默认账号密码请查看上方表格，首次登录后建议修改密码

## 检查报告

**检查日期：** 2026-04-26

**综合结论：** 本轮已修复 Spring Boot 3 与旧 MyBatis-Plus starter 不兼容、默认环境强依赖 MySQL/Redis、H2 自举缺失、初始化账号密码哈希不可用、登录返回密码哈希泄露、MyBatis-Plus 自动时间填充失效以及前端依赖锁缺失等问题。当前后端可在默认 H2 环境下通过冒烟测试并真实启动，登录、量表、题目、咨询师、文章、测评提交和预约创建接口抽测通过；前端可构建并通过 Vite 代理完成登录接口联调。

> 完整检查报告请查看 `docs/checks/041-psychology-assessment-consultation-system.md`

