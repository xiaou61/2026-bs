---
title: 072 - 基于SpringBoot和Vue的哈尔滨文旅系统
description: 基于SpringBoot+Vue3的哈尔滨文旅系统（Spring Boot 2.7.18 + MyBatis-Plus 3.5.5 + Redis + Vue3 + Element Plus + ECharts + 双角色：管理员/用户 + 冰雪旅游+景点门票预订+路线规划+酒店餐厅推荐+活动报名+游记分享+评价收藏+钱包支付+运营看板）
---

# 基于SpringBoot和Vue的哈尔滨文旅系统

> 基于SpringBoot+Vue3的哈尔滨文旅系统（Spring Boot 2.7.18 + MyBatis-Plus 3.5.5 + Redis + Vue3 + Element Plus + ECharts + 双角色：管理员/用户 + 冰雪旅游+景点门票预订+路线规划+酒店餐厅推荐+活动报名+游记分享+评价收藏+钱包支付+运营看板）

## 项目概览

| 属性 | 内容 |
|------|------|
| 项目编号 | 072 |
| 项目名称 | 基于SpringBoot和Vue的哈尔滨文旅系统 |
| 项目全称 | 基于SpringBoot+Vue3的哈尔滨文旅系统（Spring Boot 2.7.18 + MyBatis-Plus 3.5.5 + Redis + Vue3 + Element Plus + ECharts + 双角色：管理员/用户 + 冰雪旅游+景点门票预订+路线规划+酒店餐厅推荐+活动报名+游记分享+评价收藏+钱包支付+运营看板） |
| 项目类型 | Web应用 |
| 后端目录 | `072-backend` |
| 前端目录 | `072-frontend` |
| Spring Boot 版本 | 2.7.18 |
| 后端默认端口 | `8072` |
| 数据库名 | `Shanghai` |
| 数据库表 | 14 张 |
| 前端页面 | 20 个 |

## 项目简介

### 项目简介
哈尔滨文旅综合服务平台，聚焦冰雪之城哈尔滨的旅游文化推广。涵盖冰雪景点、历史建筑、特色美食、住宿推荐、旅游路线、活动赛事、游记分享等全方位文旅服务，为游客提供一站式哈尔滨旅游攻略与预订服务。

### 核心功能

### 技术栈
**后端**: Spring Boot 2.7.18 + MyBatis-Plus 3.5.5 + MySQL 8.0 + Redis + JWT 0.9.1 + Hutool 5.8.25
**前端**: Vue 3.4.0 + Element Plus 2.4.4 + Pinia 2.1.7 + Vue Router 4.2.5 + Axios 1.6.2 + ECharts 5.4.3

---

## 项目截图

已收录 **19** 张运行截图。

### 管理员

#### admin-01-dashboard

![admin-01-dashboard](/previews/assets/072/admin-01-dashboard.png)

#### admin-02-spots

![admin-02-spots](/previews/assets/072/admin-02-spots.png)

#### admin-03-routes

![admin-03-routes](/previews/assets/072/admin-03-routes.png)

#### admin-04-order

![admin-04-order](/previews/assets/072/admin-04-order.png)

#### admin-05-orders

![admin-05-orders](/previews/assets/072/admin-05-orders.png)

#### admin-06-hotels

![admin-06-hotels](/previews/assets/072/admin-06-hotels.png)

#### admin-07-restaurants

![admin-07-restaurants](/previews/assets/072/admin-07-restaurants.png)

#### admin-08-activities

![admin-08-activities](/previews/assets/072/admin-08-activities.png)

#### admin-09-notes

![admin-09-notes](/previews/assets/072/admin-09-notes.png)

#### admin-10-reviews

![admin-10-reviews](/previews/assets/072/admin-10-reviews.png)

#### admin-11-profile

![admin-11-profile](/previews/assets/072/admin-11-profile.png)

#### admin-12-favorites

![admin-12-favorites](/previews/assets/072/admin-12-favorites.png)

#### admin-13-wallet

![admin-13-wallet](/previews/assets/072/admin-13-wallet.png)

#### admin-14-announcements

![admin-14-announcements](/previews/assets/072/admin-14-announcements.png)

#### admin-15-users

![admin-15-users](/previews/assets/072/admin-15-users.png)

#### admin-16-spots

![admin-16-spots](/previews/assets/072/admin-16-spots.png)

#### admin-17-orders

![admin-17-orders](/previews/assets/072/admin-17-orders.png)

### 访客

#### guest-01-login

![guest-01-login](/previews/assets/072/guest-01-login.png)

#### guest-02-register

![guest-02-register](/previews/assets/072/guest-02-register.png)

## 技术栈

### 后端

- Spring Boot 2.7.18
- MyBatis-Plus 3.5.5
- MySQL 8.0
- Redis 6.0+
- JWT 0.9.1
- Hutool 5.8.25

### 前端

- Vue 3.4.0
- Element Plus 2.4.4
- Pinia 2.1.7
- Vue Router 4.2.5
- Axios 1.6.2
- ECharts 5.4.3
- Vite 5.0

## 角色权限

系统支持 **2** 种角色，各角色权限如下：

### - 管理员 (admin)

- 全平台管理权限
- 景点/酒店/餐厅/活动/游记审核
- 数据统计看板

### - 普通用户 (user)

- 浏览景点路线
- 门票预订
- 游记发布
- 评价互动
- 收藏

## 功能模块

| 模块 | 说明 |
|------|------|
| **用户管理** |  |
| **景点管理** |  |
| **路线规划** |  |
| **门票预订** |  |
| **酒店推荐** |  |
| **餐厅推荐** |  |
| **活动报名** |  |
| **游记分享** |  |
| **评价系统** |  |
| **收藏功能** |  |
| **钱包系统** |  |
| **公告管理** |  |
| **数据统计** |  |

### 模块架构

```
基于SpringBoot和Vue的哈尔滨文旅系统
├── 用户管理
├── 景点管理
├── 路线规划
├── 门票预订
├── 酒店推荐
├── 餐厅推荐
├── 活动报名
├── 游记分享
├── 评价系统
├── 收藏功能
├── 钱包系统
├── 公告管理
├── 数据统计
```

## 数据库设计

本项目共包含 **14** 张数据库表。

### user

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `username` | VARCHAR(50) | NOT NULL UNIQUE, |
| `password` | VARCHAR(100) | NOT NULL, |
| `nickname` | VARCHAR(50) | , |
| `avatar` | VARCHAR(255) | , |
| `phone` | VARCHAR(20) | , |
| `email` | VARCHAR(100) | , |
| `role` | VARCHAR(20) | DEFAULT 'user', |
| ... | ... | 更多字段请查看源码 |

### scenic_spot

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `name` | VARCHAR(100) | NOT NULL, |
| `description` | VARCHAR(500) | , |
| `detail` | TEXT | , |
| `location` | VARCHAR(200) | , |
| `category` | VARCHAR(50) | , |
| `cover_img` | VARCHAR(255) | , |
| `images` | TEXT | , |
| ... | ... | 更多字段请查看源码 |

### route

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `title` | VARCHAR(100) | NOT NULL, |
| `description` | TEXT | , |
| `days` | INT | DEFAULT 1, |
| `difficulty` | VARCHAR(20) | DEFAULT 'easy', |
| `category` | VARCHAR(50) | , |
| `cover_img` | VARCHAR(255) | , |
| `estimated_cost` | DECIMAL(10,2) | , |
| ... | ... | 更多字段请查看源码 |

### route_spot

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `route_id` | BIGINT | NOT NULL, |
| `spot_id` | BIGINT | NOT NULL, |
| `order_num` | INT | DEFAULT 1, |
| `stay_hours` | DECIMAL(3,1) | DEFAULT 2.0 |

### ticket_type

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `spot_id` | BIGINT | NOT NULL, |
| `name` | VARCHAR(100) | NOT NULL, |
| `price` | DECIMAL(10,2) | NOT NULL, |
| `stock` | INT | DEFAULT 100, |
| `max_per_order` | INT | DEFAULT 5, |
| `description` | VARCHAR(255) | , |
| `status` | INT | DEFAULT 1 |

### ticket_order

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `order_no` | VARCHAR(50) | NOT NULL UNIQUE, |
| `user_id` | BIGINT | NOT NULL, |
| `spot_id` | BIGINT | NOT NULL, |
| `ticket_type_id` | BIGINT | NOT NULL, |
| `ticket_date` | DATE | NOT NULL, |
| `quantity` | INT | NOT NULL, |
| `total_price` | DECIMAL(10,2) | NOT NULL, |
| ... | ... | 更多字段请查看源码 |

### hotel

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `name` | VARCHAR(100) | NOT NULL, |
| `address` | VARCHAR(255) | , |
| `type` | VARCHAR(50) | , |
| `price_min` | DECIMAL(10,2) | , |
| `price_max` | DECIMAL(10,2) | , |
| `description` | TEXT | , |
| `cover_img` | VARCHAR(255) | , |
| ... | ... | 更多字段请查看源码 |

### restaurant

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `name` | VARCHAR(100) | NOT NULL, |
| `address` | VARCHAR(255) | , |
| `cuisine_type` | VARCHAR(50) | , |
| `price_per_person` | DECIMAL(10,2) | , |
| `description` | TEXT | , |
| `cover_img` | VARCHAR(255) | , |
| `recommended_dish` | VARCHAR(500) | , |
| ... | ... | 更多字段请查看源码 |

### activity

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `title` | VARCHAR(100) | NOT NULL, |
| `description` | TEXT | , |
| `location` | VARCHAR(200) | , |
| `start_time` | DATETIME | , |
| `end_time` | DATETIME | , |
| `cover_img` | VARCHAR(255) | , |
| `register_deadline` | DATETIME | , |
| ... | ... | 更多字段请查看源码 |

### activity_registration

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `activity_id` | BIGINT | NOT NULL, |
| `user_id` | BIGINT | NOT NULL, |
| `register_time` | DATETIME | DEFAULT CURRENT_TIMESTAMP, |
| `status` | INT | DEFAULT 1 |

> 还有 4 张表未展示，完整表结构请查看 `072-backend/src/main/resources/sql/init.sql`

## 前端页面

本项目前端共包含 **20** 个页面组件。

| 目录 | 页面 | 路径 |
|------|------|------|
| 根目录 | ActivityList | `ActivityList` |
| 根目录 | AdminPage | `AdminPage` |
| 根目录 | AnnouncementList | `AnnouncementList` |
| 根目录 | Dashboard | `Dashboard` |
| 根目录 | HotelList | `HotelList` |
| 根目录 | ... | 还有 15 个页面 |

## 项目结构

```
072-backend/
├── src/main/java/com/xiaou/
│   ├── controller/     # 控制器
│   ├── entity/         # 实体类
│   ├── mapper/         # MyBatis映射
│   ├── service/        # 业务逻辑
│   ├── dto/            # 数据传输对象
│   ├── config/         # 配置类
│   └── common/         # 通用类
├── src/main/resources/
│   ├── application.yml # 配置文件
│   └── sql/            # 数据库脚本
└── pom.xml             # Maven配置

072-frontend/
├── src/views/         # 页面组件 (20 个)
├── src/api/           # API封装
├── src/router/        # 路由配置
├── src/utils/         # 工具类
├── package.json       # 依赖配置
└── vite.config.js     # Vite配置
```

## 快速启动

### 环境要求

| 工具 | 版本 |
|------|------|
| JDK | 17+ |
| Maven | 3.8+ |
| 数据库 | H2 内存模式（默认） |
| Node.js | 以 package.json 为准 |

### 后端启动

默认配置使用 H2 内存数据库，通常无需预先创建外部数据库。 如需切换外部数据库，请先查看 `072-backend/sql/init.sql`。

```bash
cd 072-backend
mvn spring-boot:run
```

### 前端启动

```bash
cd 072-frontend
npm install
npm run dev
```

### 默认账号

| 角色 | 用户名 | 密码 | 说明 |
|------|--------|------|------|
| 管理员 | ``admin`` | ``123456`` |  |
| 普通用户 | ``user`` | ``123456`` |  |
| 普通用户 | ``tourist`` | ``123456`` |  |

### 开发注意事项

1. 默认使用 H2 内存数据库，无需预先创建外部数据库
2. 如需切换外部数据库，请查看 `072-backend/sql/init.sql`
3. 后端默认端口为 `8072`，前端代理需配置对应端口
4. 默认账号密码请查看上方表格，首次登录后建议修改密码

## 检查报告

**检查日期：** 2026-05-01

**综合结论：** 本轮已修复默认环境强依赖 MySQL/Redis/8080、H2 自举缺失、MySQL 驱动旧坐标、JDK 17 配置不足、前端路由指向不存在目录、前端 API 导出名与页面不匹配、前端代理仍指向 8080、JWT 不兼容 Bearer token、未登录/越权 HTTP 状态不准确、登录响应密码泄露、后台接口缺少服务端角色校验、订单/游记/评价归属校验不足等问题。当前后端可在默认 H2 环境下启动并通过自动化冒烟测试，前端可完成生产构建，真实 HTTP 文旅主链路和前端代理登录复测通过。

> 完整检查报告请查看 `docs/checks/072-harbin-tourism-system.md`

