---
title: 027 - 线上理发预约系统 🔥
description: 线上理发预约系统（智能预约+会员积分+订单支付）
---

# 线上理发预约系统 🔥

> 线上理发预约系统（智能预约+会员积分+订单支付）

## 项目概览

| 属性 | 内容 |
|------|------|
| 项目编号 | 027 |
| 项目名称 | 线上理发预约系统 🔥 |
| 项目全称 | 线上理发预约系统（智能预约+会员积分+订单支付） |
| 项目类型 | Web应用 |
| 后端目录 | `027-backend` |
| 前端目录 | `027-frontend` |
| Spring Boot 版本 | 3.2.0 |
| 后端默认端口 | `8027` |
| 数据库名 | `db_027` |
| 数据库表 | 12 张 |
| 前端页面 | 9 个 |

## 项目截图

已收录 **2** 张运行截图。

### 访客

#### guest-01-login

![guest-01-login](/previews/assets/027/guest-01-login.png)

#### guest-02-register

![guest-02-register](/previews/assets/027/guest-02-register.png)

## 技术栈

### 后端

- Spring Boot 3.2.0
- MyBatis-Plus 3.5.5
- MySQL 8.0
- JWT 0.12.3
- Hutool工具库

### 前端

- Vue 3.4.0
- Element Plus 2.5.1
- Vite 5.0
- Pinia
- Vue Router 4.2.5
- Axios 1.6.2

## 角色权限

系统支持 **1** 种角色，各角色权限如下：

### 用户

- 用户系统

## 功能模块

| 模块 | 说明 |
|------|------|
| **用户系统** |  |
| **门店管理** |  |
| **理发师管理** |  |
| **服务项目** |  |
| **在线预约** |  |
| **预约管理** |  |
| **会员体系** |  |
| **积分系统** |  |
| **订单支付** |  |
| **评价系统** |  |

### 模块架构

```
线上理发预约系统 🔥
├── 用户系统
├── 门店管理
├── 理发师管理
├── 服务项目
├── 在线预约
├── 预约管理
├── 会员体系
├── 积分系统
├── 订单支付
├── 评价系统
```

## 数据库设计

本项目共包含 **12** 张数据库表。

### user

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT COMMENT '用户ID', |
| `phone` | VARCHAR(11) | NOT NULL UNIQUE COMMENT '手机号', |
| `password` | VARCHAR(255) | NOT NULL COMMENT '密码（BCrypt加密）', |
| `nickname` | VARCHAR(50) | DEFAULT NULL COMMENT '昵称', |
| `avatar` | VARCHAR(255) | DEFAULT 'https://via.placeholder.com/150' COMMENT  |
| `gender` | TINYINT | DEFAULT 0 COMMENT '性别（0女1男）', |
| `points` | INT | DEFAULT 0 COMMENT '积分', |
| `level` | VARCHAR(20) | DEFAULT '普通会员' COMMENT '会员等级', |
| ... | ... | 更多字段请查看源码 |

### store

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT COMMENT '门店ID', |
| `name` | VARCHAR(100) | NOT NULL COMMENT '门店名称', |
| `address` | VARCHAR(255) | NOT NULL COMMENT '详细地址', |
| `phone` | VARCHAR(20) | NOT NULL COMMENT '联系电话', |
| `open_time` | TIME | NOT NULL DEFAULT '09:00:00' COMMENT '营业开始时间', |
| `close_time` | TIME | NOT NULL DEFAULT '21:00:00' COMMENT '营业结束时间', |
| `intro` | TEXT | COMMENT '门店介绍', |
| `images` | TEXT | COMMENT '门店图片（JSON数组）', |
| ... | ... | 更多字段请查看源码 |

### hairdresser

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT COMMENT '理发师ID', |
| `store_id` | BIGINT | NOT NULL COMMENT '所属门店ID', |
| `name` | VARCHAR(50) | NOT NULL COMMENT '姓名', |
| `gender` | TINYINT | DEFAULT 1 COMMENT '性别（0女1男）', |
| `work_no` | VARCHAR(20) | NOT NULL COMMENT '工号', |
| `avatar` | VARCHAR(255) | DEFAULT 'https://via.placeholder.com/150' COMMENT  |
| `skills` | VARCHAR(500) | DEFAULT NULL COMMENT '技能标签（JSON数组）', |
| `work_years` | INT | DEFAULT 0 COMMENT '从业年限', |
| ... | ... | 更多字段请查看源码 |

### service

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT COMMENT '服务项目ID', |
| `store_id` | BIGINT | NOT NULL COMMENT '所属门店ID', |
| `category` | VARCHAR(20) | NOT NULL COMMENT '分类', |
| `name` | VARCHAR(100) | NOT NULL COMMENT '项目名称', |
| `price` | DECIMAL(10,2) | NOT NULL COMMENT '价格', |
| `duration` | INT | NOT NULL DEFAULT 30 COMMENT '时长（分钟）', |
| `description` | TEXT | COMMENT '描述', |
| `image` | VARCHAR(255) | DEFAULT NULL COMMENT '效果图', |
| ... | ... | 更多字段请查看源码 |

### appointment

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT COMMENT '预约ID', |
| `user_id` | BIGINT | NOT NULL COMMENT '用户ID', |
| `store_id` | BIGINT | NOT NULL COMMENT '门店ID', |
| `hairdresser_id` | BIGINT | NOT NULL COMMENT '理发师ID', |
| `service_id` | BIGINT | NOT NULL COMMENT '服务项目ID', |
| `appointment_date` | DATE | NOT NULL COMMENT '预约日期', |
| `appointment_time` | TIME | NOT NULL COMMENT '预约时间', |
| `status` | VARCHAR(20) | DEFAULT '待确认' COMMENT '状态', |
| ... | ... | 更多字段请查看源码 |

### orders

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT COMMENT '订单ID', |
| `order_no` | VARCHAR(32) | NOT NULL UNIQUE COMMENT '订单号', |
| `appointment_id` | BIGINT | NOT NULL COMMENT '预约ID', |
| `user_id` | BIGINT | NOT NULL COMMENT '用户ID', |
| `store_id` | BIGINT | NOT NULL COMMENT '门店ID', |
| `hairdresser_id` | BIGINT | NOT NULL COMMENT '理发师ID', |
| `service_name` | VARCHAR(100) | NOT NULL COMMENT '服务名称', |
| `original_price` | DECIMAL(10,2) | NOT NULL COMMENT '原价', |
| ... | ... | 更多字段请查看源码 |

### review

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT COMMENT '评价ID', |
| `order_id` | BIGINT | NOT NULL COMMENT '订单ID', |
| `user_id` | BIGINT | NOT NULL COMMENT '用户ID', |
| `hairdresser_id` | BIGINT | NOT NULL COMMENT '理发师ID', |
| `store_id` | BIGINT | NOT NULL COMMENT '门店ID', |
| `rating` | TINYINT | NOT NULL COMMENT '评分（1-5）', |
| `content` | TEXT | COMMENT '评价内容', |
| `images` | TEXT | COMMENT '评价图片（JSON数组）', |
| ... | ... | 更多字段请查看源码 |

### schedule

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT COMMENT '排班ID', |
| `hairdresser_id` | BIGINT | NOT NULL COMMENT '理发师ID', |
| `work_date` | DATE | NOT NULL COMMENT '工作日期', |
| `start_time` | TIME | NOT NULL COMMENT '开始时间', |
| `end_time` | TIME | NOT NULL COMMENT '结束时间', |
| `status` | TINYINT | DEFAULT 1 COMMENT '状态（0休息1上班）', |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP, |
| `updated_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMEST |

### points_log

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT COMMENT '记录ID', |
| `user_id` | BIGINT | NOT NULL COMMENT '用户ID', |
| `points` | INT | NOT NULL COMMENT '积分变动（正数增加，负数减少）', |
| `reason` | VARCHAR(100) | NOT NULL COMMENT '变动原因', |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP, |

### balance_log

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT COMMENT '记录ID', |
| `user_id` | BIGINT | NOT NULL COMMENT '用户ID', |
| `amount` | DECIMAL(10,2) | NOT NULL COMMENT '金额变动', |
| `type` | VARCHAR(20) | NOT NULL COMMENT '类型（充值/消费）', |
| `reason` | VARCHAR(100) | NOT NULL COMMENT '变动原因', |
| `created_at` | DATETIME | DEFAULT CURRENT_TIMESTAMP, |

> 还有 2 张表未展示，完整表结构请查看 `027-backend/src/main/resources/sql/init.sql`

## 前端页面

本项目前端共包含 **9** 个页面组件。

| 目录 | 页面 | 路径 |
|------|------|------|
| appointment | CreateAppointment | `appointment/CreateAppointment` |
| appointment | MyAppointments | `appointment/MyAppointments` |
| hairdresser | HairdresserDetail | `hairdresser/HairdresserDetail` |
| hairdresser | HairdresserList | `hairdresser/HairdresserList` |
| 根目录 | Home | `Home` |
| 根目录 | Login | `Login` |
| 根目录 | Profile | `Profile` |
| store | StoreDetail | `store/StoreDetail` |
| store | StoreList | `store/StoreList` |

## 项目结构

```
027-backend/
├── src/main/java/com/xiaou/
│   ├── controller/     # 控制器
│   ├── entity/         # 实体类
│   ├── mapper/         # MyBatis映射
│   ├── service/        # 业务逻辑
│   ├── dto/            # 数据传输对象
│   ├── config/         # 配置类
│   └── common/         # 通用类
├── src/main/resources/
│   ├── application.yml # 配置文件
│   └── sql/            # 数据库脚本
└── pom.xml             # Maven配置

027-frontend/
├── src/views/         # 页面组件 (9 个)
├── src/api/           # API封装
├── src/router/        # 路由配置
├── src/utils/         # 工具类
├── package.json       # 依赖配置
└── vite.config.js     # Vite配置
```

## 快速启动

### 环境要求

| 工具 | 版本 |
|------|------|
| JDK | 17+ |
| Maven | 3.8+ |
| 数据库 | H2 内存模式（默认） |
| Node.js | 以 package.json 为准 |

### 后端启动

默认配置使用 H2 内存数据库，通常无需预先创建外部数据库。

```bash
cd 027-backend
mvn spring-boot:run
```

### 前端启动

```bash
cd 027-frontend
npm install
npm run dev
```

### 默认账号

- 未从项目资料中提取到演示账号，请以项目 README、检查报告或初始化脚本为准。

### 开发注意事项

1. 默认使用 H2 内存数据库，无需预先创建外部数据库
2. 后端默认端口为 `8027`，前端代理需配置对应端口
3. 默认账号密码请查看上方表格，首次登录后建议修改密码

## 检查报告

**检查日期：** 2026-04-14

**综合结论：** 已完成 JDK 17 兼容修复、默认 H2 自举、前后端构建与核心链路抽测，项目现在可以在无本地 MySQL 的情况下直接启动并完成关键业务验证。

> 完整检查报告请查看 `docs/checks/027-hair-salon-booking-system.md`

