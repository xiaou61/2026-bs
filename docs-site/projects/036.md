---
title: 036 - 小梦想全球公益捐赠平台 🔥最新
description: 基于SpringBoot的小梦想全球公益捐赠平台的设计与实现（Spring Data JPA + PostgreSQL + Vue3）
---

# 小梦想全球公益捐赠平台 🔥最新

> 基于SpringBoot的小梦想全球公益捐赠平台的设计与实现（Spring Data JPA + PostgreSQL + Vue3）

## 项目概览

| 属性 | 内容 |
|------|------|
| 项目编号 | 036 |
| 项目名称 | 小梦想全球公益捐赠平台 🔥最新 |
| 项目全称 | 基于SpringBoot的小梦想全球公益捐赠平台的设计与实现（Spring Data JPA + PostgreSQL + Vue3） |
| 项目类型 | Web应用 |
| 后端目录 | `036-backend` |
| 前端目录 | `036-frontend` |
| Spring Boot 版本 | 3.2.0 |
| 后端默认端口 | `8036` |
| 数据库名 | `dream_donation` |
| 数据库表 | 4 张 |
| 前端页面 | 7 个 |

## 项目截图

已收录 **4** 张运行截图。

### 访客

#### guest-01-projects

![guest-01-projects](/previews/assets/036/guest-01-projects.png)

#### guest-02-my

![guest-02-my](/previews/assets/036/guest-02-my.png)

#### guest-03-login

![guest-03-login](/previews/assets/036/guest-03-login.png)

#### guest-04-register

![guest-04-register](/previews/assets/036/guest-04-register.png)

## 技术栈

### 后端

- Spring Boot 3.2.0
- Spring Data JPA（区别于其他项目的MyBatis-Plus）
- PostgreSQL（区别于其他项目的MySQL）
- JWT 0.11.5
- Lombok
- Hutool工具库
- Fastjson2

### 前端

- Vue 3.3.4
- Element Plus 2.4.0
- Vite 4.4.9
- Pinia 2.1.6
- Axios 1.5.0
- Vue Router 4.2.4

## 角色权限

系统支持 **1** 种角色，各角色权限如下：

### 用户

- 用户系统

## 功能模块

| 模块 | 说明 |
|------|------|
| **用户系统** |  |
| **公益项目管理** |  |
| **捐赠功能** |  |
| **项目进度** |  |
| **数据统计** |  |
| **个人中心** |  |

### 模块架构

```
小梦想全球公益捐赠平台 🔥最新
├── 用户系统
├── 公益项目管理
├── 捐赠功能
├── 项目进度
├── 数据统计
├── 个人中心
```

## 数据库设计

本项目共包含 **4** 张数据库表。

### users

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, |
| `username` | VARCHAR(50) | UNIQUE NOT NULL, |
| `password` | VARCHAR(255) | NOT NULL, |
| `real_name` | VARCHAR(50) | , |
| `email` | VARCHAR(100) | UNIQUE, |
| `phone` | VARCHAR(20) | , |
| `avatar` | VARCHAR(500) | , |
| `role` | VARCHAR(20) | DEFAULT 'USER', |
| ... | ... | 更多字段请查看源码 |

### donation_projects

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, |
| `title` | VARCHAR(200) | NOT NULL, |
| `description` | TEXT | , |
| `cover_image` | VARCHAR(500) | , |
| `category` | VARCHAR(100) | , |
| `target_amount` | DECIMAL(15,2) | NOT NULL, |
| `current_amount` | DECIMAL(15,2) | DEFAULT 0 NOT NULL, |
| `start_date` | TIMESTAMP | NOT NULL, |
| ... | ... | 更多字段请查看源码 |

### donations

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, |
| `user_id` | BIGINT | , |
| `project_id` | BIGINT | NOT NULL, |
| `amount` | DECIMAL(15,2) | NOT NULL, |
| `message` | VARCHAR(500) | , |
| `anonymous` | BOOLEAN | DEFAULT FALSE NOT NULL, |
| `payment_method` | VARCHAR(20) | , |
| `transaction_id` | VARCHAR(100) | , |
| ... | ... | 更多字段请查看源码 |

### project_progress

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, |
| `project_id` | BIGINT | NOT NULL, |
| `title` | VARCHAR(200) | NOT NULL, |
| `content` | TEXT | , |
| `images` | VARCHAR(1000) | , |
| `create_time` | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP, |

## 前端页面

本项目前端共包含 **7** 个页面组件。

| 目录 | 页面 | 路径 |
|------|------|------|
| 根目录 | Home | `Home` |
| 根目录 | Layout | `Layout` |
| 根目录 | Login | `Login` |
| 根目录 | MyCenter | `MyCenter` |
| 根目录 | ProjectDetail | `ProjectDetail` |
| 根目录 | ... | 还有 2 个页面 |

## 项目结构

```
036-backend/
├── src/main/java/com/xiaou/
│   ├── controller/     # 控制器
│   ├── entity/         # 实体类
│   ├── mapper/         # MyBatis映射
│   ├── service/        # 业务逻辑
│   ├── dto/            # 数据传输对象
│   ├── config/         # 配置类
│   └── common/         # 通用类
├── src/main/resources/
│   ├── application.yml # 配置文件
│   └── sql/            # 数据库脚本
└── pom.xml             # Maven配置

036-frontend/
├── src/views/         # 页面组件 (7 个)
├── src/api/           # API封装
├── src/router/        # 路由配置
├── src/utils/         # 工具类
├── package.json       # 依赖配置
└── vite.config.js     # Vite配置
```

## 快速启动

### 环境要求

| 工具 | 版本 |
|------|------|
| JDK | 17+ |
| Maven | 3.8+ |
| 数据库 | H2 内存模式（默认） |
| Node.js | 以 package.json 为准 |

### 后端启动

默认配置使用 H2 内存数据库，通常无需预先创建外部数据库。 如需切换外部数据库，请先查看 `036-backend/src/main/resources/schema.sql`。

```bash
cd 036-backend
mvn spring-boot:run
```

### 前端启动

```bash
cd 036-frontend
npm install
npm run dev
```

### 默认账号

| 角色 | 用户名 | 密码 | 说明 |
|------|--------|------|------|
| 用户 | `user1` | `123456` |  |

### 开发注意事项

1. 默认使用 H2 内存数据库，无需预先创建外部数据库
2. 如需切换外部数据库，请查看 `036-backend/src/main/resources/schema.sql`
3. 后端默认端口为 `8036`，前端代理需配置对应端口
4. 默认账号密码请查看上方表格，首次登录后建议修改密码

## 检查报告

**检查日期：** 2026-04-26

**综合结论：** 本轮已修复源码 HTML 实体转义导致的前后端无法正常编译、默认环境强依赖 PostgreSQL、默认密码哈希不可用、JWT 未真正参与当前用户识别、接口通过 userId 请求头默认冒充、登录/用户信息泄露密码、普通用户可越权创建项目、捐赠金额缺少下限校验、JPA 懒加载代理序列化失败、前端日期格式提交不兼容以及外部占位图/缺失 favicon 导致的浏览器资源错误。当前项目可在默认 H2 环境下通过后端测试、后端真实启动、核心接口抽测、前端构建与浏览器联调。

> 完整检查报告请查看 `docs/checks/036-dream-donation-platform.md`

