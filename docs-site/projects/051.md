---
title: 051 - 微信小程序网络安全知识科普APP 🔥最新
description: 基于微信小程序的网络安全知识科普APP
---

# 微信小程序网络安全知识科普APP 🔥最新

> 基于微信小程序的网络安全知识科普APP

## 项目概览

| 属性 | 内容 |
|------|------|
| 项目编号 | 051 |
| 项目名称 | 微信小程序网络安全知识科普APP 🔥最新 |
| 项目全称 | 基于微信小程序的网络安全知识科普APP |
| 项目类型 | 微信小程序 |
| Spring Boot 版本 | 2.7.18 |
| 后端默认端口 | `8051` |
| 数据库名 | `Shanghai` |
| 后端 Controller | 8 个 |
| 后端 API 接口 | 50 个 |
| 数据库表 | 33 张 |
| 实体类 | 11 个 |

## 项目简介

本项目是一个面向普通用户的网络安全知识科普系统，包含知识分类、文章学习、每日答题、错题本、排行榜、安全资讯、互动问答和后台管理能力。后端采用 Spring Boot 2.7、MyBatis-Plus、JWT，前端包含 Vue 3 管理端和原生微信小程序。

本轮巡检后，项目默认使用 H2 内存数据库自举，启动后会自动建表并写入演示数据，无需提前安装 MySQL 或 Redis。需要连接 MySQL 时，可切换到 `mysql` profile 并导入 `sql/init.sql`。

## 技术栈

### 后端

- Spring Boot 2.7.x
- MyBatis-Plus
- MySQL 8.0
- Redis
- JWT认证

### 前端

- 微信小程序原生开发
- WXML + WXSS + JavaScript
- Vue 3
- Element Plus

## 角色权限

系统支持 **1** 种角色，各角色权限如下：

### 管理员

- 用户系统管理
- 知识学习模块管理
- 答题测验模块管理
- 安全资讯模块管理
- 互动社区模块管理
- 后台管理模块管理

## 功能模块

| 模块 | 说明 |
|------|------|
| **用户系统** | 微信授权登录、用户信息管理、积分等级系统 |
| **知识学习模块** | 知识分类浏览（密码安全/防诈骗/隐私保护/病毒防护）、图文文章详情、学习进度记录、收藏功能 |
| **答题测验模块** | 每日答题挑战、分类专项测验、错题本功能、答题排行榜 |
| **安全资讯模块** | 最新安全新闻、诈骗案例警示、安全预警推送 |
| **互动社区模块** | 安全问答（用户提问/回答）、经验分享、评论点赞 |
| **后台管理模块** | 用户管理、内容管理（知识文章、题库）、资讯管理、数据统计 |

### 模块架构

```
微信小程序网络安全知识科普APP 🔥最新
├── 用户系统
├── 知识学习模块
├── 答题测验模块
├── 安全资讯模块
├── 互动社区模块
├── 后台管理模块
```

## 演示流程

1. 用户微信授权登录 → 进入首页
2. 浏览知识分类 → 选择感兴趣的安全知识
3. 阅读知识文章 → 图文学习 → 收藏喜欢的内容
4. 进入答题模块 → 每日答题挑战 → 获得积分
5. 专项测验 → 选择分类 → 测试学习成果
6. 查看错题本 → 复习答错的题目
7. 浏览安全资讯 → 了解最新诈骗手段和防护方法
8. 社区问答 → 提问或回答其他用户的问题
9. 查看个人中心 → 积分等级、学习记录、收藏列表
10. 管理员后台 → 内容管理、用户管理、数据统计

## 数据库设计

本项目共包含 **11** 张数据库表。

### user

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `openid` | VARCHAR(64) | NOT NULL UNIQUE, |
| `nickname` | VARCHAR(50) | , |
| `avatar` | VARCHAR(255) | , |
| `points` | INT | DEFAULT 0, |
| `level` | INT | DEFAULT 1, |
| `create_time` | DATETIME | DEFAULT CURRENT_TIMESTAMP, |
| `update_time` | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMEST |

### knowledge_category

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `name` | VARCHAR(50) | NOT NULL, |
| `icon` | VARCHAR(255) | , |
| `sort` | INT | DEFAULT 0, |
| `status` | TINYINT | DEFAULT 1 |

### knowledge_article

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `category_id` | BIGINT | NOT NULL, |
| `title` | VARCHAR(100) | NOT NULL, |
| `cover` | VARCHAR(255) | , |
| `content` | TEXT | , |
| `view_count` | INT | DEFAULT 0, |
| `like_count` | INT | DEFAULT 0, |
| `status` | TINYINT | DEFAULT 1, |
| ... | ... | 更多字段请查看源码 |

### question

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `category_id` | BIGINT | NOT NULL, |
| `content` | VARCHAR(500) | NOT NULL, |
| `options` | JSON | , |
| `answer` | VARCHAR(10) | NOT NULL, |
| `explanation` | VARCHAR(500) | , |
| `difficulty` | TINYINT | DEFAULT 1, |

### answer_record

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `user_id` | BIGINT | NOT NULL, |
| `question_id` | BIGINT | NOT NULL, |
| `user_answer` | VARCHAR(10) | , |
| `is_correct` | TINYINT | , |
| `create_time` | DATETIME | DEFAULT CURRENT_TIMESTAMP, |

### learn_record

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `user_id` | BIGINT | NOT NULL, |
| `article_id` | BIGINT | NOT NULL, |
| `progress` | INT | DEFAULT 0, |
| `create_time` | DATETIME | DEFAULT CURRENT_TIMESTAMP, |
| `update_time` | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMEST |

### favorite

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `user_id` | BIGINT | NOT NULL, |
| `article_id` | BIGINT | NOT NULL, |
| `create_time` | DATETIME | DEFAULT CURRENT_TIMESTAMP, |

### news

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `title` | VARCHAR(100) | NOT NULL, |
| `cover` | VARCHAR(255) | , |
| `content` | TEXT | , |
| `source` | VARCHAR(50) | , |
| `view_count` | INT | DEFAULT 0, |
| `status` | TINYINT | DEFAULT 1, |
| `publish_time` | DATETIME | DEFAULT CURRENT_TIMESTAMP |

### qa_post

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `user_id` | BIGINT | NOT NULL, |
| `title` | VARCHAR(100) | NOT NULL, |
| `content` | TEXT | , |
| `view_count` | INT | DEFAULT 0, |
| `reply_count` | INT | DEFAULT 0, |
| `status` | TINYINT | DEFAULT 1, |
| `create_time` | DATETIME | DEFAULT CURRENT_TIMESTAMP, |

### qa_reply

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `post_id` | BIGINT | NOT NULL, |
| `user_id` | BIGINT | NOT NULL, |
| `content` | TEXT | , |
| `like_count` | INT | DEFAULT 0, |
| `create_time` | DATETIME | DEFAULT CURRENT_TIMESTAMP, |

> 还有 1 张表未展示，完整表结构请查看 `051-backend/src/main/resources/sql/init.sql`

## API 接口

本项目共包含 **8** 个 Controller，**50** 个 API 接口。

### Admin

| 方法 | 路径 |
|------|------|
| POST | `/api/admin/login` |
| GET | `/api/admin/stats` |
| GET | `/api/admin/user/list` |
| PUT | `/api/admin/user/status/{id}` |
| GET | `/api/admin/category/list` |
| ... | 还有 13 个接口 |

### Article

| 方法 | 路径 |
|------|------|
| GET | `/api/article/list` |
| GET | `/api/article/{id}` |
| POST | `/api/article/like/{id}` |
| POST | `/api/article/learn` |

### Category

| 方法 | 路径 |
|------|------|
| GET | `/api/category/list` |

### Favorite

| 方法 | 路径 |
|------|------|
| POST | `/api/favorite/add` |
| DELETE | `/api/favorite/{articleId}` |
| GET | `/api/favorite/list` |

### News

| 方法 | 路径 |
|------|------|
| GET | `/api/news/list` |
| GET | `/api/news/{id}` |

### Qa

| 方法 | 路径 |
|------|------|
| GET | `/api/qa/list` |
| GET | `/api/qa/{id}` |
| POST | `/api/qa/post` |
| POST | `/api/qa/reply` |
| POST | `/api/qa/like/{replyId}` |

### Question

| 方法 | 路径 |
|------|------|
| GET | `/api/question/daily` |
| GET | `/api/question/category/{categoryId}` |
| POST | `/api/answer/submit` |
| GET | `/api/answer/wrong-list` |
| GET | `/api/rank/list` |

### User

| 方法 | 路径 |
|------|------|
| POST | `/api/user/login` |
| GET | `/api/user/info` |
| PUT | `/api/user/info` |
| GET | `/api/user/stats` |

## 项目结构

```
051-backend/
├── src/main/java/com/xiaou/
│   ├── controller/     # 控制器 (8 个)
│   ├── entity/         # 实体类 (11 个)
│   ├── mapper/         # MyBatis映射
│   ├── service/        # 业务逻辑
│   ├── dto/            # 数据传输对象
│   ├── config/         # 配置类
│   └── common/         # 通用类
├── src/main/resources/
│   ├── application.yml # 配置文件
│   └── sql/            # 数据库脚本
└── pom.xml             # Maven配置

051-miniprogram/
├── pages/             # 页面
├── utils/             # 工具类
├── app.js             # 应用入口
├── app.json           # 应用配置
└── app.wxss           # 全局样式
```

## 快速启动

### 环境要求

| 工具 | 版本 |
|------|------|
| JDK | 8+ / 11+ |
| Maven | 3.8+ |
| MySQL | 8.0+ |

### 后端启动

```bash
cd 051-backend
# 1. 创建数据库并执行 init.sql
# 2. 修改 application.yml 数据库配置
# spring.datasource.url=jdbc:mysql://localhost:3306/Shanghai
# spring.datasource.username=root
# spring.datasource.password=你的密码
mvn spring-boot:run
```

### 小程序启动

1. 使用微信开发者工具打开 `051-miniprogram` 目录
2. 确认 AppID 配置正确
3. 修改后端接口地址（通常在 config.js 或 utils/request.js 中）
4. 点击编译运行

### 默认账号

| 角色 | 用户名 | 密码 | 说明 |
|------|--------|------|------|
| 管理员 | `admin` | `123456` |  |

### 开发注意事项

1. ```bash
2. cd 051-backend
3. mvn spring-boot:run
4. ```
5. 启动后可访问：
6. ```text

## 检查报告

**检查日期：** 2026-04-27

**综合结论：** 本轮已修复默认环境强依赖 MySQL/Redis/微信接口、后端端口未项目化、JJWT 在 JDK 11+ 运行缺 JAXB、MyBatis-Plus 分页方言固定 MySQL、公开接口鉴权规则误配置、普通用户 token 可访问管理端接口、小程序学习记录和收藏请求体与后端参数不匹配、管理端/小程序仍指向 8080、小程序 pages/qa 声明缺页、本地 static 图片资源断链以及后端缺少可执行冒烟测试等问题。当前后端可在默认 H2 环境下启动并通过自动化冒烟测试，管理端可构建，小程序页面声明和资源引用静态核查通过。

> 完整检查报告请查看 `docs/checks/051-security-knowledge-miniprogram.md`

