---
title: 083 - 基于B/S的老年人体检管理系统 🔥最新
description: 基于SpringBoot+Vue3的老年人体检管理系统（Spring Boot 2.7.14 + MyBatis-Plus 3.5.3 + Redis + Vue3 + Element Plus + ECharts + 四角色：管理员/医生/护士/前台 + 老人档案+体检项目+套餐管理+预约登记+结果录入+异常预警+随访管理+通知看板）
---

# 基于B/S的老年人体检管理系统 🔥最新

> 基于SpringBoot+Vue3的老年人体检管理系统（Spring Boot 2.7.14 + MyBatis-Plus 3.5.3 + Redis + Vue3 + Element Plus + ECharts + 四角色：管理员/医生/护士/前台 + 老人档案+体检项目+套餐管理+预约登记+结果录入+异常预警+随访管理+通知看板）

## 项目概览

| 属性 | 内容 |
|------|------|
| 项目编号 | 083 |
| 项目名称 | 基于B/S的老年人体检管理系统 🔥最新 |
| 项目全称 | 基于SpringBoot+Vue3的老年人体检管理系统（Spring Boot 2.7.14 + MyBatis-Plus 3.5.3 + Redis + Vue3 + Element Plus + ECharts + 四角色：管理员/医生/护士/前台 + 老人档案+体检项目+套餐管理+预约登记+结果录入+异常预警+随访管理+通知看板） |
| 项目类型 | Web应用 |
| 后端目录 | `083-backend` |
| 前端目录 | `083-frontend` |
| Spring Boot 版本 | 2.7.14 |
| 后端默认端口 | `8083` |
| 数据库名 | `Shanghai` |
| 后端 Controller | 10 个 |
| 后端 API 接口 | 51 个 |
| 数据库表 | 12 张 |
| 实体类 | 12 个 |
| 前端页面 | 11 个 |

## 项目简介

本项目面向体检中心、社区卫生服务机构和养老机构，提供老人档案、体检项目与套餐、预约登记、结果录入、异常预警、随访管理、公告和统计看板能力。

## 项目截图

已收录 **11** 张运行截图。

### 管理员

#### admin-01-dashboard

![admin-01-dashboard](/previews/assets/083/admin-01-dashboard.png)

#### admin-02-elder

![admin-02-elder](/previews/assets/083/admin-02-elder.png)

#### admin-03-item

![admin-03-item](/previews/assets/083/admin-03-item.png)

#### admin-04-package

![admin-04-package](/previews/assets/083/admin-04-package.png)

#### admin-05-appointment

![admin-05-appointment](/previews/assets/083/admin-05-appointment.png)

#### admin-06-result

![admin-06-result](/previews/assets/083/admin-06-result.png)

#### admin-07-warning

![admin-07-warning](/previews/assets/083/admin-07-warning.png)

#### admin-08-follow-up

![admin-08-follow-up](/previews/assets/083/admin-08-follow-up.png)

#### admin-09-notice

![admin-09-notice](/previews/assets/083/admin-09-notice.png)

### 访客

#### guest-01-login

![guest-01-login](/previews/assets/083/guest-01-login.png)

#### guest-02-register

![guest-02-register](/previews/assets/083/guest-02-register.png)

## 技术栈

### 后端

- Spring Boot 2.7.14
- MyBatis-Plus 3.5.3
- MySQL 8.0
- Redis
- JWT认证

### 前端

- Vue 3.4.0
- Element Plus 2.4.4
- Vite 5
- Pinia 2.1.7
- Axios 1.6.2
- ECharts 5.4.3

## 角色权限

系统支持 **4** 种角色，各角色权限如下：

### - 管理员

- admin
- 维护全量数据与系统配置

### - 医生

- doctor
- 负责体检结果录入与报告确认

### - 护士

- nurse
- 负责到检登记与基础体征录入

### - 前台

- reception
- 负责预约与档案维护

## 功能模块

| 模块 | 说明 |
|------|------|
| **用户权限** |  |
| **老人档案** |  |
| **体检项目** |  |
| **体检套餐** |  |
| **预约管理** |  |
| **结果录入** |  |
| **异常预警** |  |
| **随访管理** |  |
| **通知公告** |  |
| **数据看板** |  |

### 模块架构

```
基于B/S的老年人体检管理系统 🔥最新
├── 用户权限
├── 老人档案
├── 体检项目
├── 体检套餐
├── 预约管理
├── 结果录入
├── 异常预警
├── 随访管理
├── 通知公告
├── 数据看板
```

## 数据库设计

本项目共包含 **12** 张数据库表。

### sys_user

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `username` | VARCHAR(50) | NOT NULL UNIQUE, |
| `password` | VARCHAR(100) | NOT NULL, |
| `real_name` | VARCHAR(50) | , |
| `phone` | VARCHAR(20) | , |
| `role` | VARCHAR(20) | NOT NULL, |
| `status` | INT | DEFAULT 1, |
| `create_time` | DATETIME | DEFAULT CURRENT_TIMESTAMP, |
| ... | ... | 更多字段请查看源码 |

### elder_profile

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `name` | VARCHAR(50) | NOT NULL, |
| `gender` | VARCHAR(10) | , |
| `age` | INT | , |
| `id_card` | VARCHAR(30) | , |
| `phone` | VARCHAR(20) | , |
| `address` | VARCHAR(255) | , |
| `blood_type` | VARCHAR(10) | , |
| ... | ... | 更多字段请查看源码 |

### elder_medical_history

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `elder_id` | BIGINT | NOT NULL, |
| `allergy_history` | VARCHAR(500) | , |
| `past_history` | VARCHAR(1000) | , |
| `family_history` | VARCHAR(1000) | , |
| `medication_history` | VARCHAR(1000) | , |
| `create_time` | DATETIME | DEFAULT CURRENT_TIMESTAMP, |
| `update_time` | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMEST |

### elder_contact

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `elder_id` | BIGINT | NOT NULL, |
| `contact_name` | VARCHAR(50) | NOT NULL, |
| `relation` | VARCHAR(30) | , |
| `contact_phone` | VARCHAR(20) | , |
| `address` | VARCHAR(255) | , |
| `create_time` | DATETIME | DEFAULT CURRENT_TIMESTAMP, |
| `update_time` | DATETIME | DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMEST |

### check_item

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `item_code` | VARCHAR(50) | NOT NULL UNIQUE, |
| `item_name` | VARCHAR(100) | NOT NULL, |
| `unit` | VARCHAR(20) | , |
| `low_limit` | DECIMAL(10,2) | , |
| `high_limit` | DECIMAL(10,2) | , |
| `warning_level` | VARCHAR(20) | DEFAULT 'medium', |
| `status` | INT | DEFAULT 1, |
| ... | ... | 更多字段请查看源码 |

### check_package

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `package_name` | VARCHAR(100) | NOT NULL, |
| `package_code` | VARCHAR(50) | NOT NULL UNIQUE, |
| `price` | DECIMAL(10,2) | DEFAULT 0, |
| `description` | VARCHAR(1000) | , |
| `suitable_crowd` | VARCHAR(100) | , |
| `status` | INT | DEFAULT 1, |
| `create_time` | DATETIME | DEFAULT CURRENT_TIMESTAMP, |
| ... | ... | 更多字段请查看源码 |

### check_package_item

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `package_id` | BIGINT | NOT NULL, |
| `item_id` | BIGINT | NOT NULL, |
| `sort` | INT | DEFAULT 0 |

### check_appointment

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `appointment_no` | VARCHAR(50) | NOT NULL UNIQUE, |
| `elder_id` | BIGINT | NOT NULL, |
| `package_id` | BIGINT | NOT NULL, |
| `appointment_date` | DATE | NOT NULL, |
| `slot_time` | VARCHAR(30) | , |
| `source` | VARCHAR(30) | , |
| `status` | INT | DEFAULT 0, |
| ... | ... | 更多字段请查看源码 |

### check_result

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `appointment_id` | BIGINT | NOT NULL, |
| `elder_id` | BIGINT | NOT NULL, |
| `item_id` | BIGINT | NOT NULL, |
| `item_value` | VARCHAR(100) | , |
| `abnormal_flag` | INT | DEFAULT 0, |
| `conclusion` | VARCHAR(500) | , |
| `doctor_id` | BIGINT | , |
| ... | ... | 更多字段请查看源码 |

### abnormal_warning

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `result_id` | BIGINT | NOT NULL, |
| `elder_id` | BIGINT | NOT NULL, |
| `item_id` | BIGINT | NOT NULL, |
| `warning_level` | VARCHAR(20) | , |
| `warning_content` | VARCHAR(500) | , |
| `status` | INT | DEFAULT 0, |
| `create_time` | DATETIME | DEFAULT CURRENT_TIMESTAMP, |
| ... | ... | 更多字段请查看源码 |

> 还有 2 张表未展示，完整表结构请查看 `083-backend/src/main/resources/sql/init.sql`

## API 接口

本项目共包含 **10** 个 Controller，**51** 个 API 接口。

### Appointment

| 方法 | 路径 |
|------|------|
| GET | `/api/appointment/list` |
| POST | `/api/appointment/add` |
| PUT | `/api/appointment/update` |
| PUT | `/api/appointment/status` |
| DELETE | `/api/appointment/delete/{id}` |

### Auth

| 方法 | 路径 |
|------|------|
| POST | `/api/auth/login` |
| GET | `/api/auth/info` |
| POST | `/api/auth/logout` |

### CheckItem

| 方法 | 路径 |
|------|------|
| GET | `/api/item/list` |
| GET | `/api/item/all` |
| POST | `/api/item/add` |
| PUT | `/api/item/update` |
| DELETE | `/api/item/delete/{id}` |

### CheckPackage

| 方法 | 路径 |
|------|------|
| GET | `/api/package/list` |
| GET | `/api/package/all` |
| POST | `/api/package/add` |
| PUT | `/api/package/update` |
| DELETE | `/api/package/delete/{id}` |

### Elder

| 方法 | 路径 |
|------|------|
| GET | `/api/elder/list` |
| GET | `/api/elder/all` |
| POST | `/api/elder/add` |
| PUT | `/api/elder/update` |
| DELETE | `/api/elder/delete/{id}` |

### FollowUp

| 方法 | 路径 |
|------|------|
| GET | `/api/follow-up/list` |
| POST | `/api/follow-up/add` |
| PUT | `/api/follow-up/update` |
| DELETE | `/api/follow-up/delete/{id}` |

### Notice

| 方法 | 路径 |
|------|------|
| GET | `/api/notice/list` |
| GET | `/api/notice/public/list` |
| GET | `/api/notice/public/detail/{id}` |
| POST | `/api/notice/add` |
| PUT | `/api/notice/update` |
| ... | 还有 1 个接口 |

### Result

| 方法 | 路径 |
|------|------|
| GET | `/api/result/list` |
| POST | `/api/result/add` |
| PUT | `/api/result/update` |
| DELETE | `/api/result/delete/{id}` |

> 还有 2 个 Controller 未展示。

## 前端页面

本项目前端共包含 **11** 个页面组件。

| 目录 | 页面 | 路径 |
|------|------|------|
| appointment | index | `appointment/index` |
| 根目录 | Dashboard | `Dashboard` |
| 根目录 | Layout | `Layout` |
| 根目录 | Login | `Login` |
| elder | index | `elder/index` |
| follow-up | index | `follow-up/index` |
| item | index | `item/index` |
| notice | index | `notice/index` |
| package | index | `package/index` |
| result | index | `result/index` |
| warning | index | `warning/index` |

## 项目结构

```
083-backend/
├── src/main/java/com/xiaou/
│   ├── controller/     # 控制器 (10 个)
│   ├── entity/         # 实体类 (12 个)
│   ├── mapper/         # MyBatis映射
│   ├── service/        # 业务逻辑
│   ├── dto/            # 数据传输对象
│   ├── config/         # 配置类
│   └── common/         # 通用类
├── src/main/resources/
│   ├── application.yml # 配置文件
│   └── sql/            # 数据库脚本
└── pom.xml             # Maven配置

083-frontend/
├── src/views/         # 页面组件 (11 个)
├── src/api/           # API封装
├── src/router/        # 路由配置
├── src/utils/         # 工具类
├── package.json       # 依赖配置
└── vite.config.js     # Vite配置
```

## 快速启动

### 环境要求

| 工具 | 版本 |
|------|------|
| JDK | 17+ |
| Maven | 3.8+ |
| 数据库 | H2 内存模式（默认） |
| Node.js | 以 package.json 为准 |

### 后端启动

默认配置使用 H2 内存数据库，通常无需预先创建外部数据库。 如需切换外部数据库，请先查看 `083-backend/sql/init.sql`。

```bash
cd 083-backend
mvn spring-boot:run
```

### 前端启动

```bash
cd 083-frontend
npm install
npm run dev
```

### 默认账号

| 角色 | 用户名 | 密码 | 说明 |
|------|--------|------|------|
| 管理员 | ``admin`` | ``123456`` |  |
| 医生 | ``doctor`` | ``123456`` |  |
| 护士 | ``nurse`` | ``123456`` |  |
| 前台 | ``reception`` | ``123456`` |  |

### 开发注意事项

1. 默认使用 H2 内存数据库，无需预先创建外部数据库
2. 如需切换外部数据库，请查看 `083-backend/sql/init.sql`
3. 后端默认端口为 `8083`，前端代理需配置对应端口
4. 默认账号密码请查看上方表格，首次登录后建议修改密码

## 检查报告

**检查日期：** 2026-05-02

**综合结论：** 本轮已修复默认环境强依赖 MySQL root/root、Redis 和 8080、H2 自举缺失、MySQL 驱动旧坐标、JDK 17/JJWT 兼容、后端有效自动化测试缺失、登录响应 password 字段泄露、JWT Bearer 不兼容、未登录/越权 HTTP 状态不准确、登出后 token 不失效、管理员/医生/护士/前台权限边界不足、体检结果 elderId/doctorId 可伪造、预约 createBy 可伪造、前端代理端口写死和请求头不规范等问题。当前后端默认 H2 环境可通过冒烟测试，MySQL root/1234 的 mysql-verify 临时库真实 HTTP 验证通过；前端可完成生产构建和 3083 代理登录联调。

> 完整检查报告请查看 `docs/checks/083-eldercare-checkup-system.md`

