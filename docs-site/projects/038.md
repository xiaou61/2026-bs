---
title: 038 - 个人饮食管理系统 🔥最新
description: 基于SpringBoot的个人饮食管理系统的设计与实现（Spring Boot 3.2 + MyBatis-Plus 3.5.5 + Vue3 + Element Plus + ECharts）
---

# 个人饮食管理系统 🔥最新

> 基于SpringBoot的个人饮食管理系统的设计与实现（Spring Boot 3.2 + MyBatis-Plus 3.5.5 + Vue3 + Element Plus + ECharts）

## 项目概览

| 属性 | 内容 |
|------|------|
| 项目编号 | 038 |
| 项目名称 | 个人饮食管理系统 🔥最新 |
| 项目全称 | 基于SpringBoot的个人饮食管理系统的设计与实现（Spring Boot 3.2 + MyBatis-Plus 3.5.5 + Vue3 + Element Plus + ECharts） |
| 项目类型 | Web应用 |
| 后端目录 | `038-backend` |
| 前端目录 | `038-frontend` |
| Spring Boot 版本 | 3.2.0 |
| 后端默认端口 | `8038` |
| 数据库名 | `Shanghai` |
| 数据库表 | 7 张 |
| 前端页面 | 9 个 |

## 项目简介

基于SpringBoot的个人饮食管理系统后端服务

## 项目截图

已收录 **9** 张运行截图。

### 访客

#### guest-01-dashboard

![guest-01-dashboard](/previews/assets/038/guest-01-dashboard.png)

#### guest-02-diet-record

![guest-02-diet-record](/previews/assets/038/guest-02-diet-record.png)

#### guest-02-register

![guest-02-register](/previews/assets/038/guest-02-register.png)

#### guest-03-food-library

![guest-03-food-library](/previews/assets/038/guest-03-food-library.png)

#### guest-04-nutrition

![guest-04-nutrition](/previews/assets/038/guest-04-nutrition.png)

#### guest-05-health

![guest-05-health](/previews/assets/038/guest-05-health.png)

#### guest-06-goal

![guest-06-goal](/previews/assets/038/guest-06-goal.png)

#### guest-07-recipe

![guest-07-recipe](/previews/assets/038/guest-07-recipe.png)

#### guest-08-login

![guest-08-login](/previews/assets/038/guest-08-login.png)

## 技术栈

### 后端

- Spring Boot 3.2.0
- MyBatis-Plus 3.5.5
- MySQL 8.0
- Redis 6.0+
- Spring Security + JWT 0.11.5
- Knife4j 4.3.0（API文档）
- Druid 1.2.20（数据库连接池）
- Hutool 5.8.23、Lombok 1.18.30

### 前端

- Vue 3 + Vite
- Element Plus
- ECharts（数据可视化）
- Pinia（状态管理）
- Vue Router 4
- Axios

## 角色权限

系统支持 **3** 种角色，各角色权限如下：

### 管理员

- 用户系统管理
- 饮食记录管理
- 食物库管理管理
- 营养分析管理
- 健康数据管理管理
- 营养目标设定管理
- 食谱推荐管理
- 数据统计与报告管理

### 用户1

- 饮食记录
- 营养分析
- 营养目标设定

### 用户2

- 饮食记录
- 营养分析
- 营养目标设定

## 功能模块

| 模块 | 说明 |
|------|------|
| **用户系统** |  |
| **饮食记录** |  |
| **食物库管理** |  |
| **营养分析** |  |
| **健康数据管理** |  |
| **营养目标设定** |  |
| **食谱推荐** |  |
| **数据统计与报告** |  |

### 模块架构

```
个人饮食管理系统 🔥最新
├── 用户系统
├── 饮食记录
├── 食物库管理
├── 营养分析
├── 健康数据管理
├── 营养目标设定
├── 食谱推荐
├── 数据统计与报告
```

## 数据库设计

本项目共包含 **7** 张数据库表。

### users

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | AUTO_INCREMENT PRIMARY KEY, |
| `username` | VARCHAR(50) | NOT NULL UNIQUE, |
| `password` | VARCHAR(255) | NOT NULL, |
| `nickname` | VARCHAR(50) | , |
| `avatar` | VARCHAR(255) | , |
| `gender` | TINYINT | , |
| `age` | INT | , |
| `height` | DECIMAL(5,2) | , |
| ... | ... | 更多字段请查看源码 |

### foods

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | AUTO_INCREMENT PRIMARY KEY, |
| `name` | VARCHAR(100) | NOT NULL, |
| `category` | VARCHAR(50) | NOT NULL, |
| `unit` | VARCHAR(20) | NOT NULL DEFAULT 'g', |
| `calorie` | DECIMAL(10,2) | NOT NULL, |
| `protein` | DECIMAL(10,2) | DEFAULT 0, |
| `fat` | DECIMAL(10,2) | DEFAULT 0, |
| `carbohydrate` | DECIMAL(10,2) | DEFAULT 0, |
| ... | ... | 更多字段请查看源码 |

### diet_records

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | AUTO_INCREMENT PRIMARY KEY, |
| `user_id` | BIGINT | NOT NULL, |
| `food_id` | BIGINT | NOT NULL, |
| `food_name` | VARCHAR(100) | NOT NULL, |
| `meal_type` | VARCHAR(20) | NOT NULL, |
| `weight` | DECIMAL(10,2) | NOT NULL, |
| `calorie` | DECIMAL(10,2) | NOT NULL, |
| `protein` | DECIMAL(10,2) | DEFAULT 0, |
| ... | ... | 更多字段请查看源码 |

### nutrition_goals

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | AUTO_INCREMENT PRIMARY KEY, |
| `user_id` | BIGINT | NOT NULL, |
| `goal_type` | VARCHAR(20) | NOT NULL, |
| `target_weight` | DECIMAL(5,2) | , |
| `target_calorie` | DECIMAL(10,2) | NOT NULL, |
| `target_protein` | DECIMAL(10,2) | , |
| `target_fat` | DECIMAL(10,2) | , |
| `target_carbohydrate` | DECIMAL(10,2) | , |
| ... | ... | 更多字段请查看源码 |

### health_data

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | AUTO_INCREMENT PRIMARY KEY, |
| `user_id` | BIGINT | NOT NULL, |
| `record_date` | DATE | NOT NULL, |
| `weight` | DECIMAL(5,2) | , |
| `bmi` | DECIMAL(5,2) | , |
| `body_fat_rate` | DECIMAL(5,2) | , |
| `muscle_mass` | DECIMAL(5,2) | , |
| `basal_metabolism` | DECIMAL(10,2) | , |
| ... | ... | 更多字段请查看源码 |

### recipes

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | AUTO_INCREMENT PRIMARY KEY, |
| `name` | VARCHAR(100) | NOT NULL, |
| `category` | VARCHAR(50) | NOT NULL, |
| `difficulty` | VARCHAR(20) | DEFAULT 'EASY', |
| `cooking_time` | INT | , |
| `servings` | INT | DEFAULT 1, |
| `total_calorie` | DECIMAL(10,2) | DEFAULT 0, |
| `image` | VARCHAR(255) | , |
| ... | ... | 更多字段请查看源码 |

### recipe_favorites

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | AUTO_INCREMENT PRIMARY KEY, |
| `user_id` | BIGINT | NOT NULL, |
| `recipe_id` | BIGINT | NOT NULL, |
| `create_time` | TIMESTAMP | NOT NULL DEFAULT CURRENT_TIMESTAMP, |

## 前端页面

本项目前端共包含 **9** 个页面组件。

| 目录 | 页面 | 路径 |
|------|------|------|
| 根目录 | Dashboard | `Dashboard` |
| 根目录 | DietRecord | `DietRecord` |
| 根目录 | FoodLibrary | `FoodLibrary` |
| 根目录 | Goal | `Goal` |
| 根目录 | Health | `Health` |
| 根目录 | ... | 还有 4 个页面 |

## 项目结构

```
src/main/java/com/diet/management/
├── common/           # 通用类
├── config/           # 配置类
├── controller/       # 控制器
├── entity/           # 实体类
├── enums/            # 枚举类
├── mapper/           # Mapper接口
├── service/          # 服务层
└── util/             # 工具类
```

## 快速启动

### 环境要求

| 工具 | 版本 |
|------|------|
| JDK | 17+ |
| Maven | 3.8+ |
| 数据库 | H2 内存模式（默认） |
| Node.js | 以 package.json 为准 |

### 后端启动

默认配置使用 H2 内存数据库，通常无需预先创建外部数据库。

```bash
cd 038-backend
mvn spring-boot:run
```

### 前端启动

```bash
cd 038-frontend
npm install
npm run dev
```

### 默认账号

| 角色 | 用户名 | 密码 | 说明 |
|------|--------|------|------|
| 管理员 | `admin` | `123456` |  |
| 用户1 | `user1` | `123456` |  |
| 用户2 | `user2` | `123456` |  |

### 开发注意事项

1. 默认使用 H2 内存数据库，无需预先创建外部数据库
2. JDK 17
3. Maven 3.6+
4. 默认启动无需提前安装 MySQL / Redis
5. 如需生产式部署，可准备 MySQL 8.0 与 Redis 6.0+
6. ```bash
7. mvn spring-boot:run

## 检查报告

**检查日期：** 2026-04-26

**综合结论：** 本轮已修复 Spring Boot 3.2 与旧 MyBatis-Plus starter 不兼容导致默认启动失败、默认环境强依赖 MySQL/Redis、Spring Security 默认登录页阻断接口、登录服务未校验密码、初始化 SQL 中 admin 密码哈希断行损坏、核心业务接口过少导致演示链路不足等问题。当前后端可在默认 H2 环境下通过测试并真实启动，管理员登录、食物库、饮食记录、营养汇总、健康数据、营养目标、食谱详情接口抽测通过；前端可构建并通过 Vite 代理完成登录接口联调。

> 完整检查报告请查看 `docs/checks/038-personal-diet-management-system.md`

