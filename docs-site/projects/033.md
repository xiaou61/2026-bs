---
title: 033 - 婚纱写真馆管理系统
description: 基于 SpringBoot 的婚纱写真馆管理系统
---

# 婚纱写真馆管理系统

> 基于 SpringBoot 的婚纱写真馆管理系统

## 项目概览

| 属性 | 内容 |
|------|------|
| 项目编号 | 033 |
| 项目名称 | 婚纱写真馆管理系统 |
| 项目全称 | 基于 SpringBoot 的婚纱写真馆管理系统 |
| 项目类型 | Web应用 |
| Spring Boot 版本 | 3.2.0 |
| 后端默认端口 | `6379` |
| 数据库名 | `Shanghai` |
| 数据库表 | 54 张 |

## 项目截图

已收录 **2** 张运行截图。

### 访客

#### guest-01-login

![guest-01-login](/previews/assets/033/guest-01-login.png)

#### guest-02-register

![guest-02-register](/previews/assets/033/guest-02-register.png)

## 技术栈

### 后端

- Spring Boot 3.2
- MyBatis
- MySQL 8.0
- Redis
- JWT

### 前端

- React 18 + Vite
- Ant Design
- Axios

## 角色权限

系统支持 **1** 种角色，各角色权限如下：

### 管理员

- 基本操作权限

## 数据库设计

本项目共包含 **18** 张数据库表。

### user

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `username` | VARCHAR(50) | NOT NULL UNIQUE COMMENT '用户名', |
| `password` | VARCHAR(200) | NOT NULL COMMENT '密码', |
| `real_name` | VARCHAR(50) | COMMENT '真实姓名', |
| `phone` | VARCHAR(20) | COMMENT '手机号', |
| `email` | VARCHAR(100) | COMMENT '邮箱', |
| `avatar` | VARCHAR(500) | COMMENT '头像URL', |
| `role` | VARCHAR(20) | NOT NULL DEFAULT 'CUSTOMER' COMMENT '角色', |
| ... | ... | 更多字段请查看源码 |

### customer

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `user_id` | BIGINT | COMMENT '关联用户ID', |
| `name` | VARCHAR(50) | NOT NULL COMMENT '客户姓名', |
| `gender` | TINYINT | COMMENT '性别', |
| `age` | INT | COMMENT '年龄', |
| `phone` | VARCHAR(20) | NOT NULL COMMENT '联系电话', |
| `email` | VARCHAR(100) | COMMENT '邮箱', |
| `address` | VARCHAR(200) | COMMENT '地址', |
| ... | ... | 更多字段请查看源码 |

### customer_follow_up

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `customer_id` | BIGINT | NOT NULL COMMENT '客户ID', |
| `follow_user_id` | BIGINT | NOT NULL COMMENT '跟进人ID', |
| `follow_type` | VARCHAR(20) | COMMENT '跟进方式', |
| `content` | TEXT | COMMENT '跟进内容', |
| `next_follow_time` | DATETIME | COMMENT '下次跟进时间', |
| `created_at` | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP, |

### package

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `name` | VARCHAR(100) | NOT NULL COMMENT '套餐名称', |
| `category` | VARCHAR(50) | NOT NULL COMMENT '套餐分类', |
| `price` | DECIMAL(10,2) | NOT NULL COMMENT '价格', |
| `description` | TEXT | COMMENT '套餐描述', |
| `content` | TEXT | COMMENT '套餐内容', |
| `costume_count` | INT | COMMENT '服装数量', |
| `refined_photo_count` | INT | COMMENT '精修照片数量', |
| ... | ... | 更多字段请查看源码 |

### photographer

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `user_id` | BIGINT | COMMENT '关联用户ID', |
| `name` | VARCHAR(50) | NOT NULL COMMENT '姓名', |
| `phone` | VARCHAR(20) | COMMENT '手机号', |
| `photo` | VARCHAR(500) | COMMENT '照片', |
| `level` | VARCHAR(20) | COMMENT '等级', |
| `specialty` | TEXT | COMMENT '擅长风格', |
| `portfolio` | TEXT | COMMENT '作品集', |
| ... | ... | 更多字段请查看源码 |

### photographer_schedule

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `photographer_id` | BIGINT | NOT NULL COMMENT '摄影师ID', |
| `work_date` | DATE | NOT NULL COMMENT '工作日期', |
| `time_slot` | VARCHAR(20) | NOT NULL COMMENT '时段', |
| `status` | VARCHAR(20) | NOT NULL DEFAULT 'AVAILABLE' COMMENT '状态', |
| `created_at` | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP, |
| `updated_at` | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRE |

### studio

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `name` | VARCHAR(100) | NOT NULL COMMENT '影棚名称', |
| `area` | DECIMAL(10,2) | COMMENT '面积', |
| `style` | VARCHAR(50) | COMMENT '风格', |
| `equipment` | TEXT | COMMENT '设备', |
| `description` | TEXT | COMMENT '描述', |
| `images` | TEXT | COMMENT '图片', |
| `status` | TINYINT | NOT NULL DEFAULT 1 COMMENT '状态', |
| ... | ... | 更多字段请查看源码 |

### studio_booking

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `studio_id` | BIGINT | NOT NULL COMMENT '影棚ID', |
| `booking_date` | DATE | NOT NULL COMMENT '预约日期', |
| `time_slot` | VARCHAR(20) | NOT NULL COMMENT '时段', |
| `status` | VARCHAR(20) | NOT NULL DEFAULT 'AVAILABLE' COMMENT '状态', |
| `order_id` | BIGINT | COMMENT '订单ID', |
| `created_at` | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP, |
| `updated_at` | DATETIME | NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRE |

### costume

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `code` | VARCHAR(50) | NOT NULL UNIQUE COMMENT '服装编号', |
| `name` | VARCHAR(100) | NOT NULL COMMENT '服装名称', |
| `category` | VARCHAR(50) | COMMENT '类别', |
| `size` | VARCHAR(20) | COMMENT '尺码', |
| `color` | VARCHAR(50) | COMMENT '颜色', |
| `images` | TEXT | COMMENT '图片', |
| `status` | VARCHAR(20) | NOT NULL DEFAULT 'AVAILABLE' COMMENT '状态', |
| ... | ... | 更多字段请查看源码 |

### costume_borrow

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `costume_id` | BIGINT | NOT NULL COMMENT '服装ID', |
| `order_id` | BIGINT | NOT NULL COMMENT '订单ID', |
| `customer_id` | BIGINT | NOT NULL COMMENT '客户ID', |
| `borrow_date` | DATETIME | NOT NULL COMMENT '借出日期', |
| `return_date` | DATETIME | COMMENT '归还日期', |
| `status` | VARCHAR(20) | NOT NULL DEFAULT 'BORROWED' COMMENT '状态', |
| `remark` | TEXT | COMMENT '备注', |
| ... | ... | 更多字段请查看源码 |

> 还有 8 张表未展示，完整表结构请查看 `033-backend/src/main/resources/sql/init.sql`

## 项目结构

```
033-backend/
├── src/main/java/com/xiaou/
│   ├── controller/     # 控制器
│   ├── entity/         # 实体类
│   ├── mapper/         # MyBatis映射
│   ├── service/        # 业务逻辑
│   ├── dto/            # 数据传输对象
│   ├── config/         # 配置类
│   └── common/         # 通用类
├── src/main/resources/
│   ├── application.yml # 配置文件
│   └── sql/            # 数据库脚本
└── pom.xml             # Maven配置

033-frontend/
├── src/views/         # 页面组件
├── src/api/           # API封装
├── src/router/        # 路由配置
├── src/utils/         # 工具类
├── package.json       # 依赖配置
└── vite.config.js     # Vite配置
```

## 快速启动

### 环境要求

| 工具 | 版本 |
|------|------|
| JDK | 17+ |
| Maven | 3.8+ |
| MySQL | 8.0+ |
| Node.js | 18+ |

### 后端启动

```bash
cd 033-backend
# 1. 创建数据库并执行 init.sql
# 2. 修改 application.yml 数据库配置
# spring.datasource.url=jdbc:mysql://localhost:3306/Shanghai
# spring.datasource.username=root
# spring.datasource.password=你的密码
mvn spring-boot:run
```

### 前端启动

```bash
cd 033-frontend
npm install
# 修改后端接口地址（通常在 .env.development 或 src/utils/request.js 中）
npm run dev
```

### 默认账号

| 角色 | 用户名 | 密码 | 说明 |
|------|--------|------|------|
| 管理员 | `admin` | `admin123` |  |

### 开发注意事项

1. 数据库配置：修改 `application.yml` 中的数据库连接信息（数据库名：`Shanghai`）
2. 后端启动前需先创建数据库并执行 SQL 初始化脚本
3. 后端默认端口为 `6379`，前端代理需配置对应端口
4. 默认账号密码请查看上方表格，首次登录后建议修改密码

## 检查报告

**检查日期：** 2026-04-22

**综合结论：** 已修复默认 MySQL 强依赖、初始化账号口径不清和异常吞错问题，补齐 H2 默认自举、MySQL 兼容配置与后端自动化测试；当前项目可在默认 H2 环境下通过测试并启动，管理员登录、套餐、客户、预约、订单、统计链路与前端开发代理联调均已验证通过。

> 完整检查报告请查看 `docs/checks/033-wedding-photo-studio-management-system.md`

