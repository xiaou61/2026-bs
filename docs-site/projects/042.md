---
title: 042 - 房屋租赁管理系统 🔥最新
description: 基于SpringBoot的房屋租赁管理系统的设计与实现（三角色+房源管理+预约看房+合同账单）
---

# 房屋租赁管理系统 🔥最新

> 基于SpringBoot的房屋租赁管理系统的设计与实现（三角色+房源管理+预约看房+合同账单）

## 项目概览

| 属性 | 内容 |
|------|------|
| 项目编号 | 042 |
| 项目名称 | 房屋租赁管理系统 🔥最新 |
| 项目全称 | 基于SpringBoot的房屋租赁管理系统的设计与实现（三角色+房源管理+预约看房+合同账单） |
| 项目类型 | Web应用 |
| 后端目录 | `042-backend` |
| 前端目录 | `042-frontend` |
| Spring Boot 版本 | 3.2.0 |
| 后端默认端口 | `8042` |
| 数据库名 | `Shanghai` |
| 后端 Controller | 10 个 |
| 后端 API 接口 | 51 个 |
| 数据库表 | 9 张 |
| 实体类 | 9 个 |
| 前端页面 | 23 个 |

## 项目简介

本项目是基于 Spring Boot 3.2、MyBatis-Plus、JWT 的房屋租赁管理系统后端，提供用户登录注册、房源浏览与发布、收藏、预约看房、合同签署、租金账单、报修和消息通知等核心接口。

## 项目截图

已收录 **4** 张运行截图。

### 访客

#### guest-01-house

![guest-01-house](/previews/assets/042/guest-01-house.png)

#### guest-02-profile

![guest-02-profile](/previews/assets/042/guest-02-profile.png)

#### guest-03-login

![guest-03-login](/previews/assets/042/guest-03-login.png)

#### guest-04-register

![guest-04-register](/previews/assets/042/guest-04-register.png)

## 技术栈

### 后端

- Spring Boot 3.2.0
- MyBatis-Plus 3.5.5
- MySQL 8.0
- JWT 0.12.3
- Hutool 5.8.25
- BCrypt密码加密

### 前端

- Vue 3.4.21
- Vite 5.2.0
- Element Plus 2.6.1
- Pinia 2.1.7
- Vue Router 4.3.0
- Axios 1.6.8

## 角色权限

系统支持 **1** 种角色，各角色权限如下：

### 管理员

- 用户系统管理
- 房源管理管理
- 搜索筛选管理
- 收藏功能管理
- 预约看房管理
- 租赁合同管理
- 账单管理管理
- 报修管理管理
- ...还有 3 项权限

## 功能模块

| 模块 | 说明 |
|------|------|
| **用户系统** |  |
| **房源管理** |  |
| **搜索筛选** |  |
| **收藏功能** |  |
| **预约看房** |  |
| **租赁合同** |  |
| **账单管理** |  |
| **报修管理** |  |
| **评价系统** |  |
| **消息通知** |  |
| **数据统计** |  |

### 模块架构

```
房屋租赁管理系统 🔥最新
├── 用户系统
├── 房源管理
├── 搜索筛选
├── 收藏功能
├── 预约看房
├── 租赁合同
├── 账单管理
├── 报修管理
├── 评价系统
├── 消息通知
├── 数据统计
```

## 数据库设计

本项目共包含 **9** 张数据库表。

### user

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, |
| `username` | VARCHAR(50) | NOT NULL, |
| `password` | VARCHAR(100) | NOT NULL, |
| `role` | VARCHAR(20) | NOT NULL DEFAULT 'tenant', |
| `real_name` | VARCHAR(50) | , |
| `phone` | VARCHAR(20) | , |
| `id_card` | VARCHAR(20) | , |
| `avatar` | VARCHAR(255) | , |
| ... | ... | 更多字段请查看源码 |

### house

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, |
| `landlord_id` | BIGINT | NOT NULL, |
| `title` | VARCHAR(100) | NOT NULL, |
| `description` | TEXT | , |
| `province` | VARCHAR(50) | , |
| `city` | VARCHAR(50) | , |
| `district` | VARCHAR(50) | , |
| `address` | VARCHAR(200) | , |
| ... | ... | 更多字段请查看源码 |

### house_favorite

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, |
| `user_id` | BIGINT | NOT NULL, |
| `house_id` | BIGINT | NOT NULL, |
| `create_time` | DATETIME | DEFAULT CURRENT_TIMESTAMP, |

### appointment

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, |
| `house_id` | BIGINT | NOT NULL, |
| `tenant_id` | BIGINT | NOT NULL, |
| `landlord_id` | BIGINT | NOT NULL, |
| `appointment_time` | DATETIME | NOT NULL, |
| `contact_phone` | VARCHAR(20) | , |
| `remark` | VARCHAR(500) | , |
| `status` | TINYINT | NOT NULL DEFAULT 0, |
| ... | ... | 更多字段请查看源码 |

### contract

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, |
| `contract_no` | VARCHAR(50) | NOT NULL, |
| `house_id` | BIGINT | NOT NULL, |
| `landlord_id` | BIGINT | NOT NULL, |
| `tenant_id` | BIGINT | NOT NULL, |
| `start_date` | DATE | NOT NULL, |
| `end_date` | DATE | NOT NULL, |
| `monthly_rent` | DECIMAL(10,2) | NOT NULL, |
| ... | ... | 更多字段请查看源码 |

### bill

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, |
| `bill_no` | VARCHAR(50) | NOT NULL, |
| `contract_id` | BIGINT | NOT NULL, |
| `tenant_id` | BIGINT | NOT NULL, |
| `landlord_id` | BIGINT | NOT NULL, |
| `amount` | DECIMAL(10,2) | NOT NULL, |
| `bill_month` | VARCHAR(10) | NOT NULL, |
| `due_date` | DATE | NOT NULL, |
| ... | ... | 更多字段请查看源码 |

### repair

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, |
| `house_id` | BIGINT | NOT NULL, |
| `contract_id` | BIGINT | , |
| `tenant_id` | BIGINT | NOT NULL, |
| `landlord_id` | BIGINT | NOT NULL, |
| `type` | VARCHAR(50) | NOT NULL, |
| `description` | TEXT | , |
| `images` | TEXT | , |
| ... | ... | 更多字段请查看源码 |

### review

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, |
| `house_id` | BIGINT | NOT NULL, |
| `contract_id` | BIGINT | , |
| `tenant_id` | BIGINT | NOT NULL, |
| `landlord_id` | BIGINT | NOT NULL, |
| `house_score` | INT | NOT NULL DEFAULT 5, |
| `service_score` | INT | NOT NULL DEFAULT 5, |
| `facility_score` | INT | NOT NULL DEFAULT 5, |
| ... | ... | 更多字段请查看源码 |

### message

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY, |
| `user_id` | BIGINT | NOT NULL, |
| `title` | VARCHAR(100) | NOT NULL, |
| `content` | TEXT | , |
| `type` | VARCHAR(50) | , |
| `is_read` | TINYINT | NOT NULL DEFAULT 0, |
| `create_time` | DATETIME | DEFAULT CURRENT_TIMESTAMP |

## API 接口

本项目共包含 **10** 个 Controller，**51** 个 API 接口。

### Admin

| 方法 | 路径 |
|------|------|
| GET | `/api/admin/statistics` |
| GET | `/api/admin/users` |
| PUT | `/api/admin/user/{id}/status` |
| GET | `/api/admin/houses` |
| PUT | `/api/admin/house/{id}/audit` |
| ... | 还有 2 个接口 |

### Appointment

| 方法 | 路径 |
|------|------|
| GET | `/api/appointment/list` |
| PUT | `/api/appointment/{id}/confirm` |
| PUT | `/api/appointment/{id}/reject` |
| PUT | `/api/appointment/{id}/complete` |
| PUT | `/api/appointment/{id}/cancel` |

### Auth

| 方法 | 路径 |
|------|------|
| POST | `/api/auth/register` |
| POST | `/api/auth/login` |
| GET | `/api/auth/userinfo` |
| PUT | `/api/auth/password` |
| PUT | `/api/auth/userinfo` |

### Bill

| 方法 | 路径 |
|------|------|
| GET | `/api/bill/list` |
| POST | `/api/bill/{id}/pay` |
| GET | `/api/bill/statistics` |

### Contract

| 方法 | 路径 |
|------|------|
| GET | `/api/contract/list` |
| GET | `/api/contract/{id}` |
| PUT | `/api/contract/{id}/sign` |
| PUT | `/api/contract/{id}/terminate` |

### File

| 方法 | 路径 |
|------|------|
| POST | `/api/file/upload` |

### House

| 方法 | 路径 |
|------|------|
| PUT | `/api/house/{id}` |
| DELETE | `/api/house/{id}` |
| GET | `/api/house/list` |
| GET | `/api/house/detail/{id}` |
| GET | `/api/house/my` |
| ... | 还有 3 个接口 |

### Message

| 方法 | 路径 |
|------|------|
| GET | `/api/message/list` |
| PUT | `/api/message/{id}/read` |
| PUT | `/api/message/read-all` |
| GET | `/api/message/unread-count` |

> 还有 2 个 Controller 未展示。

## 前端页面

本项目前端共包含 **23** 个页面组件。

| 目录 | 页面 | 路径 |
|------|------|------|
| admin | Contracts | `admin/Contracts` |
| admin | Dashboard | `admin/Dashboard` |
| admin | Houses | `admin/Houses` |
| admin | Users | `admin/Users` |
| auth | Login | `auth/Login` |
| auth | Register | `auth/Register` |
| home | Index | `home/Index` |
| home | Profile | `home/Profile` |
| house | Detail | `house/Detail` |
| house | List | `house/List` |
| landlord | Appointments | `landlord/Appointments` |
| landlord | Bills | `landlord/Bills` |
| landlord | Contracts | `landlord/Contracts` |
| landlord | Dashboard | `landlord/Dashboard` |
| landlord | Houses | `landlord/Houses` |
| landlord | ... | 还有 2 个页面 |
| tenant | Appointments | `tenant/Appointments` |
| tenant | Bills | `tenant/Bills` |
| tenant | Contracts | `tenant/Contracts` |
| tenant | Dashboard | `tenant/Dashboard` |
| tenant | Favorites | `tenant/Favorites` |
| tenant | ... | 还有 1 个页面 |

## 项目结构

```
042-backend/
├── src/main/java/com/xiaou/
│   ├── controller/     # 控制器 (10 个)
│   ├── entity/         # 实体类 (9 个)
│   ├── mapper/         # MyBatis映射
│   ├── service/        # 业务逻辑
│   ├── dto/            # 数据传输对象
│   ├── config/         # 配置类
│   └── common/         # 通用类
├── src/main/resources/
│   ├── application.yml # 配置文件
│   └── sql/            # 数据库脚本
└── pom.xml             # Maven配置

042-frontend/
├── src/views/         # 页面组件 (23 个)
├── src/api/           # API封装
├── src/router/        # 路由配置
├── src/utils/         # 工具类
├── package.json       # 依赖配置
└── vite.config.js     # Vite配置
```

## 快速启动

### 环境要求

| 工具 | 版本 |
|------|------|
| JDK | 17+ |
| Maven | 3.8+ |
| 数据库 | H2 内存模式（默认） |
| Node.js | 以 package.json 为准 |

### 后端启动

默认配置使用 H2 内存数据库，通常无需预先创建外部数据库。 如需切换外部数据库，请先查看 `042-backend/src/main/resources/sql/init.sql`。

```bash
cd 042-backend
mvn spring-boot:run
```

### 前端启动

```bash
cd 042-frontend
npm install
npm run dev
```

### 默认账号

| 角色 | 用户名 | 密码 | 说明 |
|------|--------|------|------|
| 管理员 | `admin` | `admin123` |  |

### 开发注意事项

1. 默认使用 H2 内存数据库，无需预先创建外部数据库
2. 如需切换外部数据库，请查看 `042-backend/src/main/resources/sql/init.sql`
3. 后端默认端口为 `8042`，前端代理需配置对应端口
4. 默认账号密码请查看上方表格，首次登录后建议修改密码

## 检查报告

**检查日期：** 2026-04-26

**综合结论：** 本轮已修复默认环境强依赖 MySQL、后端端口未项目化、前端代理仍指向 8080、H2 自举缺失、初始化账号密码哈希不可用以及后端缺少自动化冒烟测试等问题。当前后端可在默认 H2 环境下通过冒烟测试并真实启动，登录、注册、发布房源、公开房源列表/详情、收藏和预约看房链路抽测通过；前端可构建，并可通过 Vite 代理完成登录接口联调。

> 完整检查报告请查看 `docs/checks/042-house-rental-management-system.md`

