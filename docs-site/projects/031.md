---
title: 031 - 基于SpringBoot的球赛订票系统 🔥
description: 球赛订票系统（在线选座+实时订票+电子票务）
---

# 基于SpringBoot的球赛订票系统 🔥

> 球赛订票系统（在线选座+实时订票+电子票务）

## 项目概览

| 属性 | 内容 |
|------|------|
| 项目编号 | 031 |
| 项目名称 | 基于SpringBoot的球赛订票系统 🔥 |
| 项目全称 | 球赛订票系统（在线选座+实时订票+电子票务） |
| 项目类型 | Web应用 |
| 后端目录 | `031-backend` |
| 前端目录 | `031-frontend` |
| Spring Boot 版本 | 3.2.0 |
| 后端默认端口 | `8080` |
| 数据库名 | `Shanghai` |
| 数据库表 | 11 张 |

## 项目截图

已收录 **3** 张运行截图。

### 访客

#### guest-01-home

![guest-01-home](/previews/assets/031/guest-01-home.png)

#### guest-01-login

![guest-01-login](/previews/assets/031/guest-01-login.png)

#### guest-02-register

![guest-02-register](/previews/assets/031/guest-02-register.png)

## 技术栈

### 后端

- Spring Boot 3.2.0
- jOOQ 3.18.7
- MySQL 8.0
- Redis 6.0+
- JWT 0.12.3
- Hutool工具库
- BCrypt加密

### 前端

- htmx 1.9.10
- Bootstrap 5.3.0
- Vanilla JavaScript
- Font Awesome 6.4.0

## 角色权限

系统支持 **1** 种角色，各角色权限如下：

### 用户

- 用户管理

## 功能模块

| 模块 | 说明 |
|------|------|
| **用户管理** |  |
| **赛事管理** |  |
| **场馆管理** |  |
| **球队管理** |  |
| **在线选座** |  |
| **订单管理** |  |
| **支付系统** |  |
| **电子票务** |  |
| **消息通知** |  |
| **统计分析** |  |

### 模块架构

```
基于SpringBoot的球赛订票系统 🔥
├── 用户管理
├── 赛事管理
├── 场馆管理
├── 球队管理
├── 在线选座
├── 订单管理
├── 支付系统
├── 电子票务
├── 消息通知
├── 统计分析
```

## 数据库设计

本项目共包含 **11** 张数据库表。

### user

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `username` | VARCHAR(50) | NOT NULL UNIQUE, |
| `password` | VARCHAR(255) | NOT NULL, |
| `phone` | VARCHAR(20) | NOT NULL, |
| `email` | VARCHAR(100) | , |
| `real_name` | VARCHAR(50) | , |
| `id_card` | VARCHAR(20) | , |
| `role` | VARCHAR(20) | NOT NULL DEFAULT 'USER', |
| ... | ... | 更多字段请查看源码 |

### stadium

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `name` | VARCHAR(100) | NOT NULL, |
| `location` | VARCHAR(200) | NOT NULL, |
| `city` | VARCHAR(50) | NOT NULL, |
| `capacity` | INT | NOT NULL, |
| `description` | TEXT | , |
| `facilities` | VARCHAR(500) | , |
| `image_url` | VARCHAR(500) | , |
| ... | ... | 更多字段请查看源码 |

### team

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `name` | VARCHAR(100) | NOT NULL, |
| `logo_url` | VARCHAR(500) | , |
| `country` | VARCHAR(50) | , |
| `city` | VARCHAR(50) | , |
| `founded_year` | INT | , |
| `description` | TEXT | , |
| `created_at` | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP, |
| ... | ... | 更多字段请查看源码 |

### match

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `title` | VARCHAR(200) | NOT NULL, |
| `home_team_id` | BIGINT | NOT NULL, |
| `away_team_id` | BIGINT | NOT NULL, |
| `stadium_id` | BIGINT | NOT NULL, |
| `match_date` | DATETIME | NOT NULL, |
| `match_type` | VARCHAR(50) | , |
| `league` | VARCHAR(100) | , |
| ... | ... | 更多字段请查看源码 |

### seat_category

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `stadium_id` | BIGINT | NOT NULL, |
| `name` | VARCHAR(50) | NOT NULL, |
| `description` | VARCHAR(200) | , |
| `total_seats` | INT | NOT NULL, |
| `row_count` | INT | NOT NULL, |
| `column_count` | INT | NOT NULL, |
| `created_at` | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP, |

### match_pricing

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `match_id` | BIGINT | NOT NULL, |
| `category_id` | BIGINT | NOT NULL, |
| `price` | DECIMAL | (10, 2) NOT NULL, |
| `available_seats` | INT | NOT NULL, |
| `created_at` | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP, |
| `updated_at` | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP, |

### seat

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `category_id` | BIGINT | NOT NULL, |
| `row_number` | VARCHAR(10) | NOT NULL, |
| `seat_number` | VARCHAR(10) | NOT NULL, |
| `status` | VARCHAR(20) | DEFAULT 'AVAILABLE', |
| `created_at` | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP, |

### ticket_order

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `order_no` | VARCHAR(50) | NOT NULL UNIQUE, |
| `user_id` | BIGINT | NOT NULL, |
| `match_id` | BIGINT | NOT NULL, |
| `total_amount` | DECIMAL | (10, 2) NOT NULL, |
| `payment_amount` | DECIMAL | (10, 2) NOT NULL, |
| `discount_amount` | DECIMAL | (10, 2) DEFAULT 0.00, |
| `status` | VARCHAR(20) | NOT NULL DEFAULT 'PENDING', |
| ... | ... | 更多字段请查看源码 |

### ticket

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `ticket_no` | VARCHAR(50) | NOT NULL UNIQUE, |
| `order_id` | BIGINT | NOT NULL, |
| `match_id` | BIGINT | NOT NULL, |
| `seat_id` | BIGINT | NOT NULL, |
| `category_id` | BIGINT | NOT NULL, |
| `price` | DECIMAL | (10, 2) NOT NULL, |
| `status` | VARCHAR(20) | NOT NULL DEFAULT 'VALID', |
| ... | ... | 更多字段请查看源码 |

### user_favorite

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BIGINT | PRIMARY KEY AUTO_INCREMENT, |
| `user_id` | BIGINT | NOT NULL, |
| `team_id` | BIGINT | NOT NULL, |
| `created_at` | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP, |

> 还有 1 张表未展示，完整表结构请查看 `031-backend/src/main/resources/sql/init.sql`

## 项目结构

```
031-backend/
├── src/main/java/com/xiaou/
│   ├── controller/     # 控制器
│   ├── entity/         # 实体类
│   ├── mapper/         # MyBatis映射
│   ├── service/        # 业务逻辑
│   ├── dto/            # 数据传输对象
│   ├── config/         # 配置类
│   └── common/         # 通用类
├── src/main/resources/
│   ├── application.yml # 配置文件
│   └── sql/            # 数据库脚本
└── pom.xml             # Maven配置

031-frontend/
├── src/views/         # 页面组件
├── src/api/           # API封装
├── src/router/        # 路由配置
├── src/utils/         # 工具类
├── package.json       # 依赖配置
└── vite.config.js     # Vite配置
```

## 快速启动

### 环境要求

| 工具 | 版本 |
|------|------|
| JDK | 17+ |
| Maven | 3.8+ |
| 数据库 | H2 内存模式（默认） |
| Node.js | 以 package.json 为准 |

### 后端启动

默认配置使用 H2 内存数据库，通常无需预先创建外部数据库。 如需切换外部数据库，请先查看 `031-backend/src/main/resources/schema.sql`。

```bash
cd 031-backend
mvn spring-boot:run
```

### 前端启动

```bash
cd 031-frontend
npm install
npm run dev
```

### 默认账号

| 角色 | 用户名 | 密码 | 说明 |
|------|--------|------|------|
| 用户 | `user1` | `user123` |  |

### 开发注意事项

1. 默认使用 H2 内存数据库，无需预先创建外部数据库
2. 如需切换外部数据库，请查看 `031-backend/src/main/resources/schema.sql`
3. 后端默认端口为 `8080`，前端代理需配置对应端口
4. 默认账号密码请查看上方表格，首次登录后建议修改密码

## 检查报告

**检查日期：** 2026-04-21

**综合结论：** 已完成默认环境自举、JDK 17 兼容复验、订票主链路修复、JWT 鉴权补齐、前端真实接口接入、后端集成测试补齐与浏览器复测；当前项目可在默认 H2 环境下通过测试并启动。

> 完整检查报告请查看 `docs/checks/031-football-ticket-booking-system.md`

