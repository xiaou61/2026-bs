<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.opera.mapper.CulturalSeasonMapper">

    <select id="selectList" resultType="CulturalSeason">
        select * from cultural_season
        <where>
            <if test="termName != null and termName != ''">
                and term_name like concat('%', #{termName}, '%')
            </if>
            <if test="status != null">
                and status = #{status}
            </if>
            <if test="currentFlag != null">
                and current_flag = #{currentFlag}
            </if>
        </where>
        order by id desc
    </select>

    <select id="selectEnabled" resultType="CulturalSeason">
        select * from cultural_season where status = 1 order by current_flag desc, id desc
    </select>

    <select id="selectById" resultType="CulturalSeason">
        select * from cultural_season where id = #{id}
    </select>

    <insert id="insert" parameterType="CulturalSeason" useGeneratedKeys="true" keyProperty="id">
        insert into cultural_season (term_name, start_date, end_date, current_flag, status)
        values (#{termName}, #{startDate}, #{endDate}, #{currentFlag}, #{status})
    </insert>

    <update id="update" parameterType="CulturalSeason">
        update cultural_season
        <set>
            <if test="termName != null">term_name = #{termName},</if>
            <if test="startDate != null">start_date = #{startDate},</if>
            <if test="endDate != null">end_date = #{endDate},</if>
            <if test="currentFlag != null">current_flag = #{currentFlag},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        where id = #{id}
    </update>

    <delete id="deleteById">
        delete from cultural_season where id = #{id}
    </delete>

</mapper>

