<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hrm.mapper.AnnouncementMapper">
    <resultMap id="BaseResultMap" type="com.hrm.entity.Announcement">
        <id column="id" property="id"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="publisher_id" property="publisherId"/>
        <result column="is_top" property="isTop"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="publisher_name" property="publisherName"/>
    </resultMap>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT a.*, u.name as publisher_name
        FROM announcement a
        LEFT JOIN user u ON a.publisher_id = u.id
        WHERE a.id = #{id}
    </select>

    <select id="selectList" resultMap="BaseResultMap">
        SELECT a.*, u.name as publisher_name
        FROM announcement a
        LEFT JOIN user u ON a.publisher_id = u.id
        <where>
            <if test="title != null and title != ''">
                AND a.title LIKE CONCAT('%', #{title}, '%')
            </if>
            <if test="status != null">
                AND a.status = #{status}
            </if>
        </where>
        ORDER BY a.is_top DESC, a.create_time DESC
    </select>

    <select id="selectTop" resultMap="BaseResultMap">
        SELECT a.*, u.name as publisher_name
        FROM announcement a
        LEFT JOIN user u ON a.publisher_id = u.id
        WHERE a.status = 1
        ORDER BY a.is_top DESC, a.create_time DESC
        LIMIT #{limit}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO announcement (title, content, publisher_id, is_top, status, create_time)
        VALUES (#{title}, #{content}, #{publisherId}, #{isTop}, #{status}, NOW())
    </insert>

    <update id="update">
        UPDATE announcement
        <set>
            <if test="title != null">title = #{title},</if>
            <if test="content != null">content = #{content},</if>
            <if test="isTop != null">is_top = #{isTop},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM announcement WHERE id = #{id}
    </delete>
</mapper>
