<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaou.mapper.CategoryMapper">
    
    <resultMap id="BaseResultMap" type="com.xiaou.entity.Category">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="icon" property="icon"/>
        <result column="sort" property="sort"/>
        <result column="status" property="status"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <select id="findAll" resultMap="BaseResultMap">
        SELECT * FROM category ORDER BY sort ASC
    </select>

    <select id="findByStatus" resultMap="BaseResultMap">
        SELECT * FROM category WHERE status = #{status} ORDER BY sort ASC
    </select>

    <select id="findById" resultMap="BaseResultMap">
        SELECT * FROM category WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO category (name, icon, sort, status)
        VALUES (#{name}, #{icon}, #{sort}, #{status})
    </insert>

    <update id="update">
        UPDATE category
        SET name = #{name},
            icon = #{icon},
            sort = #{sort},
            status = #{status}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM category WHERE id = #{id}
    </delete>

</mapper>

