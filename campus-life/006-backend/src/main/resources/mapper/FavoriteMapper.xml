<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaou.mapper.FavoriteMapper">
    
    <resultMap id="BaseResultMap" type="com.xiaou.entity.Favorite">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="item_type" property="itemType"/>
        <result column="item_id" property="itemId"/>
        <result column="created_at" property="createdAt"/>
    </resultMap>

    <select id="findByUserId" resultMap="BaseResultMap">
        SELECT * FROM favorite WHERE user_id = #{userId} ORDER BY created_at DESC
    </select>

    <select id="findOne" resultMap="BaseResultMap">
        SELECT * FROM favorite 
        WHERE user_id = #{userId} AND item_type = #{itemType} AND item_id = #{itemId}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO favorite (user_id, item_type, item_id)
        VALUES (#{userId}, #{itemType}, #{itemId})
    </insert>

    <delete id="deleteById">
        DELETE FROM favorite WHERE id = #{id}
    </delete>

    <delete id="deleteByUserAndItem">
        DELETE FROM favorite 
        WHERE user_id = #{userId} AND item_type = #{itemType} AND item_id = #{itemId}
    </delete>

</mapper>

