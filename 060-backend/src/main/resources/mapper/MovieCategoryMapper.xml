<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.movie.mapper.MovieCategoryMapper">

    <select id="selectPage" resultType="MovieCategory">
        SELECT * FROM movie_category
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%', #{name}, '%')
            </if>
        </where>
        ORDER BY sort ASC
    </select>

    <select id="selectAll" resultType="MovieCategory">
        SELECT * FROM movie_category WHERE status = 1 ORDER BY sort ASC
    </select>

    <select id="selectById" resultType="MovieCategory">
        SELECT * FROM movie_category WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO movie_category (name, sort, status)
        VALUES (#{name}, #{sort}, #{status})
    </insert>

    <update id="update">
        UPDATE movie_category SET name = #{name}, sort = #{sort}, status = #{status} WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM movie_category WHERE id = #{id}
    </delete>
</mapper>
