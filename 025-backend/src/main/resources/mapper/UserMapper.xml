<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiaou.community.mapper.UserMapper">
    <select id="findByUsername" resultType="com.xiaou.community.entity.User">
        SELECT * FROM user WHERE username = #{username}
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user(username, password, role, create_time) VALUES(#{username}, #{password}, #{role}, NOW())
    </insert>
    <select id="findById" resultType="com.xiaou.community.entity.User">
        SELECT * FROM user WHERE id = #{id}
    </select>
    <update id="update">
        UPDATE user SET password = #{password} WHERE id = #{id}
    </update>
    <delete id="deleteById">
        DELETE FROM user WHERE id = #{id}
    </delete>
    <select id="findAll" resultType="com.xiaou.community.entity.User">
        SELECT * FROM user
    </select>
</mapper>
