# 018 校园实习招聘与求职平台巡检计划

## 目标

完成 `018` 校园实习招聘与求职平台的 PRD 一致性核对、JDK 17 兼容性验证、测试与启动验证，并对明确问题直接修复，最终形成可追踪的检查报告。

## 范围

- 文档：`PRD.md`、`README.md`、`ACCOUNTS.md`
- 后端：配置、SQL、控制器、服务、Mapper、测试
- 前端：路由、API 模块、页面、布局、构建配置
- 验证项：JDK 17、Maven 测试、前端构建、数据库初始化、前后端启动、登录、学生端、企业端与核心招聘求职链路

## 分阶段执行

### Phase 1：建档与上下文同步
- [x] 更新总台账，标记 `018` 为检查中
- [x] 建立 `018` 检查报告
- [x] 建立 `018` 巡检计划
- [x] 切换 `task_plan.md`、`findings.md`、`progress.md`
- 状态：`complete`

### Phase 2：文档与源码核对
- [x] 核对 PRD、README、账号文档、配置与 SQL
- [x] 核对前后端路由、接口、服务与页面接线
- [x] 识别当前已知断链与功能缺口
- 状态：`complete`

### Phase 3：测试、启动与抽测
- [x] 验证 `mvn -version`
- [x] 执行 `mvn test -DskipTests=false`
- [x] 执行 `npm run build`
- [x] 初始化数据库并启动前后端
- [x] 抽测登录、职位浏览、简历、投递、企业端管理等核心链路
- 状态：`complete`

### Phase 4：问题修复与复测
- [x] 修复页面断链、权限问题和核心业务缺口
- [x] 补齐必要测试和异常语义
- [x] 复测启动和核心链路
- 状态：`complete`

### Phase 5：回填与收尾
- [x] 更新总台账和单项目检查报告
- [x] 同步 `task_plan.md` / `findings.md` / `progress.md`
- [x] 清理本轮进程和临时状态
- 状态：`complete`

## 当前结论

1. `018` 已完成文档核对、问题修复、测试、启动与业务链路复测
2. 后端 `mvn test`、前端 `npm run build`、后端 `8080` 启动和前端 `5173` 页面验证均已通过
3. 本轮巡检已完成收尾，工作区已恢复到无验证进程状态
