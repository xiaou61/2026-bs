# 017 高校自助点餐系统巡检计划

## 目标

完成 `017` 高校自助点餐系统的 PRD 一致性核对、JDK 17 兼容性验证、测试与启动验证，并对明确问题直接修复，最终形成可追踪的检查报告。

## 范围

- 文档：`PRD.md`、`README.md`、`ACCOUNTS.md`
- 后端：配置、SQL、控制器、服务、Mapper、模板页面
- 验证项：JDK 17、Maven 测试、数据库初始化、应用启动、核心页面访问、登录与核心业务链路

## 分阶段执行

### Phase 1：建档与上下文同步
- [x] 更新总台账，标记 `017` 为检查中
- [x] 建立 `017` 检查报告
- [x] 建立 `017` 巡检计划
- [x] 切换 `task_plan.md`、`findings.md`、`progress.md`
- 状态：`complete`

### Phase 2：文档与源码核对
- [x] 核对 PRD、README、账号文档、配置与 SQL
- [x] 核对控制器、服务、模板页面与已声明接口
- [x] 识别当前已知断链与功能缺口
- 状态：`complete`

### Phase 3：测试、启动与抽测
- [x] 验证 `mvn -version`
- [x] 执行 `mvn test -DskipTests=false`
- [x] 初始化数据库并启动项目
- [x] 抽测首页、登录、商家列表、商家详情、购物车、订单、商家端和后台统计
- 状态：`complete`

### Phase 4：问题修复与复测
- [x] 修复页面断链、权限问题和核心业务缺口
- [x] 补齐必要测试和异常语义
- [x] 复测启动和核心链路
- 状态：`complete`

### Phase 5：回填与收尾
- [x] 更新总台账和单项目检查报告
- [x] 同步 `task_plan.md` / `findings.md` / `progress.md`
- [x] 清理本轮进程和临时状态
- 状态：`complete`

## 当前结论

1. `017` 已可以在 JDK `17` + MySQL `root / 1234` 环境下启动
2. 学生登录、购物车、下单、商家接单 / 出餐、用户确认取餐和后台统计已形成可复测闭环
3. 仍存在较多 PRD 增强能力未落地，需要在后续批次继续补全
