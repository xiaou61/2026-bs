# 毕设项目巡检总台账

## 统计口径

- 总项目数：95
- 检查单位：按项目编号统计，例如 `001`
- 当前目标：逐个项目核对 PRD、代码完整性、JDK 17 兼容性、测试结果、启动结果

## 实时统计

| 指标 | 数量 |
| --- | ---: |
| 总项目数 | 95 |
| 已开始检查 | 70 |
| 已完成检查 | 70 |
| 已通过测试并可启动 | 70 |
| 检查中 | 0 |
| 待修复 | 0 |
| 阻塞 | 0 |
| 未开始 | 25 |

## 项目进度

| 项目 | 名称 | 状态 | 文档完整性 | JDK17兼容性 | 测试结果 | 启动结果 | 最近更新 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 001 | 校园事务管理系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`） | 通过（前端 `3000`，后端 `18080`；默认 `8080` 被外部进程占用） | 2026-04-04 |
| 002 | 在线选课与成绩管理系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`） | 通过（后端登录接口 `200`，前端页面响应 `200`） | 2026-04-04 |
| 003 | 助农精准扶贫平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`） | 通过（首页 `200`，登录接口 `200`） | 2026-04-04 |
| 004 | 实时聊天与通知系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`） | 通过（后端登录接口 `200`，前端页面响应 `200`） | 2026-04-04 |
| 005 | 在线问卷调查与数据分析系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`） | 通过（首页 `200`、登录页 `200`、登录接口 `200`） | 2026-04-04 |
| 006 | 校园失物招领系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`） | 通过（登录页 `200`、登录接口 `200`、登录态首页 `200`） | 2026-04-04 |
| 007 | 志愿活动管理与积分激励平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`） | 通过（后端登录接口 `200`，前端页面响应 `200`） | 2026-04-04 |
| 008 | 智能菜谱推荐系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`） | 通过（登录接口 `200`，静态页面响应 `200`，受保护接口未登录返回 `401`） | 2026-04-04 |
| 009 | 校园快递代收管理系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`） | 通过（前后端成功启动，登录/分页/权限/代理链路复测通过） | 2026-04-04 |
| 010 | 图书馆座位预约系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`） | 通过（后端成功启动，页面/登录/预约/权限/统计链路复测通过） | 2026-04-04 |
| 011 | 校园短视频创作与分享平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`） | 通过（后端 `8011`、前端 `5173`；登录、草稿、统计、相关推荐、积分商城与浏览器页面复测通过） | 2026-04-05 |
| 012 | 在线协作白板与笔记系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`，接口抽测与 Playwright 浏览器复测） | 通过（后端 `8012`；登录、白板、脑图、实时协作、分享与附件链路复测通过） | 2026-04-05 |
| 013 | 校园共享经济平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`） | 通过（后端 `8013`、前端 `5013`；登录、分页、共享物品管理、收藏、提现、钱包与共享租借链路复测通过） | 2026-04-05 |
| 014 | 校园社团与兴趣圈层平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`，接口抽测与静态页面访问复测） | 通过（后端 `8080`；公开列表、勋章、创建社团/圈子/话题、管理员审核、活动报名签到评价链路复测通过） | 2026-04-05 |
| 015 | 校园表白墙与匿名社交平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`，接口抽测与静态页面访问复测） | 通过（后端 `8080`；登录、权限、发帖、评论/回复、点赞、举报、私信、敏感词待审与实名认证审核链路复测通过） | 2026-04-05 |
| 016 | 校园快递代领服务平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`） | 通过（后端 `8080`、前端 `5173`；登录、注册、资料修改、密码修改、发单、接单、上传凭证、确认收货、评价、投诉、通知与后台管理链路复测通过） | 2026-04-05 |
| 017 | 高校自助点餐系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`） | 通过（后端 `8080`；登录、加购、下单、商家接单/出餐、用户确认取餐与后台统计链路复测通过） | 2026-04-06 |
| 018 | 校园实习招聘与求职平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`） | 通过（后端 `8080`、前端 `5173`；登录、公开详情、注册、岗位发布、简历、投递、面试、管理员后台与浏览器页面复测通过） | 2026-04-06 |
| 019 | 校园运动健康管理平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`、接口抽测） | 通过（后端 `8080`、前端 `5173`；默认账号登录、运动打卡、计划进度、健康档案、场馆预约与约球活动链路复测通过） | 2026-04-06 |
| 020 | 校园学习资源共享平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`、接口抽测） | 通过（后端 `8020`、前端 `5020`；登录脱敏、上传奖励、回答奖励、私密笔记权限链路复测通过） | 2026-04-06 |
| 021 | 校园二手交易平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`、接口抽测） | 通过（后端 `8080`、前端 `3021`；注册、发布、收藏、议价、订单、评价、资料更新与图片上传链路复测通过） | 2026-04-08 |
| 022 | 校园自习室座位预约系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`、接口抽测与 Playwright 浏览器复测） | 通过（后端 `8022`、前端 `3022`；学生端、管理端、分页、代理、自习室/座位/预约/信用/系统配置链路复测通过） | 2026-04-08 |
| 023 | AI智能学习助手与个性化教育平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、接口抽测） | 通过（后端 `8023`；登录、课程详情、评论、学习路径、分析面板、问答会话与导出链路复测通过） | 2026-04-08 |
| 024 | 流浪动物领养与救助平台 | 已完成 | 明显缺口 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`） | 通过（后端 `8024`、前端 `3024`；注册、登录、宠物列表与宠物 CRUD 链路复测通过） | 2026-04-10 |
| 025 | 智慧社区综合服务管理平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`、接口抽测与浏览器复测） | 通过（后端 `8025`、前端 `3025`；登录、业主、缴费、报修、公告、访客、车位链路复测通过） | 2026-04-10 |
| 026 | 画师接稿平台 | 已完成 | 明显缺口 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`、新增冒烟测试） | 通过（后端 `8026`、前端 `3026`；登录脱敏、已审核画师、开放需求、尾款结算、画师审核角色回写链路复测通过） | 2026-04-14 |
| 027 | 线上理发预约系统 | 已完成 | 明显缺口 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`、接口抽测） | 通过（后端 `8027`、前端 `3027`；登录、门店、充值、支付、评价、预约/取消链路复测通过） | 2026-04-14 |
| 028 | 校园共享自行车租赁系统 | 已完成 | 明显缺口 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`、接口抽测） | 通过（后端 `8028`、前端 `3028`；登录、注册、停车点、租还车、钱包充值、故障上报与后台统计链路复测通过） | 2026-04-14 |
| 029 | 中药食疗推荐交流平台 | 已完成 | 明显缺口 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`、新增冒烟测试） | 通过（后端 `8080`、前端 `5173`；默认 H2 自举，登录、公开列表、管理员接口与前端首页访问复测通过） | 2026-04-15 |
| 030 | 居民智能健康管理平台 | 已完成 | 明显缺口 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`） | 通过（后端 `8080`、前端 `3000`；默认 H2 自举，患者/管理员登录、医生审核权限、健康知识、评估预警与统计分析链路复测通过） | 2026-04-21 |
| 031 | 基于SpringBoot的球赛订票系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、新增订票/退款自动化测试） | 通过（后端 `18034`；默认 H2 自举，登录、票价/座位、下单、支付、出票、取消退款链路复测通过） | 2026-04-21 |
| 032 | 社区老年人健康管理系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`） | 通过（后端 `8032`、前端 `5132`；默认 H2 自举，登录、老人建档、测量、评估、预约、随访、统计链路复测通过） | 2026-04-21 |
| 033 | 婚纱写真馆管理系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`） | 通过（后端 `8080`、前端 `5133`；默认 H2 自举，登录、套餐、客户、预约、订单、统计与前端代理登录链路复测通过） | 2026-04-22 |
| 034 | 零食铺子仓储管理系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、新增启动/登录烟雾测试） | 通过（后端 `8080`、静态前端 `5134`；默认 H2 自举，登录、SKU、库存、入库、出库、调拨、盘点与前端默认 API 联调复测通过） | 2026-04-22 |
| 035 | 乡村振兴水稻收割预约系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、新增预约流转与权限测试；前端 `npm run build`） | 通过（后端 `8035`、前端 `5135`；默认 H2 自举，登录、地块、设备、预约、权限与前端代理联调复测通过） | 2026-04-25 |
| 036 | 小梦想全球公益捐赠平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、新增鉴权/捐赠/项目权限测试；前端 `npm run build`） | 通过（后端 `8036`、前端 `3036`；默认 H2 自举，登录、项目详情、捐赠、个人中心与前端代理联调复测通过） | 2026-04-26 |
| 037 | 基于SpringBoot的编程学习交流平台 | 已完成 | 明显缺口 | 通过 | 通过（后端 `mvn test`、新增微信登录/课程公开接口测试；小程序页面静态核查通过） | 通过（后端 `8037`；默认 H2 自举，模拟微信登录、当前用户、课程列表/热门/详情接口抽测通过） | 2026-04-26 |
| 038 | 基于SpringBoot的个人饮食管理系统 | 已完成 | 明显缺口 | 通过 | 通过（后端 `mvn test`、新增登录/食物/营养汇总冒烟测试；前端 `npm run build`） | 通过（后端 `8038`、前端 `3038`；默认 H2 自举，登录、食物库、营养汇总、健康数据、食谱与前端代理联调复测通过） | 2026-04-26 |
| 039 | 民歌民谣交流平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、新增登录/分类/民歌/后台冒烟测试；前端 `npm run build`） | 通过（后端 `8039`、前端 `3039`；默认 H2 自举，登录、分类、民歌列表、后台用户列表与前端代理登录链路复测通过） | 2026-04-26 |
| 040 | 社区车辆违停处理系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、新增登录/违停类型/举报/审核冒烟测试；前端 `npm run build`） | 通过（后端 `8040`、前端 `3040`；默认 H2 自举，登录、违停类型、举报提交、举报审核与前端代理登录链路复测通过） | 2026-04-26 |
| 041 | 在线心理评测与咨询系统 | 已完成 | 明显缺口 | 通过 | 通过（后端 `mvn test`、新增登录/量表/咨询师/文章/测评/预约冒烟测试；前端 `npm run build`） | 通过（后端 `8041`、前端 `3041`；默认 H2 自举，登录、量表、咨询师、文章、测评提交、预约创建与前端代理登录链路复测通过） | 2026-04-26 |
| 042 | 房屋租赁管理系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、新增登录/注册/房源/收藏/预约冒烟测试；前端 `npm run build`） | 通过（后端 `8042`、前端 `3042`；默认 H2 自举，登录、房源列表/详情、收藏、预约看房与前端代理登录链路复测通过） | 2026-04-26 |
| 043 | 宠物寄养服务系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、新增登录/服务商/宠物/预约冒烟测试；前端 `npm run build`） | 通过（后端 `8043`、前端 `3043`；默认 H2 自举，登录、服务商列表/服务项目、宠物新增、预约创建与前端代理联调复测通过） | 2026-04-26 |
| 044 | 特色民宿预订平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、新增登录/民宿/收藏/预订/房东确认冒烟测试；前端 `npm run build`） | 通过（后端 `8044`、前端 `3044`；默认 H2 自举，登录、民宿列表/详情、收藏、预订与前端代理联调复测通过） | 2026-04-26 |
| 045 | 养老院管理系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、新增登录/统计/健康记录/账单/探访冒烟测试；前端 `npm run build`） | 通过（后端 `8045`、前端 `3045`；默认 H2 自举，登录、公告、管理员统计、健康记录、账单缴费、探访审批与前端代理联调复测通过） | 2026-04-26 |
| 046 | 垃圾回收服务系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、新增登录/预约/接单/完成/积分兑换冒烟测试；前端 `npm run build`） | 通过（后端 `8046`、前端 `3046`；默认 H2 自举，居民预约、回收员接单完成、积分兑换、管理员发放与前端代理联调复测通过） | 2026-04-26 |
| 047 | 剧本杀创作与预约平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、新增四角色/预约/评价/创作/审核冒烟测试；前端 `npm run build`） | 通过（后端 `8047`、前端 `3047`；默认 H2 自举，玩家预约、店家确认完成、作者创作、管理员审核与前端代理联调复测通过） | 2026-04-26 |
| 048 | 博物馆文物数字化管理平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、新增四角色/参观预约/讲解预约/研究审核冒烟测试；前端 `npm run build`） | 通过（后端 `8048`、前端 `3048`；默认 H2 自举，文物浏览、预约确认、讲解完成、研究审核与前端代理联调复测通过） | 2026-04-26 |
| 049 | 基于微信小程序的考研学习系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn clean test`、新增登录/课程/题库/错题/计划/笔记/收藏/社区/打卡冒烟测试；小程序页面静态核查通过） | 通过（后端 `8049`；默认 H2 自举，学生登录、公开科目课程、打卡统计、答题提交、管理员仪表盘与小程序配置复测通过） | 2026-04-27 |
| 050 | 基于微信小程序的课堂考勤签到APP | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn clean test`、新增三角色/课程/签到/统计/请假/消息冒烟测试；小程序页面静态核查通过） | 通过（后端 `8050`；默认 H2 自举，三角色登录、教师创建签到、学生签到、统计和消息接口复测通过） | 2026-04-27 |
| 051 | 基于微信小程序的网络安全知识科普APP | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn clean test`、新增登录/权限/答题/学习/收藏/问答冒烟测试；管理端 `npm run build`；小程序页面静态核查通过） | 通过（后端 `8051`、管理端 `3051`；默认 H2 自举，模拟微信登录、管理员统计、答题、学习记录、收藏和问答接口复测通过） | 2026-04-27 |
| 052 | 网课在线学习观看系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn clean test`、新增游客/登录/权限/学习/收藏/评论冒烟测试；用户端和管理端 `npm run build`） | 通过（后端 `8052`、用户端 `3052`、管理端 `3152`；默认 H2 自举，课程、章节、管理员统计、注册、学习进度、收藏和评论接口复测通过） | 2026-04-27 |
| 053 | 基于SpringBoot的救灾物资调度与救援系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn clean test`、新增登录/权限/灾情/库存/调度/任务/公告冒烟测试；前端 `npm run build`） | 通过（后端 `8053`、前端 `3053`；默认 H2 自举，登录、统计、灾情上报、入库、调度审批、任务状态和前端代理接口复测通过） | 2026-04-28 |
| 054 | 基于SpringBoot的农业生产技术管理系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、新增登录/权限/用户脱敏/分类/统计冒烟测试；前端 `npm run build`） | 通过（后端 `8054`、前端 `3054`；默认 H2 自举，登录、用户改密、统计权限、作物、知识、计划任务、病虫害、农资、咨询、预约和公告接口复测通过） | 2026-04-28 |
| 055 | 基于Java的企业OA管理系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、新增登录/鉴权/权限/请假/考勤冒烟测试；前端 `npm run build`） | 通过（后端 `8055`、前端 `3055`；默认 H2 自举，登录、权限、统计、部门、公告、请假审批、考勤、会议室和工作日志接口复测通过） | 2026-04-28 |
| 056 | 短文写作竞赛管理小程序 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、新增公开接口/登录/权限/投稿/审核/评分冒烟测试；管理端 `npm run build`；小程序静态核查通过） | 通过（后端 `8056`、管理端 `3056`；默认 H2 自举，公开竞赛/公告/分类、登录、权限、投稿、审核、评委评分和统计接口复测通过） | 2026-04-28 |
| 057 | 招生管理系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、新增登录/权限/统计/招生流程冒烟测试；前端 `npm run build`） | 通过（后端 `8057`、前端 `3057`；默认 H2 自举，登录、权限、院系、专业、计划、考生、成绩、报名、录取和公告接口复测通过） | 2026-04-28 |
| 058 | 鲜牛奶订购系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、新增游客/登录/权限/订阅/配送/投诉冒烟测试；前端 `npm run build`） | 通过（后端 `8058`、前端 `3058`；默认 H2 自举，游客商品、登录脱敏、权限、地址、订阅、订单、投诉、通知、配送和后台统计接口复测通过） | 2026-04-29 |
| 059 | 制造装备物联及生产管理 ERP 系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、新增登录/权限/设备/生产/物料/质检/维保冒烟测试；前端 `npm run build`） | 通过（后端 `8059`、前端 `3059`；默认 H2 自举，登录脱敏、权限、看板、设备、传感器、告警、生产、物料、质检和维保接口复测通过） | 2026-04-29 |
| 060 | 电影订票及评论网站 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、新增登录/权限/电影/订票/评论冒烟测试；前端 `npm run build`） | 通过（后端 `8060`、前端 `3060`；默认 H2 自举，登录脱敏、权限、电影、排片、收藏、下单、订单归属、评论和看板接口复测通过） | 2026-04-29 |
| 061 | 游戏交易系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、新增登录/权限/商品/订单/申诉冒烟测试；前端 `npm run build`） | 通过（后端 `8061`、前端 `3061`；默认 H2 自举，登录脱敏、权限、商品、收藏、下单支付、卖家发货、买家完成、评价、申诉和看板接口复测通过） | 2026-04-29 |
| 062 | 反欺诈平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、新增三角色/权限/登出/风控事件/预警/案件/申诉冒烟测试；前端 `npm run build`） | 通过（后端 `8062`、前端 `3062`；默认 H2 自举，登录脱敏、权限、登出失效、黑名单、规则、事件上报、自动预警、案件、申诉、公告和看板接口复测通过） | 2026-04-29 |
| 063 | 进销存管理系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、新增登录/权限/采购/销售/库存冒烟测试；前端 `npm run build`） | 通过（后端 `8063`、前端 `3063`；默认 H2 自举，登录脱敏、权限、采购入库、销售出库、库存流水、公告和看板接口复测通过） | 2026-04-29 |
| 064 | 经方药食两用服务平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、新增登录/权限/预约/体质/收藏冒烟测试；前端 `npm run build`） | 通过（后端 `8064`、前端 `3064`；默认 H2 自举，登录脱敏、权限、经方药膳、服务预约、体质记录、收藏、公告和看板接口复测通过） | 2026-04-29 |
| 065 | 名城小区物业管理系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、新增三角色/权限/缴费/报修/投诉/访客冒烟测试；前端 `npm run build`） | 通过（后端 `8065`、前端 `3065`；默认 H2 自举，登录脱敏、权限、楼栋房屋、缴费、报修、投诉、访客、车位、公告和看板接口复测通过） | 2026-04-29 |
| 066 | 精简博客系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、新增公开博客/登录/权限/发文/评论冒烟测试；前端 `npm run build`） | 通过（后端 `8066`、前端 `3066`；默认 H2 自举，登录脱敏、权限、文章发布置顶、评论审核、公告和看板接口复测通过） | 2026-04-30 |
| 067 | 大学生一体化服务平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、新增三角色/课程/活动/岗位/失物冒烟测试；前端 `npm run build`） | 通过（后端 `8067`、前端 `3067`；默认 H2 自举，登录脱敏、权限、课程选课、活动报名、岗位投递、失物招领、公告和看板接口复测通过） | 2026-04-30 |
| 068 | 周边游平台个人管理模块 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、新增登录/权限/出行人/收藏/订单/评价/投诉冒烟测试；前端 `npm run build`） | 通过（后端 `8068`、前端 `3068`；默认 H2 自举，登录脱敏、权限、景点、出行人、收藏、下单支付完成、评价回复、投诉处理和看板接口复测通过） | 2026-04-30 |
| 069 | 科任教师考评系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、新增三角色登录/权限/评教/申诉/公告冒烟测试；前端 `npm run build`） | 通过（后端 `8069`、前端 `3069`；默认 H2 自举，登录脱敏、权限、基础档案、学生评教、教师申诉、管理员处理和看板接口复测通过） | 2026-04-30 |
| 070 | 基于SpringBoot和Vue的最优网络购票系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、新增购票/支付/核销/评论冒烟测试；前端 `npm run build`） | 通过（后端 `8070`、前端 `3070`；默认 H2 自举，登录脱敏、权限、选座锁座、下单、余额支付、出票核销、评论审核和登出失效接口复测通过） | 2026-04-30 |

## 状态说明

- `未开始`：尚未进入巡检
- `检查中`：已建档，正在核对文档、代码、测试或启动链路
- `待修复`：已找到问题，等待修复后复验
- `阻塞`：受外部依赖影响，当前无法继续推进
- `已完成`：检查结论已写入单项目报告
- `已通过测试并可启动`：在 `已完成` 基础上，前后端测试与启动验证均通过

## 备注

- `001` 的默认后端端口 `8080` 在当前机器上被其他 Java 进程占用，但项目本身在切换到 `18080` 后可正常启动并完成登录接口验证。
- `002` 在巡检过程中出现过端口占用复现，根因是验证脚本留下了 `spring-boot:run` / Vite 子进程，不是项目代码自身启动失败。
- `003` 为 Spring Boot + Thymeleaf 一体化项目，无单独前端目录；当前核心页面和登录链路已验证通过。
- `004` 在本机 Redis 未启动的情况下仍可完成基础启动与登录验证，但实时聊天、在线状态和通知的完整联调能力仍需后续补充验证。
- `005` 为 Spring Boot + Thymeleaf 一体化项目，无独立前端目录；当前可启动并完成登录验证，但 PRD 中“前后端分离”和部分扩展功能描述与现有实现不完全一致。
- `006` 为 Spring Boot + Thymeleaf 一体化项目，无独立前端目录；当前可启动并完成管理员登录验证，但 PRD 中 JWT、管理员默认密码和部分管理员扩展能力与现有实现不完全一致。
- `007` 为前后端分离项目，当前可启动并完成登录验证；已修复 `ACCOUNTS.md` 管理员密码错误和 `/api/auth/info` 返回 `400` 的问题，剩余风险主要是 MD5 密码存储与前端主包偏大。
- `008` 为 Spring Boot 一体化部署项目，前端位于 `static/`；当前可启动并完成登录与页面访问验证，已修复受保护接口未登录返回 `200` + `code=500` 的问题和 MySQL 驱动坐标告警，剩余风险主要是 MD5 密码存储与自动化测试缺失。
- `009` 为前后端分离项目，当前可启动并完成登录、分页、权限、代理与模板下载验证；已修复文档默认密码不一致、批量导入通知 `expressId` 为空、分页拦截器缺失以及服务端角色权限缺失问题，剩余风险主要是自动化测试缺失、MD5 密码存储与前端大包体积。
- `010` 为 Spring Boot + 静态页面一体化项目，无独立前端目录；当前可启动并完成学生/管理员登录、页面访问、预约列表、预约创建/取消、通知、统计与权限校验验证，已修复 PRD 口径不一致、MySQL 驱动旧坐标、服务端管理员权限缺失、预约返回缺少 `seatNo` 以及未登录返回乱码问题，剩余风险主要是自动化测试缺失、MD5 密码存储与 README/快速启动文档缺失。
- `011` 为前后端分离项目，当前可启动并完成登录、权限、草稿、创作中心、单视频统计、相关推荐、积分商城和浏览器页面验证；已修复用户端关键 PRD 缺口、历史媒体 URL 脏数据、发布视频 `publishTime` 空值与服务端管理员权限缺失问题，剩余风险主要是管理员前端未实现、自动化测试覆盖不足和前端主包偏大。
- `012` 为 Spring Boot + Thymeleaf 一体化项目，无独立前端目录；当前可启动并完成登录、白板、脑图、双标签实时协作、版本历史、协作者管理、分享页与附件链路验证，已补齐核心编辑器和协作闭环，剩余风险主要是管理员模块、导出能力、自动化测试缺失以及业务异常仍采用 `HTTP 200 + code=500` 返回。
- `013` 为前后端分离项目，当前可启动并完成登录、分页、共享物品管理、收藏、提现、钱包充值和共享物品租借/归还链路验证；已修复前端构建失败、管理端后端权限缺失、分页插件缺失、用户列表密码泄露、共享物品管理空壳以及文档口径冲突，剩余风险主要是闲置租借与技能预约详情页仍为占位流程、地图与图表区域未接入真实组件以及自动化测试缺失。
- `014` 为 Spring Boot + 静态页面一体化项目，无独立前端目录；当前可启动并完成公开列表、登录、勋章、创建社团/圈子/话题、管理员审核、圈子加入、话题互动和活动报名/签到/评价链路验证，已修复公开页误鉴权、后台越权、分页插件缺失、密码字段泄露、勋章墙占位和创建入口缺失问题，剩余风险主要是通知/积分流水前端页缺失、管理员角色仍未细分、自动化测试缺失以及前端依赖 CDN。
- `015` 为 Spring Boot + 静态页面一体化项目，无独立前端目录；当前可启动并完成登录、权限、发帖、评论/回复、点赞、举报、私信、敏感词待审与实名认证审核链路验证，已修复后台越权、公开路由放行错误、分页插件缺失、敏感词高等级审核失效、静态页接口断链、JDK 17 编译问题、初始化密码哈希错误以及数据库配置口径错误，剩余风险主要是 PRD/README 中的图片上传、标签接口、收藏、通知、关注排序等能力仍未完整落地，以及控制台中文日志存在编码乱码。
- `016` 为前后端分离项目，当前可启动并完成登录、注册、资料修改、密码修改、订单广场筛选、发单、接单、上传取件/送达凭证、确认收货、评价、投诉、通知、后台统计、用户/订单/投诉/交易筛选与管理员强制取消链路验证；已修复后台越权、分页插件缺失、用户资料接口缺失、后台处理接口缺失、管理端返回字段不匹配、钱包消费流水缺失、默认密码哈希错误以及账号文档数据库口径错误，剩余风险主要是图片上传仍为 URL 录入、投诉成立后的已完成订单暂未做资金逆向冲正，以及前端主包体积偏大。
- `017` 为 Spring Boot + Thymeleaf 一体化项目，无独立前端目录；当前可启动并完成学生 / 商家 / 管理员登录、购物车、下单、商家接单 / 出餐、用户确认取餐和后台统计链路验证，已修复数据库密码口径错误、默认密码哈希错误、商家端 / 管理端登录后页面断链、购物车未接后端、订单核心链路缺失、JWT 未接鉴权以及权限错误仍返回 `HTTP 200` 的问题，剩余风险主要是用户资料、余额充值、评价、收藏、优惠券、搜索和更完整的后台管理仍未落地。
- `018` 为前后端分离项目，当前可启动并完成登录、公开详情、企业注册、岗位发布、简历创建、投递、企业查看投递、安排面试、学生 / 企业面试列表、简历权限校验、管理员审核与浏览器页面验证；已修复管理员模块缺口、分页插件缺失、企业注册未创建企业资料、简历与投递越权、企业面试列表缺失、公开详情路由误放行导致鉴权失效以及岗位类型展示兼容问题，剩余风险主要是前端构建仍有大包告警、Vite 开发环境缺少 `favicon.ico`，以及自动化测试覆盖仍偏薄。
- `019` 为前后端分离项目，当前可启动并完成默认账号登录、运动打卡、计划进度积分、健康档案 BMI、场馆预约 / 签到与约球活动人数链路验证；已修复默认密码哈希占位、假测试、BMI 公式错误、积分未累计、活动发起人参与记录缺失、预约状态断链与场馆排期缺失问题，剩余风险主要是排行榜的日 / 周 / 月周期统计仍未真正区分，以及前端主包仍有体积告警。
- `020` 为前后端分离项目，当前可启动并完成登录脱敏、资源上传奖励、问答回答奖励、私密笔记权限与公开列表泄露检查；已修复前端 alias 缺失导致的构建失败、首页统计缺口、密码哈希泄露、上传/回答积分规则缺失、问答防刷边界与私密笔记越权问题，剩余风险主要是 PRD 未定义具体奖励分值、前端仍有大包体积告警，以及自动化测试尚未覆盖完整集成链路。
- `021` 为前后端分离项目，当前可启动并完成注册、登录、商品发布/编辑、收藏、聊天、议价、接受议价自动生成订单、确认收货、双方评价、资料更新、图片上传与前端页面访问验证；已修复 Spring Boot 3.2 与旧 MyBatis-Plus starter 不兼容、订单模块编译错误、商品接口公开路由误放行、`product.condition` 关键字导致的 SQL 语法错误、资料更新空密码覆盖、前端页面缺失与登录态映射错误，并补齐了订单/聊天/收藏后端闭环与前端页面，剩余风险主要是简化管理后台仍未落地、前端主包体积偏大以及后端自动化测试仍为空。
- `022` 为前后端分离项目，当前可启动并完成学生端 / 管理端登录、资料修改、密码修改、自习室与座位浏览、预约创建 / 取消、预约分页、信用记录、仪表盘、用户管理、房间 / 座位 CRUD、系统配置与浏览器页面验证；已修复管理员初始化缺失、系统配置与管理接口缺口、预约详情字段缺失、未来预约误占当前座位、分页拦截器缺失、更新时间不刷新和前端代理错误改写 `/api` 前缀等问题，剩余风险主要是 README 中的 WebSocket 实时刷新、审计日志、数据导出、邮件 / 短信通知仍未落地，以及前端构建仍有大包与 Sass 旧 API 告警。
- `023` 为 Spring Boot + Thymeleaf 一体化项目，无独立前端目录；当前可启动并完成登录、注册校验、课程详情、评论、收藏、学习路径、分析看板、问答会话与导出链路验证，已修复 Boot 3 / JDK 17 兼容、H2 自举、JWT 返回结构、模板页面缺失、页面导航 fallback 与大批模板断链接口问题，剩余风险主要是 README / PRD 中的“AI 能力”描述明显强于当前实际实现，以及自动化测试覆盖仍偏薄。
- `024` 为前后端分离项目，当前可启动并完成注册、登录、宠物列表、宠物新增 / 编辑 / 删除与浏览器联调验证；已修复后端 `UserServiceImpl` 包声明缺失导致的启动失败、默认 MySQL 强依赖、前端缺少 `package.json` 脚本与关键依赖、Vite 入口与别名缺失，并补了前端注册页，剩余风险主要是 PRD 中领养申请、救助中心、爱心社区、公告和后台审核等模块仍未完整落地，当前实现更接近“简化宠物管理台”而非完整平台。
- `025` 为前后端分离项目，当前可启动并完成登录、用户注册、业主管理、物业缴费、报修状态流转、公告增删改、访客登记 / 状态更新、车位增删改与浏览器页面联调验证；已修复默认端口冲突、`mybatis` 配置层级错误导致 XML 未加载、默认 MySQL 凭据失效、前端请求地址与开发端口未项目化、访客时间格式不兼容、车位新增强制回写 `FREE`、业主删除缺少关联数据校验以及登录返回密码泄露问题，剩余风险主要是密码仍为明文存储、缺少真正的接口鉴权 / 角色隔离、后端自动化测试为空且异常仍采用 `HTTP 200 + code=500` 返回。
- `026` 为前后端分离项目，当前可启动并完成登录、画师列表、公开需求、订单详情、尾款支付与画师审核链路验证；已修复后端工程骨架缺依赖、默认 MySQL 强依赖、H2 自举缺失、订单状态字段长度不足、异常返回 `msg=null`、用户密码泄露、画师审核角色未回写、尾款收益入错账户、前端默认请求地址与开发端口未项目化等问题，剩余风险主要是 PRD 中报价招标、订单聊天、钱包提现、搜索推荐、管理后台、JWT/加密与更完整权限体系仍未落地。
- `027` 为前后端分离项目，当前可启动并完成登录、门店、理发师、服务列表、余额充值、订单支付、订单评价、预约创建与取消验证；已修复 Boot 3 / MyBatis-Plus 兼容、默认 MySQL 强依赖、端口冲突、分页插件缺失、自动时间填充缺失与默认密码哈希错误问题，剩余风险主要是订单自动生成、积分/余额流水、通知与前端预约详情展示仍未完整落地。
- `028` 为前后端分离项目，当前可启动并完成登录、注册、停车点列表、租车/还车、钱包充值、故障上报和后台统计验证；已修复 Boot 3 / MyBatis-Plus 兼容、默认 MySQL 凭据失效导致的登录 500、默认环境无法直接启动、分页数据库类型不匹配以及启动提示端口固定问题，剩余风险主要是 PRD 中订单申诉、临时锁车、真实地图找车、图片上传闭环与信用记录/等级能力仍未完整落地。
- `029` 为前后端分离项目，当前可启动并完成登录、公开食谱列表、用户资料、管理员接口与前端首页访问验证；已修复 JDK 17 泛型编译错误、JJWT 0.12.3 兼容、Boot 3 / MyBatis-Plus starter 冲突、默认 MySQL 强依赖、H2 自举脚本、JWT 密钥长度不足、密码哈希泄露、前端缺页和前后端接口路径不一致问题，剩余风险主要是项目名称口径冲突、管理端前端仍未独立落地以及前端大包告警。
- `030` 为前后端分离项目，当前可启动并完成患者/管理员登录、医生审核权限、健康数据新增/查询、健康知识库公开读取与管理端维护、健康评估自动生成、预警列表查看与已读处理、管理员统计总览以及医生统计看板等前后端页面验证；已修复默认 PostgreSQL 强依赖、无效 `schema.sql`、H2 保留字建表失败、默认账号缺失、密码哈希泄露、患者/医生/管理员接口越权、健康知识模块缺失、评估预警模块缺失、统计分析占位以及前端角色路由缺失问题，剩余风险主要是深度风险建模、自定义报表和系统管理等 PRD 模块仍有明显缺口。
- `031` 为 Spring Boot + 静态页面项目；当前可启动并完成登录、首页赛事渲染、真实选座、下单、余额支付与电子门票查看验证；已修复默认 MySQL / Redis / jOOQ codegen 强依赖、JWT 未真正鉴权、密码哈希泄露、未来赛事缺失、选座页硬编码、订单金额固定为 0、支付后不出票、取消不释放库存以及浏览器控制台 favicon 404 等问题，剩余风险主要是支付超时释放、真正二维码图片与更完整的后台/通知能力仍未落地。
- `032` 为前后端分离项目；当前可启动并完成管理员登录、JWT claims、老人建档、健康测量、健康评估、预约、随访、统计与前端页面访问验证；已修复默认 MySQL 强依赖、JWT 短密钥与解析错误、Spring Data JDBC 列映射问题、默认账号缺失和密码字段泄露问题，剩余风险主要是通知触达能力、权限模型颗粒度和前端大包体积仍有优化空间。
- `033` 为前后端分离项目；当前可启动并完成管理员登录、套餐、客户、预约、订单、统计与前端代理登录验证；已修复默认 MySQL 强依赖、默认账号口径不清、异常吞错和自动化测试缺失问题，剩余风险主要是客户端预约/选片/支付、真实通知与日志备份模块仍未完整落地，以及前端主包体积偏大。
- `034` 为 Spring Boot 后端 + 单文件静态前端项目；当前可启动并完成管理员登录、SKU、库存、入库、出库、调拨、盘点与前端默认 API 联调验证；已修复 Spring Security 循环依赖导致的默认启动失败，并补齐启动/登录烟雾测试，剩余风险主要是明文密码、硬编码 JWT 密钥、细粒度权限与报表/通知/导入导出模块仍未完整落地。
- `035` 为前后端分离项目；当前可启动并完成管理员登录、农户地块、机手设备、预约创建、管理员指派、机手开工/完工与前端代理登录验证；已修复 Boot 3 / MyBatis-Plus starter 兼容、默认 MySQL 强依赖、JWT 弱密钥、初始化口令口径失真、密码字段泄露、角色越权、预约状态枚举落库兼容与前端代理目标不可覆盖问题，剩余风险主要是派工记录/作业记录/评价模块仍未形成完整闭环，以及前端功能页面明显不足。
- `036` 为前后端分离项目；当前可启动并完成用户登录、项目列表/详情、登录捐赠、个人中心记录与组织创建项目验证；已修复源码 HTML 实体转义、默认 PostgreSQL 强依赖、JWT 当前用户识别缺失、请求头默认冒充、密码字段泄露、普通用户创建项目越权、捐赠金额校验缺失、JPA 懒加载序列化失败和前端日期/占位资源问题，剩余风险主要是真实支付、管理端状态流转页面、项目进度管理入口和前端主包体积仍需后续优化。
- `037` 为 Spring Boot 后端 + 原生微信小程序项目；当前可启动并完成模拟微信登录、当前用户、课程分页/分类/热门/详情接口验证，小程序声明页面已补齐可编译骨架；已修复默认 MySQL 强依赖、Security context-path 放行错误、真实微信配置缺失时无法演示、`/auth/userinfo` 未登录响应不一致、课程公开接口缺失、分类分页总数错误、小程序缺页和缺失图标/图片引用问题，剩余风险主要是 README/PRD 宣称的问答、文章、代码、打卡、`80+ API` 等完整业务能力仍未落地，当前多页面仍为占位壳，真实微信登录需正式 `appId` / `appSecret`。
- `038` 为前后端分离项目；当前可启动并完成管理员登录、食物分页、营养汇总、健康数据、食谱详情与前端代理登录验证；已修复 Boot 3 / MyBatis-Plus starter 兼容、默认 MySQL 强依赖、Spring Security 默认拦截、登录占位实现、初始化密码哈希损坏和核心接口不足问题，剩余风险主要是前端多个业务页仍为静态/占位内容、token 尚非完整 JWT 鉴权体系、Redis 能力未覆盖以及前端主包体积偏大。
- `039` 为前后端分离项目；当前可启动并完成管理员登录、公开分类、民歌列表、后台用户列表和前端代理登录验证；已修复默认 PostgreSQL 强依赖、Spring Data JDBC 字段映射与 SQL 小写字段不匹配、H2 初始化中文编码、默认 Spring Security 表单/Basic 干扰、初始化管理员密码哈希不可用以及端口/代理未项目化问题，剩余风险主要是后台统计面板仍偏简化、审核理由字段未落库、真实媒体上传存储策略较基础以及前端主包体积偏大。
- `040` 为前后端分离项目；当前可启动并完成管理员登录、违停类型、举报提交、举报审核和前端代理登录验证；已修复 QueryDSL 重复生成 Q 类、启动类 `.java` 误传、默认 MySQL 强依赖、初始化密码哈希不可用、Hibernate 6 浮点字段 `scale` 建模失败以及演示数据缺失问题，剩余风险主要是申诉/处理/公告等扩展模块仍偏基础、接口鉴权仍依赖简化 `userId` 请求头，以及前端主包体积偏大。
- `041` 为前后端分离项目；当前可启动并完成管理员登录、心理量表、题目、咨询师、文章、测评提交、预约创建和前端代理登录验证；已修复 Boot 3 与旧 MyBatis-Plus starter 不兼容、默认 MySQL/Redis 演示依赖、H2 自举缺失、默认密码哈希不可用、登录返回密码哈希泄露、MyBatis-Plus 时间自动填充失效以及前端依赖锁缺失问题，剩余风险主要是 WebSocket 实时咨询、Redis 缓存、后台管理、评价/通知/心理档案等 PRD 扩展能力仍未完整落地。
- `042` 为前后端分离项目；当前可启动并完成管理员/租客登录、租客注册、房东发布房源、公开房源列表/详情、收藏房源、预约看房和前端代理登录验证；已修复默认 MySQL 强依赖、H2 自举缺失、默认密码哈希不可用、后端/前端端口未项目化、前端代理目标错误以及后端自动化测试缺失问题，剩余风险主要是上传接口鉴权偏宽、JWT 密钥配置仍偏演示化、真实支付/电子签章/实时通知等增强能力尚未完整落地，以及前端主包体积偏大。
- `043` 为前后端分离项目；当前可启动并完成普通用户登录、公开服务商列表、服务项目、宠物新增、预约创建、预约分页和前端代理登录验证；已修复 Boot 3 / MyBatis-Plus starter 兼容、默认 MySQL/Redis 强依赖、H2 自举缺失、登录角色字段类型不一致、预约金额/状态字段断链、服务商公开路由误放行、JWT 当前用户识别缺失、前端默认请求地址错误以及前端构建类型错误问题，剩余风险主要是真实支付、服务商后台、管理员统计、寄养过程反馈和前端主包体积仍需后续优化。
- `044` 为前后端分离项目；当前可启动并完成游客登录、民宿列表/详情、收藏、创建预订、我的订单、房东民宿列表、房东确认订单和前端代理登录验证；已修复默认 MySQL/Redis 强依赖、H2 自举缺失、JWT principal 与控制器参数不匹配、分页方言固定 MySQL、前端代理目标错误、vue-tsc 与 TypeScript 版本不兼容以及前端构建无用变量问题，剩余风险主要是真实支付、日期库存、管理员审核/结算和前端主包体积仍需后续优化。
- `045` 为前后端分离项目；当前可启动并完成管理员登录、公告列表、管理员统计、老人列表、护工健康记录新增/查询、家属账单查询/缴费、探访申请/审批和前端代理登录验证；已修复默认 MySQL/Redis 强依赖、H2 自举缺失、默认端口未项目化、分页方言固定 MySQL、H2 控制台未放行、前端代理目标错误以及后端自动化测试缺失问题，剩余风险主要是真实支付、护理排班提醒、退住结算、资源上传和前端主包体积仍需后续优化。
- `046` 为前后端分离项目；当前可启动并完成居民登录、公告/分类列表、居民预约、回收员待接单列表、接单、完成回收、积分增加、积分商品兑换、管理员发放兑换和前端代理登录验证；已修复默认 MySQL/Redis 强依赖、H2 自举缺失、默认端口未项目化、分页方言固定 MySQL、H2 控制台未放行、JWT 角色语义沿用上一项目、前端项目名错误、前端代理目标错误以及后端自动化测试缺失问题，剩余风险主要是真实地图派单、图片凭证、物流核销、回收员排班和前端主包体积仍需后续优化。
- `047` 为前后端分离项目；当前可启动并完成管理员/玩家/店家/作者四角色登录、公告、管理员统计、剧本分类、剧本列表、店铺房间、玩家预约、店家确认完成、玩家评价收藏、作者创作剧本、管理员审核上架和前端代理登录验证；已修复默认 MySQL/Redis 强依赖、H2 自举缺失、默认端口未项目化、分页方言固定 MySQL、H2 控制台未放行、JWT 角色语义沿用上一项目、前端项目名错误、前端代理目标错误以及后端自动化测试缺失问题，剩余风险主要是复杂创作协作、支付退款、预约冲突锁单、文件上传和前端主包体积仍需后续优化。
- `048` 为前后端分离项目；当前可启动并完成管理员/游客/讲解员/研究员四角色登录、公告、管理员统计、文物分类/朝代/展厅、文物列表/详情/图片/点赞、展览、研究成果、参观预约、管理员确认完成、收藏、讲解预约、讲解员确认完成、研究员投稿、管理员审核发布和前端代理登录验证；已修复默认配置缩进错误、默认 MySQL/Redis 强依赖、H2 自举缺失、默认端口未项目化、分页方言固定 MySQL、H2 控制台未放行、JWT 角色语义沿用上一项目、前端项目名错误、前端代理目标错误以及后端自动化测试缺失问题，剩余风险主要是 3D/音频/图片文件存储、预约容量冲突、评论闭环、研究附件版本管理和前端主包体积仍需后续优化。
- `049` 为 Spring Boot 后端 + 原生微信小程序项目；当前可启动并完成学生登录、公开科目/课程/题库、个人统计、答题与错题、学习计划、笔记、收藏、社区发帖评论、每日打卡和管理员仪表盘验证；已修复默认 MySQL/Redis 强依赖、H2 自举缺失、默认端口未项目化、JWT token/过滤器不一致、请求上下文缺少 `userId`、登录密码泄露、分页方言固定 MySQL、小程序接口端口错误、图片资源断链以及前后端字段不一致问题，剩余风险主要是明文密码、课程购买/视频播放/支付、真实微信登录和管理端 UI 仍需后续优化。
- `050` 为 Spring Boot 后端 + 原生微信小程序项目；当前可启动并完成公告、当前学期、课程分页、管理员/教师/学生登录、教师课程、学生课程、教师创建签到任务、学生签到、签到统计、请假列表和消息未读数验证；已修复默认 MySQL/Redis 强依赖、H2 自举缺失、默认端口未项目化、artifact/启动类命名串项、根目录剧本杀 SQL 串项、JWT token/过滤器不一致、角色语义沿用其他项目、登录密码泄露、分页方言固定 MySQL、签到记录缺少 `courseId`、小程序接口端口错误以及 tabBar 图标资源断链问题，剩余风险主要是真实微信登录、二维码/定位签到端侧验证、管理员端 UI 和生产防作弊策略仍需后续优化。
- `051` 为 Spring Boot 后端 + Vue 管理端 + 原生微信小程序项目；当前可启动并完成分类、文章、资讯、管理员登录统计、模拟微信登录、普通用户管理端拒绝、每日答题、学习记录、收藏和问答发布验证；已修复默认 MySQL/Redis/微信接口强依赖、H2 自举缺失、默认端口未项目化、JJWT 缺 JAXB、分页方言固定 MySQL、公开路由误放行、普通用户越权访问管理端、小程序 JSON 请求体绑定失败、管理端/小程序接口端口错误、QA 页面缺失以及本地静态图片资源断链问题，剩余风险主要是真实微信登录、Redis 生产级缓存切换、图片上传、内容审核、推送通知和密码 BCrypt 改造仍需后续优化。
- `052` 为 Spring Boot 后端 + Vue 用户端 + Vue 管理端项目；当前可启动并完成课程、章节、管理员登录统计、普通用户管理端拒绝、用户注册、学习进度、收藏和评论验证；已修复默认 MySQL/8080 强依赖、H2 自举缺失、MySQL 驱动旧坐标、分页方言固定 MySQL、普通用户越权访问管理端、用户密码字段泄露风险、学习记录断链、收藏/评论孤儿数据、前端代理端口错误以及本地静态图片资源断链问题，剩余风险主要是真实视频资源、课程购买支付、教师后台、Redis 生产级缓存和密码 BCrypt 改造仍需后续优化。
- `053` 为 Spring Boot 后端 + Vue 管理端项目；当前可启动并完成管理员/信息员登录、统计权限、灾情上报、物资入库、调度创建审批、任务创建流转、公告列表和前端代理接口验证；已修复默认 MySQL/8080 强依赖、H2 自举缺失、分页方言固定 MySQL、登录密码泄露风险、普通用户越权访问统计/用户管理、前端 token 保存错误、请求头缺少 Bearer、前端 API 路径与后端不一致、灾情字段口径不一致以及任务状态接口缺失问题，剩余风险主要是图片上传、地图定位、调度路线、复杂角色权限和密码 BCrypt 改造仍需后续优化。
- `054` 为 Spring Boot 后端 + Vue 管理端项目；当前可启动并完成管理员/农户登录、用户改密、统计权限、作物、知识、生产计划/任务、病虫害预警、农资入出库、技术咨询采纳、专家预约评价、公告管理和前端代理接口验证；已修复后端编译失败、默认 MySQL/8080 强依赖、H2 自举缺失、分页方言固定 MySQL、用户密码响应泄露风险、修改密码假成功、普通用户越权访问统计/用户管理、前端代理端口错误、角色枚举大小写不一致和登录提示密码错误问题，剩余风险主要是明文密码、图片上传、AI 病虫害识别、消息通知和前端主包体积仍需后续优化。
- `055` 为 Spring Boot 后端 + Vue 管理端项目；当前可启动并完成管理员/员工登录、未登录拦截、员工越权拦截、统计、部门、公告、请假申请与审批、考勤打卡、会议室和工作日志接口验证；已修复后端编译失败、默认 MySQL/8080 强依赖、H2 自举缺失、JJWT/JDK 17 兼容风险、分页方言固定 MySQL、用户密码响应泄露风险、JWT Bearer 兼容缺失、基础管理员权限缺失以及前后端考勤/会议室/工作日志/请假审批路径不一致问题，剩余风险主要是明文密码、复杂岗位审批权限、真实文件存储、Redis 扩展能力和前端主包体积仍需后续优化。
- `056` 为 Spring Boot 后端 + Vue 管理端 + uni-app 小程序项目；当前可启动并完成公开分类/竞赛/公告、管理员/评委/参赛者登录、未登录拦截、参赛者越权拦截、管理员统计、作品投稿、作品审核、评委待评分与提交评分接口验证；已修复默认 MySQL/8080 强依赖、H2 自举缺失、分页方言固定 MySQL、游客分类接口被拦截、用户密码响应泄露风险、基础管理员/评委权限缺失、评分标准保存强转异常、管理端/小程序端口错误以及小程序缺失静态资源引用问题，剩余风险主要是 MD5 密码、真实微信登录、上传存储、细粒度权限和前端主包体积仍需后续优化。
- `057` 为 Spring Boot 后端 + Vue 管理端项目；当前可启动并完成超级管理员/普通管理员登录、未登录拦截、普通管理员访问管理员管理越权拦截、统计、院系、专业、招生计划、考生新增审核、成绩录入、报名审核、录取确认和公告分页接口验证；已修复默认 MySQL/8080 强依赖、H2 自举缺失、分页方言固定 MySQL、JWT Bearer 兼容缺失、未登录/越权 HTTP 状态不准确、Redis 不可用阻断演示登录、密码响应泄露风险以及前端代理端口错误问题，剩余风险主要是明文密码、Excel 批量导入、自动录取规则、录取通知书生成和前端主包体积仍需后续优化。
- `058` 为 Spring Boot 后端 + Vue 前端项目；当前可启动并完成游客分类/商品、管理员/配送员/普通用户登录、未登录拦截、用户越权拦截、管理员统计与管理接口、普通用户地址/订阅/订单/投诉/通知、配送员任务完成和管理员投诉回复接口验证；已修复默认 MySQL/Redis/8080 强依赖、H2 自举缺失、分页方言固定 MySQL、JWT Bearer 兼容缺失、未登录/越权 HTTP 状态不准确、Redis 不可用阻断演示登录、用户密码响应泄露风险以及前端代理端口错误问题，并将 MySQL profile 调整为本机 `root / 1234`，剩余风险主要是明文密码、支付闭环、订阅自动生成订单、真实图片上传/消息推送和前端主包体积仍需后续优化。
- `059` 为 Spring Boot 后端 + Vue 前端项目；当前可启动并完成管理员/生产经理/设备操作员/质检员登录、未登录拦截、操作员访问用户管理越权拦截、管理员看板与管理接口、操作员设备/传感器/告警/维保、生产经理物料入出库与工单状态、质检员质量检测接口验证；已修复默认 MySQL/Redis/8080 强依赖、H2 自举缺失、分页方言固定 MySQL、JWT Bearer 兼容缺失、未登录/越权 HTTP 状态不准确、Redis 不可用阻断演示登录/退出、用户密码响应泄露风险以及前端代理端口错误问题，并将 MySQL profile 调整为本机 `root / 1234`，剩余风险主要是明文密码、真实物联网网关、告警规则引擎、排产优化、库存预警推送和前端主包体积仍需后续优化。
- `060` 为 Spring Boot 后端 + Vue 前端项目；当前可启动并完成管理员/普通用户登录、未登录拦截、普通用户访问管理接口越权拦截、管理员看板与管理接口、普通用户电影浏览、未来排片、收藏、下单、本人订单支付和评论发布验证；已修复 Maven 依赖不可解析、默认 MySQL/Redis/8080 强依赖、H2 自举缺失、JWT Bearer 兼容缺失、未登录/越权 HTTP 状态不准确、用户密码响应泄露风险、订单归属校验缺失、演示排片日期过期以及前端代理端口错误问题，并将 MySQL profile 调整为本机 `root / 1234`，剩余风险主要是明文密码、可视化选座/锁座、真实支付、评论审核闭环和前端主包体积仍需后续优化。
- `061` 为 Spring Boot 后端 + Vue 前端项目；当前可启动并完成管理员/普通用户登录、未登录拦截、普通用户访问管理接口越权拦截、管理员看板与管理接口、普通用户商品浏览、收藏、下单支付、卖家发货、买家确认完成、评价和申诉验证；已修复默认 MySQL/Redis/8080 强依赖、H2 自举缺失、MySQL 驱动旧坐标、分页方言固定 MySQL、JWT Bearer 兼容缺失、未登录/越权 HTTP 状态不准确、Redis 不可用阻断演示登录、用户密码响应泄露风险以及前端代理端口错误问题，并将 MySQL profile 调整为本机 `root / 1234`，剩余风险主要是明文密码、真实支付、自动发货/验号、对象存储和前端主包体积仍需后续优化。
- `062` 为 Spring Boot 后端 + Vue 前端项目；当前可启动并完成管理员/风控分析员/商户用户登录、未登录拦截、普通用户访问用户管理越权拦截、管理员看板、黑名单、规则、风险事件上报、自动风险评分与预警、预警指派处理、案件、申诉、公告、日志和登出失效验证；已修复默认 MySQL/Redis/8080 强依赖、H2 自举缺失、SQL 初始化中文编码、MySQL 驱动旧坐标、JWT Bearer 兼容缺失、未登录/越权 HTTP 状态不准确、Redis 不可用阻断演示登录、用户密码响应泄露风险以及前端代理端口错误问题，并将 MySQL profile 调整为本机 `root / 1234`，剩余风险主要是明文密码、复杂反欺诈模型、实时流式风控、通知报表和前端主包体积仍需后续优化。
- `063` 为 Spring Boot 后端 + Vue 前端项目；当前可启动并完成管理员/业务员登录、未登录拦截、业务员访问用户管理越权拦截、管理员用户/公告管理、业务员看板、供应商、客户、分类、商品、采购审核入库、销售审核出库、库存流水和登出失效验证；已修复默认 MySQL/Redis/8080 强依赖、H2 自举缺失、MySQL 驱动旧坐标、PageHelper 固定 MySQL 方言、JWT Bearer 兼容缺失、未登录/越权 HTTP 状态不准确、Redis 不可用阻断演示登录、用户密码响应泄露风险以及前端代理端口错误问题，并将 MySQL profile 调整为本机 `root / 1234`，剩余风险主要是明文密码、多明细单据、并发库存扣减、盘点审批流和前端主包体积仍需后续优化。
- `064` 为 Spring Boot 后端 + Vue 前端项目；当前可启动并完成管理员/普通用户登录、未登录拦截、普通用户访问用户管理越权拦截、分类、食材、经方、药膳方案、公告、服务预约、管理员确认、体质记录、管理员回复、收藏、看板和登出失效验证；已修复默认 MySQL/Redis/8080 强依赖、H2 自举缺失、MySQL 驱动旧坐标、JWT Bearer 兼容缺失、未登录/越权 HTTP 状态不准确、Redis 不可用阻断演示登录、用户密码响应泄露风险、用户分页空参数 NPE、H2 趋势统计别名冲突以及前端代理端口错误问题，并将 MySQL profile 调整为本机 `root / 1234`，剩余风险主要是明文密码、真实体质测评规则、服务排班/支付/通知、收藏对象强约束和前端主包体积仍需后续优化。
- `065` 为 Spring Boot 后端 + Vue 前端项目；当前可启动并完成管理员/物业人员/业主登录、未登录拦截、业主访问用户管理越权拦截、楼栋、房屋、业主列表、车位、公告、物业费创建与缴费、报修接单完成、投诉回复、访客审批、看板和登出失效验证；已修复默认 MySQL/Redis/8080 强依赖、H2 自举缺失、MySQL 驱动旧坐标、PageHelper 固定 MySQL 方言、JWT Bearer 兼容缺失、未登录/越权 HTTP 状态不准确、Redis 不可用阻断演示登录、用户密码响应泄露风险、H2 趋势统计别名冲突以及前端代理端口错误问题，并将 MySQL profile 调整为本机 `root / 1234`，剩余风险主要是明文密码、真实支付对账、维修派工/图片/评价、访客门禁联动和前端主包体积仍需后续优化。
- `066` 为 Spring Boot 后端 + Vue 前端项目；当前可启动并完成管理员/普通用户登录、未登录拦截、普通用户访问用户管理越权拦截、公开文章、文章详情、分类标签、公告、用户发文、管理员置顶、评论提交审核、看板和登出失效验证；已修复默认 MySQL/Redis/8080 强依赖、H2 自举缺失、MySQL 驱动旧坐标、MyBatis-Plus 分页方言兼容、JWT Bearer 兼容缺失、未登录/越权 HTTP 状态不准确、Redis 不可用阻断演示登录、用户密码响应泄露风险、H2 趋势统计别名冲突以及前端代理端口错误问题，并将 MySQL profile 调整为本机 `root / 1234`，剩余风险主要是明文密码、富文本上传/草稿版本、评论风控通知、看板深度分析和前端主包体积仍需后续优化。
- `067` 为 Spring Boot 后端 + Vue 前端项目；当前可启动并完成管理员/教师/学生登录、未登录拦截、学生访问用户管理越权拦截、教师发布课程/活动/岗位/公告、学生选课/报名/投递/发布失物招领、教师录入选课成绩/活动签到/岗位审核、看板和登出失效验证；已修复默认 MySQL/Redis/8080 强依赖、H2 自举缺失、MySQL 驱动旧坐标、PageHelper 固定 MySQL 方言、JWT Bearer 兼容缺失、未登录/越权 HTTP 状态不准确、Redis 不可用阻断演示登录、用户密码响应泄露风险、H2 趋势统计别名冲突以及前端代理端口错误问题，并将 MySQL profile 调整为本机 `root / 1234`，剩余风险主要是明文密码、课表冲突/容量并发、二维码签到、真实简历/图片上传和前端主包体积仍需后续优化。
- `068` 为 Spring Boot 后端 + Vue 前端项目；当前可启动并完成管理员/普通用户登录、未登录拦截、普通用户访问用户管理越权拦截、景点浏览、出行人管理、收藏、下单支付完成、评价回复、投诉处理、订单导出、看板和登出失效验证；已修复默认 MySQL/Redis/8080 强依赖、H2 自举缺失、MySQL 驱动旧坐标、MyBatis-Plus 分页方言固定 MySQL、JWT Bearer 兼容缺失、未登录/越权 HTTP 状态不准确、Redis 不可用阻断演示登录、用户密码响应泄露风险以及前端代理端口错误问题，并将 MySQL profile 调整为本机 `root / 1234`，剩余风险主要是明文密码、真实支付退款、景点容量/库存并发、图片附件存储和前端主包体积仍需后续优化。
- `069` 为 Spring Boot 后端 + Vue 前端项目；当前可启动并完成管理员/教师/学生登录、未登录拦截、学生访问用户管理越权拦截、管理员基础档案与看板接口、学生提交评教、教师查看记录并发起申诉、管理员处理申诉、公告发布和登出失效验证；已修复非法包名 `com.teacher.new`、068 周边游源码串项、默认 MySQL/Redis/8080 强依赖、H2 自举缺失、MySQL 驱动旧坐标、MyBatis-Plus 分页方言固定 MySQL、JWT Bearer 兼容缺失、未登录/越权 HTTP 状态不准确、Redis 不可用阻断演示登录、用户密码响应泄露风险以及前端代理端口错误问题，并将 MySQL profile 调整为本机 `root / 1234`，剩余风险主要是明文密码、评教权重模型、消息通知、批量导入导出和前端主包体积仍需后续优化。
- `070` 为 Spring Boot 后端 + Vue 前端项目；当前可启动并完成管理员/普通用户登录、未登录拦截、普通用户访问用户管理越权拦截、普通用户访问他人订单越权拦截、管理员管理接口、座位查询、锁座、下单、优惠券抵扣、余额支付、出票、核销、订单完成、评论提交、评论审核和登出失效验证；已修复默认 MySQL/Redis/8080 强依赖、H2 自举缺失、MySQL 驱动旧坐标、MyBatis-Plus 分页方言固定 MySQL、JWT Bearer 兼容缺失、未登录/越权 HTTP 状态不准确、Redis 不可用阻断演示登录与座位锁、用户密码响应泄露风险以及前端代理端口错误问题，并将 MySQL profile 调整为本机 `root / 1234`，剩余风险主要是明文密码、真实支付退款、分布式座位锁、复杂票务对账和前端主包体积仍需后续优化。
