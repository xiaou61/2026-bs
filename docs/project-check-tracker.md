# 毕设项目巡检总台账

## 统计口径

- 总项目数：95
- 检查单位：按项目编号统计，例如 `001`
- 当前目标：逐个项目核对 PRD、代码完整性、JDK 17 兼容性、测试结果、启动结果

## 实时统计

| 指标 | 数量 |
| --- | ---: |
| 总项目数 | 95 |
| 已开始检查 | 30 |
| 已完成检查 | 30 |
| 已通过测试并可启动 | 30 |
| 检查中 | 0 |
| 待修复 | 0 |
| 阻塞 | 0 |
| 未开始 | 65 |

## 项目进度

| 项目 | 名称 | 状态 | 文档完整性 | JDK17兼容性 | 测试结果 | 启动结果 | 最近更新 |
| --- | --- | --- | --- | --- | --- | --- | --- |
| 001 | 校园事务管理系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`） | 通过（前端 `3000`，后端 `18080`；默认 `8080` 被外部进程占用） | 2026-04-04 |
| 002 | 在线选课与成绩管理系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`） | 通过（后端登录接口 `200`，前端页面响应 `200`） | 2026-04-04 |
| 003 | 助农精准扶贫平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`） | 通过（首页 `200`，登录接口 `200`） | 2026-04-04 |
| 004 | 实时聊天与通知系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`） | 通过（后端登录接口 `200`，前端页面响应 `200`） | 2026-04-04 |
| 005 | 在线问卷调查与数据分析系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`） | 通过（首页 `200`、登录页 `200`、登录接口 `200`） | 2026-04-04 |
| 006 | 校园失物招领系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`） | 通过（登录页 `200`、登录接口 `200`、登录态首页 `200`） | 2026-04-04 |
| 007 | 志愿活动管理与积分激励平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`） | 通过（后端登录接口 `200`，前端页面响应 `200`） | 2026-04-04 |
| 008 | 智能菜谱推荐系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`） | 通过（登录接口 `200`，静态页面响应 `200`，受保护接口未登录返回 `401`） | 2026-04-04 |
| 009 | 校园快递代收管理系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`） | 通过（前后端成功启动，登录/分页/权限/代理链路复测通过） | 2026-04-04 |
| 010 | 图书馆座位预约系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`） | 通过（后端成功启动，页面/登录/预约/权限/统计链路复测通过） | 2026-04-04 |
| 011 | 校园短视频创作与分享平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`） | 通过（后端 `8011`、前端 `5173`；登录、草稿、统计、相关推荐、积分商城与浏览器页面复测通过） | 2026-04-05 |
| 012 | 在线协作白板与笔记系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`，接口抽测与 Playwright 浏览器复测） | 通过（后端 `8012`；登录、白板、脑图、实时协作、分享与附件链路复测通过） | 2026-04-05 |
| 013 | 校园共享经济平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`） | 通过（后端 `8013`、前端 `5013`；登录、分页、共享物品管理、收藏、提现、钱包与共享租借链路复测通过） | 2026-04-05 |
| 014 | 校园社团与兴趣圈层平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`，接口抽测与静态页面访问复测） | 通过（后端 `8080`；公开列表、勋章、创建社团/圈子/话题、管理员审核、活动报名签到评价链路复测通过） | 2026-04-05 |
| 015 | 校园表白墙与匿名社交平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`，接口抽测与静态页面访问复测） | 通过（后端 `8080`；登录、权限、发帖、评论/回复、点赞、举报、私信、敏感词待审与实名认证审核链路复测通过） | 2026-04-05 |
| 016 | 校园快递代领服务平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`） | 通过（后端 `8080`、前端 `5173`；登录、注册、资料修改、密码修改、发单、接单、上传凭证、确认收货、评价、投诉、通知与后台管理链路复测通过） | 2026-04-05 |
| 017 | 高校自助点餐系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`） | 通过（后端 `8080`；登录、加购、下单、商家接单/出餐、用户确认取餐与后台统计链路复测通过） | 2026-04-06 |
| 018 | 校园实习招聘与求职平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`） | 通过（后端 `8080`、前端 `5173`；登录、公开详情、注册、岗位发布、简历、投递、面试、管理员后台与浏览器页面复测通过） | 2026-04-06 |
| 019 | 校园运动健康管理平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`、接口抽测） | 通过（后端 `8080`、前端 `5173`；默认账号登录、运动打卡、计划进度、健康档案、场馆预约与约球活动链路复测通过） | 2026-04-06 |
| 020 | 校园学习资源共享平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`、接口抽测） | 通过（后端 `8020`、前端 `5020`；登录脱敏、上传奖励、回答奖励、私密笔记权限链路复测通过） | 2026-04-06 |
| 021 | 校园二手交易平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`、接口抽测） | 通过（后端 `8080`、前端 `3021`；注册、发布、收藏、议价、订单、评价、资料更新与图片上传链路复测通过） | 2026-04-08 |
| 022 | 校园自习室座位预约系统 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`、接口抽测与 Playwright 浏览器复测） | 通过（后端 `8022`、前端 `3022`；学生端、管理端、分页、代理、自习室/座位/预约/信用/系统配置链路复测通过） | 2026-04-08 |
| 023 | AI智能学习助手与个性化教育平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、接口抽测） | 通过（后端 `8023`；登录、课程详情、评论、学习路径、分析面板、问答会话与导出链路复测通过） | 2026-04-08 |
| 024 | 流浪动物领养与救助平台 | 已完成 | 明显缺口 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`） | 通过（后端 `8024`、前端 `3024`；注册、登录、宠物列表与宠物 CRUD 链路复测通过） | 2026-04-10 |
| 025 | 智慧社区综合服务管理平台 | 已完成 | 基本通过 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`、接口抽测与浏览器复测） | 通过（后端 `8025`、前端 `3025`；登录、业主、缴费、报修、公告、访客、车位链路复测通过） | 2026-04-10 |
| 026 | 画师接稿平台 | 已完成 | 明显缺口 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`、新增冒烟测试） | 通过（后端 `8026`、前端 `3026`；登录脱敏、已审核画师、开放需求、尾款结算、画师审核角色回写链路复测通过） | 2026-04-14 |
| 027 | 线上理发预约系统 | 已完成 | 明显缺口 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`、接口抽测） | 通过（后端 `8027`、前端 `3027`；登录、门店、充值、支付、评价、预约/取消链路复测通过） | 2026-04-14 |
| 028 | 校园共享自行车租赁系统 | 已完成 | 明显缺口 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`、接口抽测） | 通过（后端 `8028`、前端 `3028`；登录、注册、停车点、租还车、钱包充值、故障上报与后台统计链路复测通过） | 2026-04-14 |
| 029 | 中药食疗推荐交流平台 | 已完成 | 明显缺口 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`、新增冒烟测试） | 通过（后端 `8080`、前端 `5173`；默认 H2 自举，登录、公开列表、管理员接口与前端首页访问复测通过） | 2026-04-15 |
| 030 | 居民智能健康管理平台 | 已完成 | 明显缺口 | 通过 | 通过（后端 `mvn test`、前端 `npm run build`） | 通过（后端 `8080`、前端 `5173`；默认 H2 自举，患者/管理员登录、医生审核权限与健康数据链路复测通过） | 2026-04-17 |

## 状态说明

- `未开始`：尚未进入巡检
- `检查中`：已建档，正在核对文档、代码、测试或启动链路
- `待修复`：已找到问题，等待修复后复验
- `阻塞`：受外部依赖影响，当前无法继续推进
- `已完成`：检查结论已写入单项目报告
- `已通过测试并可启动`：在 `已完成` 基础上，前后端测试与启动验证均通过

## 备注

- `001` 的默认后端端口 `8080` 在当前机器上被其他 Java 进程占用，但项目本身在切换到 `18080` 后可正常启动并完成登录接口验证。
- `002` 在巡检过程中出现过端口占用复现，根因是验证脚本留下了 `spring-boot:run` / Vite 子进程，不是项目代码自身启动失败。
- `003` 为 Spring Boot + Thymeleaf 一体化项目，无单独前端目录；当前核心页面和登录链路已验证通过。
- `004` 在本机 Redis 未启动的情况下仍可完成基础启动与登录验证，但实时聊天、在线状态和通知的完整联调能力仍需后续补充验证。
- `005` 为 Spring Boot + Thymeleaf 一体化项目，无独立前端目录；当前可启动并完成登录验证，但 PRD 中“前后端分离”和部分扩展功能描述与现有实现不完全一致。
- `006` 为 Spring Boot + Thymeleaf 一体化项目，无独立前端目录；当前可启动并完成管理员登录验证，但 PRD 中 JWT、管理员默认密码和部分管理员扩展能力与现有实现不完全一致。
- `007` 为前后端分离项目，当前可启动并完成登录验证；已修复 `ACCOUNTS.md` 管理员密码错误和 `/api/auth/info` 返回 `400` 的问题，剩余风险主要是 MD5 密码存储与前端主包偏大。
- `008` 为 Spring Boot 一体化部署项目，前端位于 `static/`；当前可启动并完成登录与页面访问验证，已修复受保护接口未登录返回 `200` + `code=500` 的问题和 MySQL 驱动坐标告警，剩余风险主要是 MD5 密码存储与自动化测试缺失。
- `009` 为前后端分离项目，当前可启动并完成登录、分页、权限、代理与模板下载验证；已修复文档默认密码不一致、批量导入通知 `expressId` 为空、分页拦截器缺失以及服务端角色权限缺失问题，剩余风险主要是自动化测试缺失、MD5 密码存储与前端大包体积。
- `010` 为 Spring Boot + 静态页面一体化项目，无独立前端目录；当前可启动并完成学生/管理员登录、页面访问、预约列表、预约创建/取消、通知、统计与权限校验验证，已修复 PRD 口径不一致、MySQL 驱动旧坐标、服务端管理员权限缺失、预约返回缺少 `seatNo` 以及未登录返回乱码问题，剩余风险主要是自动化测试缺失、MD5 密码存储与 README/快速启动文档缺失。
- `011` 为前后端分离项目，当前可启动并完成登录、权限、草稿、创作中心、单视频统计、相关推荐、积分商城和浏览器页面验证；已修复用户端关键 PRD 缺口、历史媒体 URL 脏数据、发布视频 `publishTime` 空值与服务端管理员权限缺失问题，剩余风险主要是管理员前端未实现、自动化测试覆盖不足和前端主包偏大。
- `012` 为 Spring Boot + Thymeleaf 一体化项目，无独立前端目录；当前可启动并完成登录、白板、脑图、双标签实时协作、版本历史、协作者管理、分享页与附件链路验证，已补齐核心编辑器和协作闭环，剩余风险主要是管理员模块、导出能力、自动化测试缺失以及业务异常仍采用 `HTTP 200 + code=500` 返回。
- `013` 为前后端分离项目，当前可启动并完成登录、分页、共享物品管理、收藏、提现、钱包充值和共享物品租借/归还链路验证；已修复前端构建失败、管理端后端权限缺失、分页插件缺失、用户列表密码泄露、共享物品管理空壳以及文档口径冲突，剩余风险主要是闲置租借与技能预约详情页仍为占位流程、地图与图表区域未接入真实组件以及自动化测试缺失。
- `014` 为 Spring Boot + 静态页面一体化项目，无独立前端目录；当前可启动并完成公开列表、登录、勋章、创建社团/圈子/话题、管理员审核、圈子加入、话题互动和活动报名/签到/评价链路验证，已修复公开页误鉴权、后台越权、分页插件缺失、密码字段泄露、勋章墙占位和创建入口缺失问题，剩余风险主要是通知/积分流水前端页缺失、管理员角色仍未细分、自动化测试缺失以及前端依赖 CDN。
- `015` 为 Spring Boot + 静态页面一体化项目，无独立前端目录；当前可启动并完成登录、权限、发帖、评论/回复、点赞、举报、私信、敏感词待审与实名认证审核链路验证，已修复后台越权、公开路由放行错误、分页插件缺失、敏感词高等级审核失效、静态页接口断链、JDK 17 编译问题、初始化密码哈希错误以及数据库配置口径错误，剩余风险主要是 PRD/README 中的图片上传、标签接口、收藏、通知、关注排序等能力仍未完整落地，以及控制台中文日志存在编码乱码。
- `016` 为前后端分离项目，当前可启动并完成登录、注册、资料修改、密码修改、订单广场筛选、发单、接单、上传取件/送达凭证、确认收货、评价、投诉、通知、后台统计、用户/订单/投诉/交易筛选与管理员强制取消链路验证；已修复后台越权、分页插件缺失、用户资料接口缺失、后台处理接口缺失、管理端返回字段不匹配、钱包消费流水缺失、默认密码哈希错误以及账号文档数据库口径错误，剩余风险主要是图片上传仍为 URL 录入、投诉成立后的已完成订单暂未做资金逆向冲正，以及前端主包体积偏大。
- `017` 为 Spring Boot + Thymeleaf 一体化项目，无独立前端目录；当前可启动并完成学生 / 商家 / 管理员登录、购物车、下单、商家接单 / 出餐、用户确认取餐和后台统计链路验证，已修复数据库密码口径错误、默认密码哈希错误、商家端 / 管理端登录后页面断链、购物车未接后端、订单核心链路缺失、JWT 未接鉴权以及权限错误仍返回 `HTTP 200` 的问题，剩余风险主要是用户资料、余额充值、评价、收藏、优惠券、搜索和更完整的后台管理仍未落地。
- `018` 为前后端分离项目，当前可启动并完成登录、公开详情、企业注册、岗位发布、简历创建、投递、企业查看投递、安排面试、学生 / 企业面试列表、简历权限校验、管理员审核与浏览器页面验证；已修复管理员模块缺口、分页插件缺失、企业注册未创建企业资料、简历与投递越权、企业面试列表缺失、公开详情路由误放行导致鉴权失效以及岗位类型展示兼容问题，剩余风险主要是前端构建仍有大包告警、Vite 开发环境缺少 `favicon.ico`，以及自动化测试覆盖仍偏薄。
- `019` 为前后端分离项目，当前可启动并完成默认账号登录、运动打卡、计划进度积分、健康档案 BMI、场馆预约 / 签到与约球活动人数链路验证；已修复默认密码哈希占位、假测试、BMI 公式错误、积分未累计、活动发起人参与记录缺失、预约状态断链与场馆排期缺失问题，剩余风险主要是排行榜的日 / 周 / 月周期统计仍未真正区分，以及前端主包仍有体积告警。
- `020` 为前后端分离项目，当前可启动并完成登录脱敏、资源上传奖励、问答回答奖励、私密笔记权限与公开列表泄露检查；已修复前端 alias 缺失导致的构建失败、首页统计缺口、密码哈希泄露、上传/回答积分规则缺失、问答防刷边界与私密笔记越权问题，剩余风险主要是 PRD 未定义具体奖励分值、前端仍有大包体积告警，以及自动化测试尚未覆盖完整集成链路。
- `021` 为前后端分离项目，当前可启动并完成注册、登录、商品发布/编辑、收藏、聊天、议价、接受议价自动生成订单、确认收货、双方评价、资料更新、图片上传与前端页面访问验证；已修复 Spring Boot 3.2 与旧 MyBatis-Plus starter 不兼容、订单模块编译错误、商品接口公开路由误放行、`product.condition` 关键字导致的 SQL 语法错误、资料更新空密码覆盖、前端页面缺失与登录态映射错误，并补齐了订单/聊天/收藏后端闭环与前端页面，剩余风险主要是简化管理后台仍未落地、前端主包体积偏大以及后端自动化测试仍为空。
- `022` 为前后端分离项目，当前可启动并完成学生端 / 管理端登录、资料修改、密码修改、自习室与座位浏览、预约创建 / 取消、预约分页、信用记录、仪表盘、用户管理、房间 / 座位 CRUD、系统配置与浏览器页面验证；已修复管理员初始化缺失、系统配置与管理接口缺口、预约详情字段缺失、未来预约误占当前座位、分页拦截器缺失、更新时间不刷新和前端代理错误改写 `/api` 前缀等问题，剩余风险主要是 README 中的 WebSocket 实时刷新、审计日志、数据导出、邮件 / 短信通知仍未落地，以及前端构建仍有大包与 Sass 旧 API 告警。
- `023` 为 Spring Boot + Thymeleaf 一体化项目，无独立前端目录；当前可启动并完成登录、注册校验、课程详情、评论、收藏、学习路径、分析看板、问答会话与导出链路验证，已修复 Boot 3 / JDK 17 兼容、H2 自举、JWT 返回结构、模板页面缺失、页面导航 fallback 与大批模板断链接口问题，剩余风险主要是 README / PRD 中的“AI 能力”描述明显强于当前实际实现，以及自动化测试覆盖仍偏薄。
- `024` 为前后端分离项目，当前可启动并完成注册、登录、宠物列表、宠物新增 / 编辑 / 删除与浏览器联调验证；已修复后端 `UserServiceImpl` 包声明缺失导致的启动失败、默认 MySQL 强依赖、前端缺少 `package.json` 脚本与关键依赖、Vite 入口与别名缺失，并补了前端注册页，剩余风险主要是 PRD 中领养申请、救助中心、爱心社区、公告和后台审核等模块仍未完整落地，当前实现更接近“简化宠物管理台”而非完整平台。
- `025` 为前后端分离项目，当前可启动并完成登录、用户注册、业主管理、物业缴费、报修状态流转、公告增删改、访客登记 / 状态更新、车位增删改与浏览器页面联调验证；已修复默认端口冲突、`mybatis` 配置层级错误导致 XML 未加载、默认 MySQL 凭据失效、前端请求地址与开发端口未项目化、访客时间格式不兼容、车位新增强制回写 `FREE`、业主删除缺少关联数据校验以及登录返回密码泄露问题，剩余风险主要是密码仍为明文存储、缺少真正的接口鉴权 / 角色隔离、后端自动化测试为空且异常仍采用 `HTTP 200 + code=500` 返回。
- `026` 为前后端分离项目，当前可启动并完成登录、画师列表、公开需求、订单详情、尾款支付与画师审核链路验证；已修复后端工程骨架缺依赖、默认 MySQL 强依赖、H2 自举缺失、订单状态字段长度不足、异常返回 `msg=null`、用户密码泄露、画师审核角色未回写、尾款收益入错账户、前端默认请求地址与开发端口未项目化等问题，剩余风险主要是 PRD 中报价招标、订单聊天、钱包提现、搜索推荐、管理后台、JWT/加密与更完整权限体系仍未落地。
- `027` 为前后端分离项目，当前可启动并完成登录、门店、理发师、服务列表、余额充值、订单支付、订单评价、预约创建与取消验证；已修复 Boot 3 / MyBatis-Plus 兼容、默认 MySQL 强依赖、端口冲突、分页插件缺失、自动时间填充缺失与默认密码哈希错误问题，剩余风险主要是订单自动生成、积分/余额流水、通知与前端预约详情展示仍未完整落地。
- `028` 为前后端分离项目，当前可启动并完成登录、注册、停车点列表、租车/还车、钱包充值、故障上报和后台统计验证；已修复 Boot 3 / MyBatis-Plus 兼容、默认 MySQL 凭据失效导致的登录 500、默认环境无法直接启动、分页数据库类型不匹配以及启动提示端口固定问题，剩余风险主要是 PRD 中订单申诉、临时锁车、真实地图找车、图片上传闭环与信用记录/等级能力仍未完整落地。
- `029` 为前后端分离项目，当前可启动并完成登录、公开食谱列表、用户资料、管理员接口与前端首页访问验证；已修复 JDK 17 泛型编译错误、JJWT 0.12.3 兼容、Boot 3 / MyBatis-Plus starter 冲突、默认 MySQL 强依赖、H2 自举脚本、JWT 密钥长度不足、密码哈希泄露、前端缺页和前后端接口路径不一致问题，剩余风险主要是项目名称口径冲突、管理端前端仍未独立落地以及前端大包告警。
- `030` 为前后端分离项目，当前可启动并完成患者/管理员登录、医生审核权限、健康数据新增/查询与前端登录页访问验证；已修复默认 PostgreSQL 强依赖、无效 `schema.sql`、H2 保留字建表失败、默认账号缺失、密码哈希泄露、患者/医生/管理员接口越权以及前端角色路由缺失问题，剩余风险主要是健康知识、评估预警、统计报表与系统管理等 PRD 模块仍有明显缺口。
