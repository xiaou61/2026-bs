# 001 校园事务管理系统检查报告

## 1. 基本信息

- 项目编号：`001`
- 项目名称：校园事务管理系统
- 项目目录：
  - 后端：`001-backend`
  - 前端：`001-frontend`
- 项目来源：
  - 项目总览：`readme_simple.md`
  - 后端 PRD：`001-backend/PRD/PRD.md`
  - 后端说明：`001-backend/README.md`
  - 前端说明：`001-frontend/README.md`
- 当前检查日期：`2026-04-04`

## 2. 检查目标

- 核对 PRD、README 与源码实现是否基本一致
- 检查是否满足 JDK 17 运行要求
- 验证后端构建、测试、启动链路
- 验证前端构建、启动链路
- 记录问题、修复动作和最终结论

## 3. 现状速览

### 3.1 文档与目录

- 后端存在独立 PRD 文档，内容覆盖用户、请假、报修、公告、活动等核心模块
- 后端 README 声明技术栈为 `Spring Boot 3.2.0 + MyBatis-Plus 3.5.5 + JDK 17`
- 前端 README 声明技术栈为 `Vue 3 + Vite 5 + Element Plus`
- 后端源码目录完整，已包含控制器、实体、Mapper、Service、SQL 初始化脚本
- 前端源码目录完整，已包含登录、注册、首页、请假、报修、公告、活动、用户管理、个人中心页面

### 3.2 环境前置

- Maven 可正常识别 JDK 17，`mvn -version` 显示 `Java version: 17`
- 本机 `java` 命令未加入 PATH，直接执行 `java -version` 失败
- Node.js 版本：`v22.17.0`
- npm 版本：`10.9.2`
- 后端默认端口：`8080`
- 前端默认端口：`3000`
- MySQL 监听端口：`3306`
- 后端默认依赖本地 MySQL：
  - 数据库：`campus_affairs`
  - 用户名：`root`
  - 密码：`1234`

## 4. 文档完整性检查

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| 项目概述 | 通过 | 总览、后端 README、前端 README 均有基础说明 |
| 功能模块描述 | 通过 | PRD 与 README 均覆盖核心模块 |
| 技术栈说明 | 通过 | 前后端技术栈写明较完整 |
| 环境要求 | 基本通过 | 后端与前端均列出基础运行要求 |
| 数据库初始化说明 | 通过 | 后端 `init.sql` 已提供 |
| 默认账号说明 | 通过 | 前后端 README 均列出 |
| 测试说明 | 不完整 | 未发现明确测试用例与测试命令说明 |
| 启动说明 | 基本通过 | 有启动命令，但未说明数据库未就绪时的处理方式 |
| 文档与源码一致性 | 通过 | 核心模块、路由、控制器和 SQL 初始化脚本与文档描述基本一致 |

## 5. JDK 17 与依赖检查

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| `pom.xml` Java 版本 | 通过 | `java.version`、`source`、`target` 均为 `17` |
| Spring Boot 版本 | 通过 | 使用 `3.2.0`，与 JDK 17 匹配 |
| MyBatis-Plus 版本 | 通过 | 使用 `mybatis-plus-spring-boot3-starter 3.5.5` |
| JWT 依赖 | 通过 | `jjwt 0.11.5` 与 Spring Boot 3 组合常规可用 |
| `java` PATH 配置 | 风险 | 终端直接执行 `java` 失败，但 Maven 内部可用 JDK 17 |
| 数据库依赖 | 通过 | 当前机器存在 `3306` 监听，启动后已成功查询 `campus_affairs.user` 表 |

## 6. 实测记录

### 6.1 后端

| 项目 | 命令 | 结果 | 备注 |
| --- | --- | --- | --- |
| 环境检查 | `mvn -version` | 通过 | Maven 绑定到 JDK 17 |
| 测试/构建 | `mvn test -DskipTests=false` | 通过 | 成功编译并完成 Surefire 阶段，但仓库内暂无自动化测试类 |
| 启动验证 | `mvn spring-boot:run` | 失败 | 非项目缺陷，默认端口 `8080` 被外部 Java 进程占用 |
| 隔离启动验证 | `mvn spring-boot:run -Dspring-boot.run.arguments=--server.port=18080` | 通过 | Spring Boot 成功启动，日志显示 `Started CampusAffairsApplication` |
| 接口验证 | `POST /api/auth/login` | 通过 | 使用默认账号 `admin/admin123` 返回 `200 OK`，并成功查询用户表 |

### 6.2 前端

| 项目 | 命令 | 结果 | 备注 |
| --- | --- | --- | --- |
| 环境检查 | `node -v` / `npm -v` | 通过 | Node 22 / npm 10 |
| 构建验证 | `npm run build` | 通过 | 成功生成 `dist/index.html` |
| 启动验证 | `npm run dev -- --host 127.0.0.1` | 通过 | Vite 日志显示 `ready`，监听 `http://127.0.0.1:3000/` |

## 7. 当前问题清单

| 编号 | 问题 | 严重度 | 状态 |
| --- | --- | --- | --- |
| 001-1 | 本机 `java` 命令未加入 PATH，可能影响手工启动命令与脚本通用性 | 中 | 未修复 |
| 001-2 | 项目未提供自动化测试文件，当前主要依赖构建验证、启动验证与接口抽测 | 中 | 未修复 |
| 001-3 | 默认后端端口 `8080` 在当前机器上被其他 Java 进程占用 | 低 | 已确认是环境问题，可通过切换端口规避 |

## 8. 修复记录

- 未修改业务代码。
- 为排除环境端口冲突影响，使用 `--server.port=18080` 对后端进行了隔离启动验证。
- 使用默认管理员账号完成了登录接口抽测，确认数据库连接、JWT 登录链路和用户查询逻辑可用。

## 9. 当前结论

- `001` 已完成首轮巡检。
- 文档、PRD 与核心源码实现基本一致，项目后端面向 JDK 17 的依赖配置正常。
- 后端已通过 `mvn test`，前端已通过 `npm run build`。
- 后端在默认端口 `8080` 下受外部进程占用影响无法直接启动，但在 `18080` 端口可正常启动，并通过默认管理员登录接口验证。
- 前端开发服务可在 `3000` 端口正常启动。
- 当前可判定 `001` 在现有环境下“可通过测试并启动”，剩余问题主要是自动化测试缺失和本机 Java PATH 配置风险。
