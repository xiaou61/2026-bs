# 007 志愿活动管理与积分激励平台检查报告

## 1. 基本信息

- 项目编号：`007`
- 项目名称：志愿活动管理与积分激励平台
- 项目目录：
  - 后端：`007-backend`
  - 前端：`007-frontend`
- 项目来源：
  - 项目总览：`readme_simple.md`
  - 后端 PRD：`007-backend/PRD/PRD.md`
  - 测试账号：`007-backend/ACCOUNTS.md`
- 当前检查日期：`2026-04-04`

## 2. 检查目标

- 核对 PRD、账号文档与源码实现是否基本一致
- 检查是否满足 JDK 17 运行要求
- 验证后端构建、前端构建、启动与基础链路
- 记录安全风险、文档空缺与最终结论

## 3. 现状速览

### 3.1 文档与目录

- 项目为前后端分离结构，包含 `007-backend` 与 `007-frontend`
- 后端暂无独立 README，当前主要依赖 PRD 与账号文档
- 后端源码已包含认证、用户、活动、报名、签到、积分、奖励、兑换、统计等模块
- 前端源码已包含登录、注册、管理员后台、志愿者活动页、积分页、兑换页、排行榜、个人中心等页面

### 3.2 环境前置

- Maven/JDK 公共环境已确认可使用 JDK 17
- MySQL `3306` 端口当前已监听
- 后端默认端口：`8087`
- 前端默认端口：`3007`
- 前端代理目标：`http://localhost:8087`
- 后端默认依赖本地 MySQL：
  - 数据库：`volunteer_db`
  - 用户名：`root`
  - 密码：`1234`
- 数据初始化脚本：`007-backend/src/main/resources/sql/init.sql`

## 4. 文档完整性检查

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| 项目概述 | 基本通过 | PRD 与源码对前后端分离、角色和核心模块描述基本一致 |
| 功能模块描述 | 基本通过 | 认证、用户、活动、报名、签到、积分、奖励、兑换、统计模块均有实现 |
| 账号说明 | 基本通过 | 志愿者账号与 `init.sql` 一致，但管理员密码与账号文档不一致 |
| 启动说明 | 通过 | 端口、数据库、构建和启动命令可复现 |
| 文档与源码一致性 | 基本通过 | 核心路由和控制器较齐全，但存在 `auth/info` 接口缺陷、后端 README 缺失和账号文档偏差 |

## 5. JDK 17 与依赖检查

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| `pom.xml` Java 版本 | 通过 | `maven.compiler.source`、`maven.compiler.target` 均为 `17` |
| Spring Boot 版本 | 通过 | 使用 `3.2.0` |
| MyBatis-Plus 依赖 | 通过 | 使用 `mybatis-plus-spring-boot3-starter 3.5.5` |
| JWT 依赖 | 通过 | 已引入 `java-jwt 4.4.0`，拦截器和工具类已接入 |
| 前端构建配置 | 通过 | Vite 监听 `3007`，并代理 `/api` 到 `8087` |
| 数据库初始化脚本 | 通过 | `init.sql` 提供建库建表、默认账号、活动和奖励数据 |
| 密码存储方式 | 风险 | 登录与注册使用 MD5 哈希处理密码，安全性弱于更现代的密码散列方案 |

## 6. 实测记录

### 6.1 后端

| 项目 | 命令 | 结果 | 备注 |
| --- | --- | --- | --- |
| 环境检查 | `mvn -version` | 通过 | Maven 绑定到 JDK 17 |
| 测试/构建 | `mvn test -DskipTests=false` | 通过 | 成功编译并完成 Surefire 阶段，但仓库内暂无 `src/test` 自动化测试目录 |
| 启动验证 | `mvn spring-boot:run` | 通过 | 应用成功启动，监听 `8087`，日志显示 `Started VolunteerApplication` |
| 接口验证 | `POST /api/auth/login` | 通过 | `admin/123456`、`volunteer/123456` 登录成功返回 `200` 与 token |
| 接口缺陷验证 | `GET /api/auth/info` | 通过 | 修复鉴权白名单后，携带合法 Bearer Token 可正常返回 `200` |

### 6.2 前端

| 项目 | 命令 | 结果 | 备注 |
| --- | --- | --- | --- |
| 环境检查 | `node -v` / `npm -v` | 通过 | Node `v22.17.0` / npm `10.9.2` |
| 构建验证 | `npm run build` | 通过 | 成功生成 Vite 构建产物，但主包大小告警超过 `500 kB` |
| 启动验证 | `npm run dev -- --host 127.0.0.1` | 通过 | Vite 成功启动，监听 `http://127.0.0.1:3007/` |
| 页面验证 | `curl -I http://127.0.0.1:3007/` | 通过 | 返回 `HTTP/1.1 200 OK` |
| 代理验证 | `POST http://127.0.0.1:3007/api/auth/login` | 通过 | 前端代理到后端登录接口返回 `200` |

## 7. 当前问题清单

| 编号 | 问题 | 严重度 | 状态 |
| --- | --- | --- | --- |
| 007-1 | 后端缺少正式 README，运行方式和依赖说明主要分散在 PRD 与账号文档中 | 中 | 未修复 |
| 007-2 | 项目未提供自动化测试目录，当前主要依赖构建验证与启动抽测 | 中 | 未修复 |
| 007-3 | 登录与注册使用 MD5 哈希处理密码，安全性弱于更现代的密码散列方案 | 中 | 未修复 |
| 007-4 | `ACCOUNTS.md` 曾声明管理员密码为 `admin123`，但初始化 SQL 与实测结果均为 `123456` | 中 | 已修复 |
| 007-5 | `/api/auth/info` 曾被纳入 `/api/auth/**` 白名单，导致拦截器不注入 `userId`，接口携带合法 token 返回 `400` | 高 | 已修复 |
| 007-6 | 前端构建产物主包超过 `500 kB`，存在首屏加载体积偏大的性能风险 | 低 | 未修复 |

## 8. 修复记录

- 未修改业务代码。
- 已完成后端 `mvn test`、前端 `npm run build`、后端启动、前端启动、后端登录接口和前端代理登录接口抽测。
- 已把 `007-backend/ACCOUNTS.md` 中管理员密码从 `admin123` 更正为 `123456`，与初始化 SQL 和实测结果保持一致。
- 已修复 `/api/auth/info` 返回 `400` 的问题：将鉴权拦截器白名单从整个 `/api/auth/**` 缩小为仅放行 `/api/auth/login` 与 `/api/auth/register`，复测后接口已返回 `200`。
- 巡检结束后将清理本次验证产生的日志、进程与构建产物，避免影响后续项目检查。

## 9. 当前结论

- `007` 已完成首轮巡检。
- 项目后端面向 JDK 17 的依赖配置正常，后端已通过 `mvn test`，前端已通过 `npm run build`。
- 后端可在默认端口 `8087` 正常启动，前端开发服务可在 `3007` 正常启动，登录接口、`/api/auth/info` 和前端代理链路均通过验证。
- 当前可判定 `007` 在现有环境下“可通过测试并启动”。
- 但从“项目完整性”角度看，仍存在待改进项，主要是后端 README 缺失、密码仍使用 MD5 存储，以及前端构建主包偏大。
