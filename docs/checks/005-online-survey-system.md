# 005 在线问卷调查与数据分析系统检查报告

## 1. 基本信息

- 项目编号：`005`
- 项目名称：在线问卷调查与数据分析系统
- 项目目录：
  - 后端：`005-backend`
- 项目来源：
  - 项目总览：`readme_simple.md`
  - 后端 PRD：`005-backend/PRD/1.md`
  - 后端说明：`005-backend/README.md`
  - 测试账号：`005-backend/ACCOUNTS.md`
- 当前检查日期：`2026-04-04`

## 2. 检查目标

- 核对 PRD、README、账号文档与源码实现是否基本一致
- 检查是否满足 JDK 17 运行要求
- 验证后端构建、启动与基础页面/接口链路
- 记录数据库依赖、文档差异和最终结论

## 3. 现状速览

### 3.1 文档与目录

- 项目当前仅包含 `005-backend`，未发现独立 `005-frontend`
- README 将项目描述为 `Spring Boot + Vue 3` 的一体化系统，页面模板位于 `src/main/resources/templates`
- `src/main/resources/static/README.md` 明确写明当前项目采用前后端一体化架构，说明 PRD 中“前后端分离”的表述与当前实现不一致
- 源码已包含用户、问卷、题目、答卷、统计、导出、页面路由等模块

### 3.2 环境前置

- Maven/JDK 公共环境已确认可使用 JDK 17
- MySQL `3306` 端口当前已监听
- 后端默认端口：`8085`
- 后端默认依赖本地 MySQL：
  - 数据库：`survey_db`
  - 用户名：`root`
  - 密码：`1234`
- 数据初始化脚本：`005-backend/src/main/resources/sql/init.sql`

## 4. 文档完整性检查

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| 项目概述 | 基本通过 | 总览、PRD、README 能覆盖核心背景，但系统架构描述存在差异 |
| 功能模块描述 | 基本通过 | 用户、问卷、题目、答卷、统计、Excel 导出均有实现 |
| 账号说明 | 通过 | `ACCOUNTS.md` 与 `init.sql` 预置账号一致，登录抽测通过 |
| 启动说明 | 通过 | README 中数据库、端口和启动命令可复现 |
| 文档与源码一致性 | 基本通过 | 核心模块一致，但“前后端分离”“普通用户查看列表”“模板复制/PDF/日志”等描述未完全落地 |

## 5. JDK 17 与依赖检查

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| `pom.xml` Java 版本 | 通过 | `maven.compiler.source`、`maven.compiler.target` 均为 `17` |
| Spring Boot 版本 | 通过 | 使用 `3.2.0` |
| MyBatis-Plus 版本 | 通过 | 使用 `3.5.5` |
| JWT 依赖 | 通过 | 已引入 `jjwt 0.11.5` |
| POI 导出依赖 | 通过 | 已引入 `poi / poi-ooxml 5.2.3`，并存在 Excel 导出服务 |
| 数据库初始化脚本 | 通过 | `init.sql` 提供建库建表与默认账号数据 |
| 密码存储方式 | 通过 | 使用 `BCrypt` 进行密码哈希与校验 |

## 6. 实测记录

### 6.1 后端

| 项目 | 命令 | 结果 | 备注 |
| --- | --- | --- | --- |
| 环境检查 | `mvn -version` | 通过 | Maven 绑定到 JDK 17 |
| 测试/构建 | `mvn test -DskipTests=false` | 通过 | 成功编译并完成 Surefire 阶段，但仓库内暂无 `src/test` 自动化测试目录 |
| 启动验证 | `mvn spring-boot:run` | 通过 | 应用成功启动，监听 `8085`，日志显示 `Started SurveyApplication` |
| 首页验证 | `GET /` | 通过 | 返回 `200`，首页模板可访问 |
| 登录页验证 | `GET /login` | 通过 | 返回 `200`，登录页模板可访问 |
| 接口验证 | `POST /api/user/login` | 通过 | 使用默认管理员账号 `admin/123456` 登录返回 `200` 并返回 token |

## 7. 当前问题清单

| 编号 | 问题 | 严重度 | 状态 |
| --- | --- | --- | --- |
| 005-1 | PRD 标注“前后端分离”，但 README、静态资源说明和实际目录均为 Spring Boot + Thymeleaf 一体化项目 | 中 | 未修复 |
| 005-2 | 项目未提供自动化测试目录，当前主要依赖构建验证、启动验证与接口抽测 | 中 | 未修复 |
| 005-3 | 文档称普通用户可查看问卷列表和统计，但后端仅提供“查询我创建的问卷”接口，未发现已发布问卷公共列表接口 | 中 | 未修复 |
| 005-4 | PRD 提到问卷模板复制、PDF 报告/截图导出、操作日志、用户管理等能力，当前代码中未发现对应后端接口或管理模块 | 中 | 未修复 |

## 8. 修复记录

- 未修改业务代码。
- 已完成后端 `mvn test`、服务启动、首页/登录页访问和管理员登录接口抽测。
- 已确认项目为一体化模板渲染架构，不存在独立前端构建步骤。
- 巡检结束后将清理本次验证产生的日志、运行进程与构建产物，避免影响后续项目检查。

## 9. 当前结论

- `005` 已完成首轮巡检。
- 项目当前是 `Spring Boot + Thymeleaf + CDN Vue 3` 的一体化系统，后端面向 JDK 17 的依赖配置正常。
- 后端已通过 `mvn test`，应用可在默认端口 `8085` 正常启动，首页、登录页和登录接口抽测均通过。
- 当前可判定 `005` 在现有环境下“可通过测试并启动”。
- 但从“项目完整性”角度看，文档与实现仍存在差异，尤其是系统架构描述、普通用户查看问卷列表能力，以及 PRD 中部分扩展功能未完全落地，后续应按本报告问题清单继续补齐。
