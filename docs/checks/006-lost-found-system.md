# 006 校园失物招领系统检查报告

## 1. 基本信息

- 项目编号：`006`
- 项目名称：校园失物招领系统
- 项目目录：
  - 后端：`006-backend`
- 项目来源：
  - 项目总览：`readme_simple.md`
  - 后端 PRD：`006-backend/PRD/PRD.md`
  - 测试账号：`006-backend/ACCOUNTS.md`
- 当前检查日期：`2026-04-04`

## 2. 检查目标

- 核对 PRD、账号文档与源码实现是否基本一致
- 检查是否满足 JDK 17 运行要求
- 验证后端构建、启动与基础页面/接口链路
- 记录鉴权方式、数据库依赖、文档差异和最终结论

## 3. 现状速览

### 3.1 文档与目录

- 项目当前仅包含 `006-backend`，未发现独立 `006-frontend`
- 后端暂无独立 README，当前主要依赖 PRD 与账号文档
- 源码已包含用户、分类、失物、招领、认领、收藏、通知、文件上传与页面模板等模块
- 页面模板位于 `src/main/resources/templates`，实际形态为一体化项目

### 3.2 环境前置

- Maven/JDK 公共环境已确认可使用 JDK 17
- MySQL `3306` 端口当前已监听
- 后端默认端口：`8086`
- 后端默认依赖本地 MySQL：
  - 数据库：`lost_found_db`
  - 用户名：`root`
  - 密码：`1234`
- 上传目录：`./uploads/`
- 数据初始化脚本：`006-backend/src/main/resources/sql/init.sql`

## 4. 文档完整性检查

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| 项目概述 | 基本通过 | PRD 与账号文档能覆盖核心背景，但缺少 README 级运行汇总 |
| 功能模块描述 | 基本通过 | 用户、分类、失物、招领、认领、收藏、通知、管理员用户/分类页均有实现 |
| 账号说明 | 基本通过 | `ACCOUNTS.md` 与 `init.sql` 基本一致，但与 PRD 中管理员默认密码描述不一致 |
| 启动说明 | 通过 | 端口、数据库和启动命令可复现 |
| 文档与源码一致性 | 基本通过 | 核心流程存在，但鉴权方式、登录接口路径和部分管理员扩展能力与 PRD 不完全一致 |

## 5. JDK 17 与依赖检查

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| `pom.xml` Java 版本 | 通过 | `maven.compiler.source`、`maven.compiler.target` 均为 `17` |
| Spring Boot 版本 | 通过 | 使用 `3.2.0` |
| MyBatis 依赖 | 通过 | 使用 `mybatis-spring-boot-starter 3.0.3` |
| 文件上传配置 | 通过 | `application.yml` 已配置 `multipart` 和 `upload.path` |
| 密码加密依赖 | 风险 | 使用 `Hutool DigestUtil.md5Hex` 进行 MD5 哈希，安全性较弱 |
| 数据库初始化脚本 | 通过 | `init.sql` 提供建库建表、分类数据和测试账号 |
| 鉴权实现 | 差异 | PRD 标注 JWT 鉴权，但当前代码基于 `HttpSession` 拦截登录态 |

## 6. 实测记录

### 6.1 后端

| 项目 | 命令 | 结果 | 备注 |
| --- | --- | --- | --- |
| 环境检查 | `mvn -version` | 通过 | Maven 绑定到 JDK 17 |
| 测试/构建 | `mvn test -DskipTests=false` | 通过 | 成功编译并完成 Surefire 阶段，但仓库内暂无 `src/test` 自动化测试目录 |
| 启动验证 | `mvn spring-boot:run` | 通过 | 应用成功启动，监听 `8086`，日志显示 `Started LostFoundApplication` |
| 入口验证 | `GET /` | 通过 | 未登录状态返回 `302` 并跳转到 `/login`，符合当前实现 |
| 登录页验证 | `GET /login` | 通过 | 返回 `200` |
| 接口验证 | `POST /api/user/login` | 通过 | 使用默认管理员账号 `admin/admin` 登录返回 `200` |
| 登录态页面验证 | `GET /home` | 通过 | 携带登录 Session Cookie 后返回 `200` |
| 管理页验证 | `GET /admin/users` | 通过 | 管理员登录态下返回 `200` |

## 7. 当前问题清单

| 编号 | 问题 | 严重度 | 状态 |
| --- | --- | --- | --- |
| 006-1 | PRD 写明使用 JWT 鉴权，且登录接口为 `/api/auth/*`，但当前代码实际使用 Session 鉴权与 `/api/user/login` / `/api/user/register` | 中 | 未修复 |
| 006-2 | 项目未提供自动化测试目录，当前主要依赖构建验证与启动抽测 | 中 | 未修复 |
| 006-3 | 后端缺少正式 README，运行说明主要分散在 PRD 与账号文档中 | 中 | 未修复 |
| 006-4 | PRD 中管理员默认密码写为 `admin123`，但 `ACCOUNTS.md`、初始化 SQL 和实测结果均为 `admin` | 中 | 未修复 |
| 006-5 | 密码使用 MD5 存储和校验，存在口令安全风险 | 高 | 未修复 |
| 006-6 | PRD 提到统计分析、系统配置等管理员能力，但当前未发现对应独立控制器接口实现 | 中 | 未修复 |

## 8. 修复记录

- 未修改业务代码。
- 已完成后端 `mvn test`、服务启动、登录页访问、管理员登录、登录态页面和管理员页面抽测。
- 已确认当前项目实际采用 Session 登录态管理，而非 PRD 所述 JWT 鉴权。
- 巡检结束后将清理本次验证产生的日志、运行进程、Cookie 文件与构建产物，避免影响后续项目检查。

## 9. 当前结论

- `006` 已完成首轮巡检。
- 项目当前是 `Spring Boot + Thymeleaf` 一体化系统，后端面向 JDK 17 的依赖配置正常。
- 后端已通过 `mvn test`，应用可在默认端口 `8086` 正常启动，登录页、管理员登录、登录态首页和管理员用户页抽测均通过。
- 当前可判定 `006` 在现有环境下“可通过测试并启动”。
- 但从“项目完整性”角度看，仍存在文档与实现不一致问题，尤其是 JWT/Session 鉴权描述、管理员默认密码、统计模块与系统配置能力未完全落地，后续应按本报告问题清单继续补齐。
