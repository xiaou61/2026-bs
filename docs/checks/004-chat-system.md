# 004 实时聊天与通知系统检查报告

## 1. 基本信息

- 项目编号：`004`
- 项目名称：实时聊天与通知系统
- 项目目录：
  - 后端：`004-backend`
  - 前端：`004-frontend`
- 项目来源：
  - 项目总览：`readme_simple.md`
  - 后端 PRD：`004-backend/PRD/1.md`
  - 测试账号：`004-backend/ACCOUNTS.md`
  - 前端说明：`004-frontend/README.md`
- 当前检查日期：`2026-04-04`

## 2. 检查目标

- 核对 PRD、账号文档、前端 README 与源码实现是否基本一致
- 检查是否满足 JDK 17 运行要求
- 验证后端构建、前端构建、启动与基础链路
- 记录 Redis/MySQL/WebSocket 依赖带来的风险与最终结论

## 3. 现状速览

### 3.1 文档与目录

- 项目为前后端分离结构，包含 `004-backend` 与 `004-frontend`
- 后端暂无独立 README，但存在 PRD 与账号文档
- 后端已包含用户、好友、好友分组、聊天消息、通知、WebSocket 相关源码
- 前端已包含登录、注册、聊天、好友、通知、个人资料等页面

### 3.2 环境前置

- Maven/JDK 公共环境已确认可使用 JDK 17
- Node.js 版本：`v22.17.0`
- npm 版本：`10.9.2`
- 后端默认端口：`8004`
- 前端默认端口：`5004`
- 后端默认依赖：
  - MySQL：`chat_system`
  - Redis：`localhost:6379`
  - 数据库账号：`root / 1234`

## 4. 文档完整性检查

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| 项目概述 | 通过 | 总览、PRD、前端 README 能拼出完整背景 |
| 功能模块描述 | 通过 | PRD 覆盖聊天、好友、通知、离线消息等核心模块 |
| 账号说明 | 通过 | `ACCOUNTS.md` 与前端 README 均提供账号 |
| 启动说明 | 基本通过 | 前端说明存在，后端缺少 README 级启动文档 |
| 依赖说明 | 基本通过 | PRD 写明 Redis/MySQL/WebSocket，但后端缺正式 README 汇总 |
| 文档与源码一致性 | 通过 | 聊天、好友、通知、登录注册、个人资料等核心模块与源码结构基本一致 |

## 5. JDK 17 与依赖检查

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| `pom.xml` Java 版本 | 通过 | `source`、`target` 均为 `17` |
| Spring Boot 版本 | 通过 | 使用 `3.2.0` |
| WebSocket 依赖 | 通过 | 已引入 `spring-boot-starter-websocket` |
| Redis 依赖 | 通过 | 已引入 `spring-boot-starter-data-redis` |
| MyBatis-Plus 版本 | 通过 | 使用 `3.5.5` |
| 前端 WebSocket 依赖 | 通过 | 已引入 `sockjs-client` 与 `stompjs` |
| MySQL 实际可用性 | 通过 | 后端成功启动并完成登录接口校验，数据库查询链路可用 |
| Redis 实际可用性 | 风险 | 本机 `6379` 未监听，但基础启动与登录链路未被阻塞，实时能力未深度验证 |

## 6. 实测记录

### 6.1 后端

| 项目 | 命令 | 结果 | 备注 |
| --- | --- | --- | --- |
| 环境检查 | `mvn -version` | 通过 | Maven 绑定到 JDK 17 |
| 测试/构建 | `mvn test -DskipTests=false` | 通过 | 成功编译并完成 Surefire 阶段，但仓库内暂无 `src/test` 自动化测试目录 |
| 启动验证 | `mvn spring-boot:run` | 通过 | 应用成功启动，监听 `8004`，日志显示 `Started ChatSystemApplication` |
| 接口验证 | `POST /api/auth/login` | 通过 | 使用默认账号完成登录抽测，接口返回 `200 OK` |

### 6.2 前端

| 项目 | 命令 | 结果 | 备注 |
| --- | --- | --- | --- |
| 环境检查 | `node -v` / `npm -v` | 通过 | Node `v22.17.0` / npm `10.9.2` |
| 构建验证 | `npm run build` | 通过 | 成功生成 Vite 构建产物 |
| 启动验证 | `npm run dev -- --host 127.0.0.1` | 通过 | Vite 成功启动，监听 `http://127.0.0.1:5004/` |
| 页面验证 | `curl -I http://127.0.0.1:5004` | 通过 | 返回 `HTTP/1.1 200 OK` |

## 7. 当前问题清单

| 编号 | 问题 | 严重度 | 状态 |
| --- | --- | --- | --- |
| 004-1 | 后端缺少正式 README，运行方式和依赖说明分散在 PRD、账号文档与配置文件中 | 中 | 未修复 |
| 004-2 | 项目未提供自动化测试文件，当前主要依赖构建验证、启动验证与接口抽测 | 中 | 未修复 |
| 004-3 | 本机 Redis `6379` 未监听，当前仅验证了基础登录链路，实时聊天/通知/WebSocket 能力未做深度联调 | 高 | 未修复 |
| 004-4 | 登录逻辑直接进行明文密码比对，存在口令存储与校验安全风险 | 高 | 未修复 |

## 8. 修复记录

- 未修改业务代码。
- 已完成后端 `mvn test`、前端 `npm run build`、后端启动、前端启动、登录接口与页面响应抽测。
- 已确认 Redis 未启动时，项目基础启动和登录链路仍可工作，但未据此认定实时消息链路完全可用。
- 巡检结束后已清理本次验证产生的日志、端口占用进程与构建产物，避免影响后续项目检查。

## 9. 当前结论

- `004` 已完成首轮巡检。
- 项目文档虽缺少后端 README，但 PRD、账号文档、前端 README 与核心源码实现基本一致。
- 后端面向 JDK 17 的依赖配置正常，已通过 `mvn test`；前端已通过 `npm run build`。
- 后端可在默认端口 `8004` 正常启动，登录接口抽测返回 `200 OK`；前端开发服务可在 `5004` 正常启动，页面响应 `200`。
- 当前可判定 `004` 在现有环境下“可通过测试并启动”。
- 剩余风险主要集中在自动化测试缺失、Redis 未联调的实时能力不确定性、明文密码校验以及后端运行文档缺失。
