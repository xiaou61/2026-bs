# 022 校园自习室座位预约系统检查报告

## 1. 基本信息

- 项目编号：`022`
- 项目名称：校园自习室座位预约系统
- 检查日期：`2026-04-08`
- 当前状态：`已完成`
- 最终结论：`已通过测试并可启动`
- 项目目录：
  - 后端：`022-backend`
  - 前端：`022-frontend`
- 主要资料：
  - 项目总览：`readme_simple.md`
  - 后端 PRD：`022-backend/PRD/校园自习室座位预约系统PRD.md`
  - 后端说明：`022-backend/README.md`
  - 后端配置：`022-backend/pom.xml`
  - 后端应用配置：`022-backend/src/main/resources/application.yml`
  - 初始化脚本：`022-backend/sql/init.sql`
  - 前端依赖：`022-frontend/package.json`

## 2. 验收结论

| 维度 | 结论 | 说明 |
| --- | --- | --- |
| 文档完整性 | 基本通过 | PRD、README、配置和 SQL 已可对应主要模块；README 中仍保留若干扩展待办 |
| JDK 17 兼容性 | 通过 | 后端已在 JDK 17 + Spring Boot 3.2 环境下通过编译、测试和启动 |
| 后端测试 | 通过 | `mvn test -DskipTests=false` 通过，补充了座位实时状态回归测试 |
| 前端构建 | 通过 | `npm run build` 通过 |
| 启动结果 | 通过 | 后端因 `8080` 被其他项目占用，使用 `8022` 成功启动；前端以 `3022` 代理到 `8022` 成功启动 |
| 核心链路 | 通过 | 学生端、管理端、接口抽测和 Playwright 浏览器复测均已跑通 |

## 3. 本轮修复摘要

### 3.1 后端补齐与修复

1. 补齐管理员初始化逻辑，启动时自动写入默认管理员 `admin / 123456`
2. 补齐鉴权辅助与管理员判定，统一管理端接口权限
3. 补齐系统配置服务与控制器，打通管理端系统配置页面
4. 补齐用户资料修改、密码修改、用户分页、状态调整、信用分调整等接口
5. 补齐座位列表、批量创建、实时状态、预约分页、预约详情字段等缺口
6. 修复登录返回中密码哈希泄露问题，统一返回脱敏用户信息和角色
7. 修复预约分页错误排序字段问题，从不存在的 `create_time` 改为 `reservation_time`
8. 修复“未来预约会立即把当前座位标成占用”的状态流转错误
9. 修复 MyBatis-Plus 分页拦截器缺失导致的 `records` 有值但 `total/pages` 为 `0`
10. 修复全局 `updateTime` 不刷新的自动填充问题

### 3.2 前端补齐与修复

1. 统一请求封装，支持自动补 `Bearer `、统一 `401` 处理和 `blob` 返回
2. 修复登录态初始化与角色归一化，解决刷新后角色识别不稳定
3. 对齐前后端 API 路径，补齐 `systemConfig` 等接口文件
4. 学生端页面补齐：登录、首页、房间详情、预约记录、预约详情、个人中心、信用记录
5. 管理端页面补齐：仪表盘、自习室管理、座位管理、预约管理、用户管理、统计、系统配置
6. 修复 Vite 代理错误重写 `/api` 前缀，解决浏览器登录与全站接口断链
7. 清理学生端 `el-radio-button` 旧写法，去掉运行期弃用警告

## 4. 关键验证结果

### 4.1 后端测试与启动

- `022-backend` 执行 `mvn test -DskipTests=false`：通过
- 后端执行 `mvn spring-boot:run -Dspring-boot.run.arguments=--server.port=8022`：通过
- 管理员自动初始化验证：通过
  - 登录账号：`admin / 123456`

### 4.2 前端构建与启动

- `022-frontend` 执行 `npm run build`：通过
- 前端执行 `npm run dev -- --host 127.0.0.1 --port 3022`：通过
- 代理目标：`VITE_API_PROXY_TARGET=http://127.0.0.1:8022`

### 4.3 接口抽测

#### 学生端接口

- 注册：通过
- 登录：通过
- 用户信息：通过
- 自习室列表：通过
- 房间座位详情：通过
- 预约创建：通过
- 预约详情 / 二维码：通过
- 预约取消：通过
- 我的预约分页：通过
- 我的信用记录分页：通过
- 资料修改：通过
- 密码修改与恢复：通过

#### 管理端接口

- 仪表盘：通过
- 用户分页：通过
- 用户状态调整：通过
- 用户信用分调整：通过
- 自习室分页：通过
- 自习室新增 / 编辑 / 删除：通过
- 座位新增 / 批量新增 / 状态修改 / 删除：通过
- 预约分页：通过
- 系统配置列表 / 更新：通过

### 4.4 浏览器联调

#### 学生端 Playwright 复测

- 登录页登录成功并跳转 `/student/home`
- 首页统计与快捷入口正常渲染
- 自习室详情页可正常加载房间和座位状态
- 我的预约页可看到取消记录
- 信用记录页可正常展示分页记录
- 个人中心页可正常展示资料与密码修改表单

#### 管理端 Playwright 复测

- 登录页登录成功并跳转 `/admin/dashboard`
- 仪表盘统计、自习室概览、最近预约均正常渲染
- 用户管理页正常展示分页数据
- 系统配置页正常展示配置列表与联调新增项

## 5. PRD / README 对照结论

### 5.1 已形成闭环的核心能力

1. 学生注册、登录、查看自习室、查看座位、预约、取消、查看信用记录
2. 学生个人资料修改、密码修改
3. 管理员登录、仪表盘、用户管理、房间管理、座位管理、预约查看、系统配置
4. 使用统计、预约分页、信用分分页等后台支撑能力

### 5.2 README 中仍未完全落地但不阻塞本轮验收的项

1. WebSocket 实时座位状态更新
2. 日志记录和审计
3. 数据导出
4. 邮件 / 短信通知

## 6. 剩余风险

1. README 中列出的 WebSocket、审计、导出、通知仍是扩展待办，本轮未继续补齐
2. 前端构建仍有主包体积偏大告警
3. Sass 依赖仍输出 legacy JS API 弃用告警，但不影响当前运行
4. 签到成功 / 结束使用的“时间窗口成功分支”本轮未做时间穿越式自动验证，当前主要通过接口实现核对与页面入口验证确认

## 7. 最终结论

- `022` 已从“仅建档定位”推进到“代码修复 + 测试通过 + 真实启动 + 页面复测通过”
- 当前可以给出“已通过测试并可启动”的结论
- 可以继续进入下一个项目巡检
