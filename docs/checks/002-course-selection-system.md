# 002 在线选课与成绩管理系统检查报告

## 1. 基本信息

- 项目编号：`002`
- 项目名称：在线选课与成绩管理系统
- 项目目录：
  - 后端：`002-backend`
  - 前端：`002-frontend`
- 项目来源：
  - 项目总览：`readme_simple.md`
  - 后端 PRD：`002-backend/PRD/readme.md`
  - 后端说明：`002-backend/README.md`
  - 前端说明：`002-frontend/README.md`
- 当前检查日期：`2026-04-04`

## 2. 检查目标

- 核对 PRD、README 与源码实现是否基本一致
- 检查是否满足 JDK 17 运行要求
- 验证后端构建、测试、启动链路
- 验证前端构建、启动链路
- 记录问题、修复动作和最终结论

## 3. 现状速览

### 3.1 文档与目录

- 后端存在独立 PRD 文档，覆盖学生、教师、管理员三类角色与选课、成绩、公告等核心模块
- 后端 README 声明技术栈为 `Spring Boot 3.2.0 + MyBatis + JWT + JDK 17`
- 前端 README 声明技术栈为 `Vue 3 + Vite + Element Plus + Pinia`
- 后端已包含控制器、实体、Mapper、Service、XML 映射与 SQL 初始化脚本
- 前端已包含登录注册页，以及学生端、教师端、管理员端页面与布局

### 3.2 环境前置

- Maven 可正常识别 JDK 17，上一轮环境检查已确认 `mvn -version` 使用 `Java version: 17`
- 本机 `java` 命令未加入 PATH，直接执行 `java -version` 仍可能失败
- Node.js 版本：`v22.17.0`
- npm 版本：`10.9.2`
- 后端默认端口：`8080`
- 前端默认端口：`3001`
- 后端默认依赖本地 MySQL：
  - 数据库：`course_selection_system`
  - 用户名：`root`
  - 密码：`1234`

## 4. 文档完整性检查

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| 项目概述 | 通过 | 总览、PRD、前后端 README 均有基础说明 |
| 角色与功能模块 | 通过 | 三角色职责和核心业务链路均有描述 |
| 技术栈说明 | 通过 | 前后端依赖说明较完整 |
| 数据库初始化说明 | 通过 | `init.sql` 已提供 |
| 默认账号说明 | 通过 | 前后端 README 均列出 |
| 测试说明 | 不完整 | 暂未发现自动化测试与测试数据说明 |
| 启动说明 | 基本通过 | 有基础命令，但数据库和端口冲突说明不足 |
| 文档与源码一致性 | 基本通过 | 核心选课、成绩、用户、公告、系统配置链路基本对应，但 PRD 中 `logout`、操作日志、消息推送等扩展项未在当前源码中看到明确实现 |

## 5. JDK 17 与依赖检查

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| `pom.xml` Java 版本 | 通过 | `source`、`target` 均为 `17` |
| Spring Boot 版本 | 通过 | 使用 `3.2.0`，与 JDK 17 匹配 |
| MyBatis Starter 版本 | 通过 | 使用 `3.0.3`，支持 Spring Boot 3 |
| JWT 依赖 | 通过 | `jjwt 0.12.3` 版本较新 |
| `java` PATH 配置 | 风险 | Maven 可用 JDK 17，但系统命令路径不完整 |
| 数据库依赖 | 通过 | 登录接口抽测时已成功连接 `course_selection_system.users` 表 |

## 6. 实测记录

### 6.1 后端

| 项目 | 命令 | 结果 | 备注 |
| --- | --- | --- | --- |
| 环境检查 | `mvn -version` | 通过 | Maven 绑定到 JDK 17 |
| 测试/构建 | `mvn test -DskipTests=false` | 通过 | 成功编译并完成 Surefire 阶段，但仓库内暂无自动化测试类 |
| 启动验证 | `mvn spring-boot:run` | 通过 | `CourseSelectionApplication` 成功启动在 `8080` |
| 接口验证 | `POST /auth/login` | 通过 | 使用默认账号 `admin/123456` 返回 `200 OK`，并成功查询 `users` 表 |

### 6.2 前端

| 项目 | 命令 | 结果 | 备注 |
| --- | --- | --- | --- |
| 环境检查 | `node -v` / `npm -v` | 通过 | Node 22 / npm 10 |
| 构建验证 | `npm run build` | 通过 | 成功生成 `dist/index.html` |
| 启动验证 | `npm run dev -- --host 127.0.0.1` | 通过 | Vite 可正常启动；首次验证监听 `3001`，复验时因残留进程自动切换到 `3003` |
| 页面验证 | `curl -I http://127.0.0.1:3003` | 通过 | 返回 `HTTP/1.1 200 OK` |

## 7. 当前问题清单

| 编号 | 问题 | 严重度 | 状态 |
| --- | --- | --- | --- |
| 002-1 | 项目暂未发现自动化测试文件，当前主要依赖构建验证、启动验证与接口抽测 | 中 | 未修复 |
| 002-2 | PRD 中提到的 `logout`、操作日志、消息推送等扩展项未在当前源码中看到明确实现 | 中 | 未修复 |
| 002-3 | 用户密码在初始化 SQL 和登录逻辑中均以明文处理，存在明显安全风险 | 高 | 未修复 |
| 002-4 | 本机 `java` 命令未加入 PATH，可能影响手工启动命令与脚本通用性 | 中 | 未修复 |

## 8. 修复记录

- 未修改业务代码。
- 在验证过程中清理过 `spring-boot:run` 与 Vite 残留子进程，避免误判端口占用问题。
- 已完成默认管理员账号登录抽测，确认数据库连接、JWT 登录链路和用户查询逻辑可用。

## 9. 当前结论

- `002` 已完成首轮巡检。
- 文档、PRD 与核心源码实现基本一致，后端依赖配置满足 JDK 17 要求。
- 后端已通过 `mvn test`，前端已通过 `npm run build`。
- 后端可在默认 `8080` 端口启动，并通过默认管理员登录接口验证。
- 前端开发服务可正常启动，页面请求已返回 `200 OK`。
- 当前可判定 `002` 在现有环境下“可通过测试并启动”，但仍存在自动化测试缺失、部分 PRD 扩展功能未实现和明文密码存储等问题。
