<view class="container">
  <view class="banner">
    <swiper autoplay circular indicator-dots>
      <swiper-item>
        <view class="banner-card">网络安全从好习惯开始</view>
      </swiper-item>
      <swiper-item>
        <view class="banner-card banner-card-alt">识别诈骗，保护隐私</view>
      </swiper-item>
    </swiper>
  </view>

  <view class="category-section">
    <view class="section-title">知识分类</view>
    <view class="category-grid">
      <view class="category-item" wx:for="{{categories}}" wx:key="id" bindtap="goCategory" data-id="{{item.id}}" data-name="{{item.name}}">
        <image wx:if="{{item.icon}}" src="{{item.icon}}" mode="aspectFit"></image>
        <view wx:else class="category-icon-placeholder">安</view>
        <text>{{item.name}}</text>
      </view>
    </view>
  </view>

  <view class="article-section">
    <view class="section-title flex-between">
      <text>推荐文章</text>
      <text class="more text-primary" bindtap="goMore">更多</text>
    </view>
    <view class="article-list">
      <view class="article-item card" wx:for="{{articles}}" wx:key="id" bindtap="goArticle" data-id="{{item.id}}">
        <image wx:if="{{item.cover}}" class="article-cover" src="{{item.cover}}" mode="aspectFill"></image>
        <view wx:else class="article-cover placeholder-cover">安全</view>
        <view class="article-info">
          <view class="article-title">{{item.title}}</view>
          <view class="article-meta text-small text-gray">
            <text>{{item.categoryName}}</text>
            <text class="meta-item">浏览 {{item.viewCount}}</text>
          </view>
        </view>
      </view>
    </view>
  </view>
</view>
