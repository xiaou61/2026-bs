server:
  port: 8074

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/craft_sales?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: 123456
  sql:
    init:
      mode: never
  redis:
    host: localhost
    port: 6379
    database: 0

mybatis-plus:
  pagination-db-type: MYSQL
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      id-type: auto

jwt:
  secret: craftSalesSecret2026Token
  expiration: 86400000

file:
  upload-path: uploads/
